---
phase: 260908-i18-add-a-read-only-deal-id-column-as-the-la
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/pipeline/components/DealTable.tsx
autonomous: true

must_haves:
  truths:
    - "The pipeline table shows an 'ID' column as the last column, after Stage."
    - "The ID column displays the deal's id as plain text — muted/monospace styling, not an EditableCell, no click handler, not editable."
  artifacts:
    - src/features/pipeline/components/DealTable.tsx
  key_links:
    - "columns array's new 'id' display column -> info.row.original.id -> rendered as static text"
---

<objective>
Add a read-only "ID" column to the pipeline table (`DealTable.tsx`), displaying each deal's `id` as muted/monospace plain text, positioned as the last column after the existing "Stage" column.

Purpose: Surface the deal's stable identifier in the UI for reference/debugging without making it editable.
Output: `DealTable.tsx`'s `columns` array gains one new trailing display column ("ID"); no other files change.
</objective>

<execution_context>
@C:/gh-repos/eld/.claude/gsd-core/workflows/execute-plan.md
@C:/gh-repos/eld/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/gh-repos/eld/.planning/STATE.md
@C:/gh-repos/eld/.claude/CLAUDE.md
@C:/gh-repos/eld/src/features/pipeline/components/DealTable.tsx

Notes:
- `columns` is a module-level `const` array built with `columnHelper.display(...)` / `columnHelper.accessor(...)` entries (from `legacyCreateColumnHelper<Deal>()`); it currently ends with the "stage" display column (`id: "stage"`, header "Stage", renders `StageSelect`).
- The expand/chevron column is prepended separately at render time via `tableColumns = [expandColumn, ...columns]` — do not touch that line or `expandColumn`; this task only appends to the `columns` array itself, so the new column lands after "Stage" and before nothing (it becomes the true last column).
- `Deal` (from `@/shared/types/deal`) has an `id: string` field, already used throughout this file via `info.row.original.id`.
- No `EditableCell` and no `onClick`/click handler on this new column — it must render as inert static text.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add read-only "ID" display column to DealTable</name>
  <files>src/features/pipeline/components/DealTable.tsx</files>
  <action>
    In `DealTable.tsx`, append one new entry to the end of the module-level `columns` array (after the existing "stage" `columnHelper.display({...})` entry, which stays unchanged): `columnHelper.display({ id: "id", header: "ID", cell: (info) => <span className="text-xs font-mono text-muted-foreground">{info.row.original.id}</span> })`. This is a plain `display` column reading `info.row.original.id` directly — no `EditableCell`, no `onClick`, no `columnHelper.accessor("id", ...)` (accessor would imply the field is sortable/editable data; this is presentation-only). Do not modify `expandColumn`, the `tableColumns` composition line, `colSpan` values, or any other column in the array.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The `columns` array's last entry has `id: "id"`, header "ID", and renders each deal's `id` as static `text-xs font-mono text-muted-foreground` text with no editable/interactive behavior; `npm run build` (tsc -b && vite build) succeeds with no errors.</done>
</task>

</tasks>

<verification>
Run `npm run build` (tsc -b && vite build) — the project's only configured automated check (no test runner). A clean build confirms the new column compiles and `Deal.id` typing is satisfied. Manually spot-check in `npm run dev` that the pipeline table now shows an "ID" column as the last column, after "Stage", displaying each deal's id in muted monospace text with no click/edit affordance.
</verification>

<success_criteria>
- `DealTable.tsx`'s `columns` array ends with a new "ID" display column, positioned after "Stage".
- The ID column renders `info.row.original.id` as static muted/monospace text — not editable, no click handler.
- `npm run build` passes with zero errors.
</success_criteria>

<output>
Create `.planning/quick/260908-i18-add-a-read-only-deal-id-column-as-the-la/260908-i18-SUMMARY.md` when done.
</output>
