---
phase: 260908-i6f-replace-uuid-format-deal-id-generation-w
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/mock/seed-data.ts
  - src/data/mock/mock-deals-repository.ts
autonomous: true

must_haves:
  truths:
    - "Seed deals generated at app startup get a 10-digit numeric string id (e.g. \"3206893803\"), not a UUID."
    - "New deals created via the mock repository's create() get a 10-digit numeric string id, not a UUID."
    - "LineItem ids are unaffected — still faker.string.uuid()-generated, out of scope."
  artifacts:
    - src/data/mock/seed-data.ts
    - src/data/mock/mock-deals-repository.ts
  key_links:
    - "buildSeedDeal() -> faker.string.numeric(10) -> Deal.id"
    - "MockDealsRepository.create() -> local generateDealId() helper -> Deal.id"
---

<objective>
Replace UUID-format Deal id generation with a plain 10-digit numeric-string id format at both call sites: `seed-data.ts`'s `buildSeedDeal()` and `mock-deals-repository.ts`'s `create()`.

Purpose: The user wants Deal ids to look like `3206893803` instead of `de1c6a2c-e8bb-4a65-a8f5-8935ba158010`.
Output: Both Deal-id generation call sites produce 10-digit numeric strings; `Deal.id` stays typed `string`; LineItem ids remain untouched (still UUIDs).
</objective>

<execution_context>
@C:/gh-repos/eld/.claude/gsd-core/workflows/execute-plan.md
@C:/gh-repos/eld/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/gh-repos/eld/.planning/STATE.md
@C:/gh-repos/eld/.claude/CLAUDE.md
@C:/gh-repos/eld/src/data/mock/seed-data.ts
@C:/gh-repos/eld/src/data/mock/mock-deals-repository.ts
@C:/gh-repos/eld/src/shared/types/deal.ts

Notes:
- `Deal.id` (in `shared/types/deal.ts`) is typed `string` and stays `string` — no type changes anywhere in this plan.
- `seed-data.ts`'s `buildSeedDeal()` currently sets the Deal's `id` via `faker.string.uuid()`. The file also has a separate `buildSeedLineItem()` whose `id` is independently `faker.string.uuid()` — that one is out of scope and must not change.
- `mock-deals-repository.ts`'s `create()` currently sets the new Deal's `id` via the Web Crypto random-UUID API (`crypto.randomUUID()`). This file has no faker import today and must not gain one — faker stays confined to the seed-data layer.
- `faker.string.numeric(10)` generates a random 10-digit numeric string (digits 0-9, may include a leading zero) — this is the format the user wants, matching the example `3206893803`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch Deal id seed generation from UUID to 10-digit numeric string</name>
  <files>src/data/mock/seed-data.ts</files>
  <action>
    In `buildSeedDeal()`, change the Deal object's `id: faker.string.uuid(),` line to `id: faker.string.numeric(10),` — a random 10-digit numeric string, matching the format the user requested and consistent with this file's existing faker-based generation style. Do NOT touch `buildSeedLineItem()`'s own `id: faker.string.uuid()` line — LineItem ids stay UUID format, out of scope. Also update the doc comment directly above the `seedDeals` export (the one starting "40 fictional deals distributed...") which currently claims every `id` is a UUID — that statement now only holds for LineItem ids, not Deal ids. Rewrite it to state that Deal `id` is a 10-digit numeric string while LineItem `id` remains a UUID. Keep the rest of that comment (the "never a sequential counter or array index" rationale, and the "always fictional data" sentence) unchanged.
  </action>
  <verify>
    <automated>npm run build && test "$(grep -c 'id: faker.string.numeric(10)' src/data/mock/seed-data.ts)" = "1" && test "$(grep -c 'id: faker.string.uuid()' src/data/mock/seed-data.ts)" = "1"</automated>
  </verify>
  <done>`npm run build` succeeds with zero errors. `buildSeedDeal()`'s `id` field is generated via `faker.string.numeric(10)` (exactly one occurrence in the file). `buildSeedLineItem()`'s `id: faker.string.uuid()` line is unchanged (exactly one remaining occurrence). The `seedDeals` doc comment no longer claims every id is a UUID.</done>
</task>

<task type="auto">
  <name>Task 2: Replace UUID-based Deal id generation in mock repository's create()</name>
  <files>src/data/mock/mock-deals-repository.ts</files>
  <action>
    Add a small local helper function above the `MockDealsRepository` class definition, `generateDealId(): string`, that builds and returns a string of exactly 10 random digit characters (0-9) using `Math.random()` — loop 10 times, each iteration appending `Math.floor(Math.random() * 10)` to an accumulator string, then return the accumulated string. Do NOT import faker into this file — keep this a tiny framework-free inline helper, matching the plain numeric id format now used in `seed-data.ts` (10-digit numeric string, per the user's requested format). Add a one-line doc comment above it noting it replaces the previous UUID-based id assignment in `create()`. In `create()`, replace the existing UUID-based id assignment on the new `deal` object (the Web Crypto random-UUID call) with a call to `generateDealId()`. No other lines in `create()`, `list()`, or `update()` change.
  </action>
  <verify>
    <automated>npm run build && test "$(grep -c 'id: generateDealId()' src/data/mock/mock-deals-repository.ts)" = "1" && ! grep -q 'randomUUID' src/data/mock/mock-deals-repository.ts && ! grep -q 'faker' src/data/mock/mock-deals-repository.ts</automated>
  </verify>
  <done>`npm run build` succeeds with zero errors. `create()`'s `id` field is generated via the new local `generateDealId()` helper (10-digit numeric string, exactly one occurrence). No reference to the Web Crypto random-UUID API remains anywhere in the file. No faker import was added to this file.</done>
</task>

</tasks>

<verification>
Run `npm run build` (tsc -b && vite build) — the project's only configured automated check (no test runner). A clean build confirms both files still type-check with `Deal.id: string` unchanged. Then confirm via targeted grep (not blanket, since `seed-data.ts` legitimately keeps one remaining `faker.string.uuid()` call for LineItem ids):
- `src/data/mock/seed-data.ts`: exactly one `id: faker.string.numeric(10)` (Deal) and exactly one `id: faker.string.uuid()` (LineItem, untouched).
- `src/data/mock/mock-deals-repository.ts`: exactly one `id: generateDealId()`, zero remaining references to the Web Crypto random-UUID API, and no faker import.
Manually spot-check in `npm run dev` that newly seeded deals and a newly added deal (via the Add Deal form) both show a 10-digit numeric id in the pipeline table's "ID" column.
</verification>

<success_criteria>
- Deal ids generated in both `seed-data.ts` and `mock-deals-repository.ts` are 10-digit numeric strings, not UUIDs.
- LineItem id generation is completely untouched (still `faker.string.uuid()`).
- `Deal.id` stays typed `string` in `shared/types/deal.ts` — no type changes.
- `npm run build` passes with zero errors.
</success_criteria>

<output>
Create `.planning/quick/260908-i6f-replace-uuid-format-deal-id-generation-w/260908-i6f-SUMMARY.md` when done.
</output>
