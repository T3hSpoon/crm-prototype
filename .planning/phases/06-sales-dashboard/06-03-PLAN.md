---
phase: 06-sales-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/features/dashboard/dashboard-metrics.ts
  - src/features/dashboard/dashboard-metrics.test.ts
  - src/features/dashboard/components/ConversionFunnelChart.tsx
  - src/features/dashboard/components/ClosedByOwnerChart.tsx
  - src/features/dashboard/components/DashboardPage.tsx
autonomous: true
requirements: [DASH-04, DASH-05]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "computeConversionFunnel guards divide-by-zero: with 0 total deals, every stage's pct is 0, never NaN or Infinity (DASH-04 boundary edge, specless-probe fallback)"
    - "With every deal Won, every funnel stage's count equals the total deal count (100% at every stage) — Won deals count toward every stage regardless of their stored pipelineStage (DASH-04 boundary edge, RESEARCH Assumption A4)"
    - "computeConversionFunnel's pct is a raw 0-1 fraction, never pre-rounded — ConversionFunnelChart displays it via half-up Math.round(pct * 100) integer percent, matching the existing computeWinRate/ForecastPage rounding convention (DASH-04 precision edge, specless-probe fallback)"
    - "ConversionFunnelChart always renders exactly 5 fixed stages (Prospect through Won) regardless of deal count (DASH-04 UI Considerations: zero-one-many)"
    - { statement: "ConversionFunnelChart renders the exact empty-state copy 'No deals in the pipeline yet.' only when the store's deals array itself is empty (DASH-04 UI-SPEC empty)", verification: explicit }
    - "computeClosedByOwnerPerMonth counts both Won (contractSignedDate) and Lost (closeDate) deals per RESEARCH Assumption A3's broader 'closed' reading, consistent with forecast-metrics.ts's existing computeWinRate treating won+lost as closed"
    - { statement: "computeClosedByOwnerPerMonth pre-seeds all 12 trailing months x all 5 roster owners at 0 before accumulating, so a sparse owner/month combination still renders as a real zero segment, never silently skipped (DASH-05 UI Considerations: partial, backstop)", verification: backstop }
    - "ClosedByOwnerChart's month-axis ticks use date-fns format('MMM yyyy'), short enough to never require truncation, and its 5-entry Legend wraps rather than clips (DASH-05 UI Considerations: overflow)"
    - { statement: "ClosedByOwnerChart renders the exact empty-state copy 'No closed deals yet — this chart fills in as deals are won or lost.' when zero Won or Lost deals exist", verification: explicit }
    - "Owner color assignment in ClosedByOwnerChart is positional and fixed (color index = the owner's fixed OWNER_ROSTER index), never a runtime-sorted array's index (RESEARCH anti-pattern, D-03)"
  prohibitions:
    - statement: "MUST NOT present the Conversion Rate funnel as a true historical stage-to-stage conversion rate without disclosing it is a current pipeline snapshot — the data model stores only a deal's current pipelineStage, not a stage-transition history, so no real cohort conversion rate can be computed (RESEARCH Pitfall 2)"
      status: resolved
      verification: test
      resolution: "ConversionFunnelChart renders a fixed disclosure caption beneath its title: 'Current pipeline distribution — not a historical conversion rate.'"
  artifacts:
    - src/features/dashboard/dashboard-metrics.ts (modified — computeConversionFunnel, computeClosedByOwnerPerMonth added)
    - src/features/dashboard/dashboard-metrics.test.ts (modified — new test cases)
    - src/features/dashboard/components/ConversionFunnelChart.tsx
    - src/features/dashboard/components/ClosedByOwnerChart.tsx
    - src/features/dashboard/components/DashboardPage.tsx (modified — Row 2 completed, Row 3 added, all 5 widgets present)
  key_links:
    - "DashboardPage -> computeConversionFunnel(deals) -> ConversionFunnelChart FunnelChart (Row 2, right slot)"
    - "DashboardPage -> computeClosedByOwnerPerMonth(deals, OWNER_ROSTER) -> ClosedByOwnerChart stacked BarChart (Row 3, full width)"
---

<objective>
Complete the Sales Dashboard by adding the Stage Conversion Rate funnel (DASH-04) and the stacked bar of deals closed per period by owner (DASH-05), finishing Row 2 and adding Row 3 of the UI-SPEC layout. All 5 Phase 6 widgets are live after this plan.

Purpose: DASH-04's funnel is the process-health drill-down; DASH-05's stacked bar is the closed-deal-volume-over-time drill-down — both complete the "people + process" and "closed history" story the dashboard promises.
Output: `computeConversionFunnel`/`computeClosedByOwnerPerMonth` added to dashboard-metrics.ts, two new chart components, DashboardPage.tsx finished (all 3 rows, all 5 widgets).
</objective>

<execution_context>
@C:/gh-repos/eld/.claude/gsd-core/workflows/execute-plan.md
@C:/gh-repos/eld/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sales-dashboard/06-CONTEXT.md
@.planning/phases/06-sales-dashboard/06-RESEARCH.md
@.planning/phases/06-sales-dashboard/06-UI-SPEC.md
@.planning/phases/06-sales-dashboard/06-01-SUMMARY.md
@.planning/phases/06-sales-dashboard/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dashboard-metrics.ts — computeConversionFunnel (DASH-04) + computeClosedByOwnerPerMonth (DASH-05)</name>
  <files>src/features/dashboard/dashboard-metrics.ts, src/features/dashboard/dashboard-metrics.test.ts</files>
  <read_first>
    src/features/dashboard/dashboard-metrics.ts (as extended by 06-02), src/features/dashboard/dashboard-config.ts, src/shared/types/deal.ts, src/shared/utils/pipeline-group.ts, src/features/forecast/forecast-metrics.ts (computeLostByStage's "use pipelineStage directly, never toPipelineGroup" pattern), .planning/phases/06-sales-dashboard/06-RESEARCH.md
  </read_first>
  <behavior>
    - computeConversionFunnel(deals): STAGE_ORDER = ["prospect", "lead", "opportunity", "deal"]; returns one entry per stage in STAGE_ORDER plus a final "won" entry, each `{ stage: string, count: number, pct: number }`. A deal counts toward a given non-won stage when `outcome === "won"` (unconditionally — a completed deal is treated as having passed every stage, RESEARCH Assumption A4) OR when `STAGE_ORDER.indexOf(deal.pipelineStage) >= STAGE_ORDER.indexOf(stage)` (covers both open deals at/past that stage and Lost deals, which retain the pipelineStage they fell from). The "won" entry's count is the number of `outcome === "won"` deals. `pct = total === 0 ? 0 : count / total`, where `total = deals.length`. With 0 total deals every pct is 0; with every deal Won every count equals total (pct 1 at every stage).
    - computeClosedByOwnerPerMonth(deals, owners = OWNER_ROSTER, months = TRAILING_MONTHS): pre-seeds `months` (12) month buckets, each `{ key: "yyyy-MM", month: "MMM yyyy", ...ownerCounts }` with every owner in `owners` initialized to 0. For each deal with `outcome === "won"` (bucketed by contractSignedDate, D-04) or `outcome === "lost"` (bucketed by closeDate, D-05) — RESEARCH Assumption A3's broader "closed = won + lost" reading — increments that deal's owner's count in its month's bucket. A month/owner combination with zero closed deals stays 0, never omitted.
  </behavior>
  <action>
    Implement `computeConversionFunnel` per the behavior above with a `STAGE_LABELS` map (`prospect: "Prospect"`, `lead: "Lead"`, `opportunity: "Opportunity"`, `deal: "Deal"`, `won: "Won"`) for the returned `stage` strings. Add a JSDoc documenting: (a) this is a cumulative current-state snapshot, not a true historical stage-to-stage conversion rate (RESEARCH Pitfall 2 — no stage-transition history exists in the data model); (b) `pct` is intentionally a raw fraction, rounded only at the display layer via `Math.round(pct * 100)`, matching `forecast-metrics.ts`'s `computeWinRate`/`ForecastPage.tsx`'s existing `Math.round(winRate * 100)` convention. Implement `computeClosedByOwnerPerMonth` per the behavior above using `eachMonthOfInterval`/`startOfMonth`/`subMonths`/`format` from `date-fns` (add to this file's existing date-fns import) and `OWNER_ROSTER`/`TRAILING_MONTHS` from `@/features/dashboard/dashboard-config`.
  </action>
  <verify>
    <automated>npx vitest run src/features/dashboard/dashboard-metrics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - computeConversionFunnel([]) returns 5 entries all with pct === 0 (no NaN, no divide-by-zero throw)
    - computeConversionFunnel with every deal outcome "won" returns 5 entries where every count equals the input length and every pct equals 1
    - computeConversionFunnel with a mix of stages returns a raw (non-rounded) fractional pct, e.g. 2/3 deals reaching a stage yields pct === 0.6666666666666666, not 0.67 or 67
    - computeClosedByOwnerPerMonth always returns exactly 12 month rows, each with all 5 OWNER_ROSTER keys present (defaulting to 0)
    - npx vitest run src/features/dashboard/dashboard-metrics.test.ts passes
  </acceptance_criteria>
  <done>Both functions implemented, tested against their boundary (0 total / all-won) and precision (unrounded fraction) cases, and pre-seeded so no month/owner combination is silently dropped.</done>
</task>

<task type="auto">
  <name>Task 2: ConversionFunnelChart (DASH-04) and ClosedByOwnerChart (DASH-05) components</name>
  <files>src/features/dashboard/components/ConversionFunnelChart.tsx, src/features/dashboard/components/ClosedByOwnerChart.tsx</files>
  <read_first>
    src/features/forecast/components/LostBreakdownChart.tsx, src/features/dashboard/dashboard-metrics.ts (as extended by Task 1), src/features/dashboard/dashboard-config.ts, .planning/phases/06-sales-dashboard/06-UI-SPEC.md
  </read_first>
  <action>
    Create `src/features/dashboard/components/ConversionFunnelChart.tsx`: Card/CardContent shell with `<h3 className="text-sm font-semibold">Conversion Rate</h3>` immediately followed by a disclosure caption `<p className="text-xs text-muted-foreground">Current pipeline distribution — not a historical conversion rate.</p>` (mitigates the transparency prohibition above — render this caption unconditionally, both in the empty and populated states). Props `{ data: ReturnType<typeof computeConversionFunnel>; hasDeals: boolean }`. `isEmpty` = `!hasDeals`; empty copy exactly: "No deals in the pipeline yet." When not empty: `ResponsiveContainer` height 320 wrapping a `FunnelChart` with a `Tooltip` whose `formatter` renders `${count} deals (${Math.round(pct * 100)}%)` per stage, and a `Funnel dataKey="count" nameKey="stage" data={data} isAnimationActive` containing a `LabelList position="right" dataKey="stage" fill="var(--muted-foreground)" stroke="none"` and one `Cell` per entry (`data.map((entry, i) => <Cell key={entry.stage} fill={`var(--chart-funnel-${i + 1})`} />)`) so each of the 5 stages gets its own step of the validated ordinal ramp. Create `src/features/dashboard/components/ClosedByOwnerChart.tsx`: Card/CardContent shell with `<h3 className="text-sm font-semibold">Closed Deals by Owner</h3>`. Props `{ data: ReturnType<typeof computeClosedByOwnerPerMonth>; owners: readonly string[]; hasClosedDeals: boolean }`. `isEmpty` = `!hasClosedDeals`; empty copy exactly: "No closed deals yet — this chart fills in as deals are won or lost." When not empty: `ResponsiveContainer` height 300 wrapping a `BarChart` with `CartesianGrid strokeDasharray="3 3" stroke="var(--chart-lost-grid)"`, `XAxis dataKey="month"`, `YAxis allowDecimals={false}`, `Tooltip`, `Legend`, and `owners.map((owner, i) => <Bar key={owner} dataKey={owner} name={owner} stackId="closed" fill={`var(--chart-owner-${i + 1})`} />)` — the color index always follows `owners`' fixed array position (OWNER_ROSTER order), never a runtime-sorted index.
  </action>
  <verify>
    <automated>npm run build && ! grep -rq "dangerouslySetInnerHTML" src/features/dashboard/components/</automated>
  </verify>
  <acceptance_criteria>
    - ConversionFunnelChart.tsx contains the exact string "Current pipeline distribution — not a historical conversion rate." (both empty and populated states)
    - ConversionFunnelChart.tsx contains the exact string "No deals in the pipeline yet."
    - ClosedByOwnerChart.tsx contains the exact string "No closed deals yet — this chart fills in as deals are won or lost."
    - ClosedByOwnerChart.tsx renders one `<Bar>` per entry in `owners`, all sharing `stackId="closed"`
    - No file under src/features/dashboard/components/ uses raw-HTML content insertion for any dynamic value (React's default JSX escaping is used throughout)
    - npm run build succeeds
  </acceptance_criteria>
  <done>Both components render correctly against real computeConversionFunnel/computeClosedByOwnerPerMonth output, with UI-SPEC-exact empty-state copy, the funnel's transparency disclosure, and positional owner coloring.</done>
</task>

<task type="auto">
  <name>Task 3: Wire ConversionFunnelChart + ClosedByOwnerChart into DashboardPage (completes all 5 widgets)</name>
  <files>src/features/dashboard/components/DashboardPage.tsx</files>
  <read_first>
    src/features/dashboard/components/DashboardPage.tsx (as extended by 06-02), src/features/dashboard/dashboard-config.ts, .planning/phases/06-sales-dashboard/06-UI-SPEC.md
  </read_first>
  <action>
    Edit `DashboardPage.tsx`'s existing Row 2 grid div to add `<ConversionFunnelChart data={computeConversionFunnel(deals)} hasDeals={deals.length > 0} />` as the second child, alongside the existing `<OwnerLeaderboard />`. Add a new Row 3 grid div (`grid grid-cols-1 gap-4`, single column — full width per UI-SPEC) after Row 2, containing `<ClosedByOwnerChart data={computeClosedByOwnerPerMonth(deals, OWNER_ROSTER)} owners={OWNER_ROSTER} hasClosedDeals={deals.some((d) => d.outcome === "won" || d.outcome === "lost")} />`. Import both new components, both new dashboard-metrics functions, and `OWNER_ROSTER` from `@/features/dashboard/dashboard-config` at the top of the file.
  </action>
  <verify>
    <automated>npm run build && npx vitest run src/features/dashboard/dashboard-metrics.test.ts src/data/mock/seed-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - DashboardPage.tsx renders all 5 widgets: UnitTargetGauge, TargetVsActualChart (Row 1), OwnerLeaderboard, ConversionFunnelChart (Row 2), ClosedByOwnerChart (Row 3, full width)
    - Row 2's grid div contains exactly 2 children; Row 3's grid div contains exactly 1 child
    - `! grep -rq "dangerouslySetInnerHTML" src/features/dashboard/` (phase-wide confirmation across all 3 plans' components)
    - npm run build succeeds; all dashboard-metrics.ts and seed-data.ts tests pass
  </acceptance_criteria>
  <done>Dashboard tab shows all 5 widgets (Gauge, Target-vs-Actual, Leaderboard, Conversion Funnel, Closed-by-Owner), all driven by real seed data, matching UI-SPEC's full 3-row layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| In-memory store -> React render tree | Same single boundary as 06-01/06-02: `usePipelineStore`'s already-loaded `Deal[]` flowing into pure derived-data functions and Recharts SVG output. No network, no user input, no auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering / Information Disclosure | ConversionFunnelChart.tsx, ClosedByOwnerChart.tsx | low | mitigate | React's default JSX escaping covers all dynamic text; no raw-HTML tooltip/label content added — verified phase-wide by Task 3's final grep check |
| T-06-03 | Denial of Service (client-side crash via NaN/Infinity render) | computeConversionFunnel, computeClosedByOwnerPerMonth | medium | mitigate | computeConversionFunnel guards divide-by-zero (total === 0 -> pct 0); computeClosedByOwnerPerMonth pre-seeds every bucket at a real 0, no division occurs |
| T-06-04 | Tampering (data-integrity: misleading business metrics) | DashboardPage.tsx | medium | mitigate | Task 3 continues reading `deals` from the same unfiltered `usePipelineStore` selector established in 06-01 — no new read path introduced |
| T-06-06 | Information Disclosure (misleading data semantics) | ConversionFunnelChart.tsx | medium | mitigate | Funnel is a current-state snapshot, not a true historical conversion rate — disclosed via the fixed caption verified in Task 2's acceptance criteria (also tracked as a resolved must_haves.prohibitions item) |

No other STRIDE-relevant threat patterns apply (RESEARCH.md Security Domain).
</threat_model>

<verification>
- `npx vitest run src/features/dashboard/dashboard-metrics.test.ts src/data/mock/seed-data.test.ts` — full suite passes
- `npm run build` succeeds
- `! grep -rq "dangerouslySetInnerHTML" src/features/dashboard/` — no raw-HTML insertion anywhere in the new feature directory
- Manual: Dashboard tab shows all 5 widgets populated with real data; funnel percentages monotonically decrease or stay flat stage-to-stage; stacked bar shows 5 owner colors across 12 months
</verification>

<success_criteria>
- DASH-04: Conversion funnel shows all 5 stages (Prospect through Won) with correct cumulative counts/percentages, including the 0-total and all-won boundaries
- DASH-05: Stacked bar shows deals closed (won + lost) per month, segmented by owner, with positional/fixed owner coloring
- All 5 Phase 6 widgets are live on the Dashboard tab
</success_criteria>

<output>
Create `.planning/phases/06-sales-dashboard/06-03-SUMMARY.md` when done
</output>
