---
phase: 06-sales-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/mock/seed-data.ts
  - src/data/mock/seed-data.test.ts
  - src/features/dashboard/dashboard-config.ts
  - src/features/dashboard/dashboard-metrics.ts
  - src/features/dashboard/dashboard-metrics.test.ts
  - src/features/dashboard/components/UnitTargetGauge.tsx
  - src/features/dashboard/components/DashboardPage.tsx
  - src/app/App.tsx
  - src/index.css
autonomous: true
requirements: [DASH-01]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "computeWonUnits(deals) sums `units` across ALL line items (product + service, not service-only) of deals with outcome === 'won' — a new function, distinct from deal-metrics.ts's service-only computeQuantity (DASH-01, RESEARCH Pitfall 4/Assumption A1)"
    - "computeUnitTargetPct(actual, target) returns exactly 1 when actual === target or actual > target (never overflows past 100%), and 0 when target === 0 — divide-by-zero-safe (DASH-01 adjacency + empty edges, specless-probe fallback)"
    - "computeWonUnits is order-invariant: the sum does not depend on the order of deals in the input array (DASH-01 ordering edge, specless-probe fallback)"
    - { statement: "UnitTargetGauge renders the exact empty-state copy 'No Won deals yet — the gauge fills in once deals close.' when zero Won deals exist, replacing the RadialBarChart entirely (DASH-01 UI-SPEC UI Considerations: empty)", verification: explicit }
    - "Every one of the 5 fixed OWNER_ROSTER names has at least one Won deal in the seeded dataset, verified deterministically via a fixed faker.seed (D-02's explicit acceptance criterion)"
    - "Dashboard tab is mounted alongside Pipeline and Forecast via the same always-mounted / `hidden`-toggle pattern — switching tabs never remounts or resets PipelineBoard/ForecastPage state"
    - "DashboardPage reads usePipelineStore((s) => s.deals) directly — the full, unfiltered array, never a filtered subset (RESEARCH Pitfall 5)"
    - "Every outcome === 'lost' deal's closeDate falls within the trailing 12 months up to today (D-05/D-06); every still-open deal's closeDate remains forward-looking (0-90 days ahead, unchanged)"
  prohibitions:
    - statement: "MUST NOT use a real iDrive employee's or any real identifiable person's name for the fixed 5-name OWNER_ROSTER — names must read as generic/fictional, consistent with seed-data.ts's existing 'never real prospect/company/person data' rule"
      status: resolved
      verification: judgment
      resolution: "Resolved by construction: the roster (Priya Nair, Marcus Webb, Elena Torres, Devon Clarke, Sana Malik) uses generic, unaffiliated names with no connection to any real iDrive employee or public figure."
  artifacts:
    - src/features/dashboard/dashboard-config.ts
    - src/features/dashboard/dashboard-metrics.ts
    - src/features/dashboard/dashboard-metrics.test.ts
    - src/features/dashboard/components/UnitTargetGauge.tsx
    - src/features/dashboard/components/DashboardPage.tsx
    - src/data/mock/seed-data.ts (modified)
    - src/data/mock/seed-data.test.ts
    - src/app/App.tsx (modified)
    - src/index.css (modified)
  key_links:
    - "App.tsx tab state -> DashboardPage mount -> usePipelineStore().deals -> computeWonUnits/computeUnitTargetPct -> UnitTargetGauge RadialBarChart"
    - "seed-data.ts faker.seed(20260917) -> deterministic OWNER_ROSTER assignment -> Leaderboard/stacked-bar color mapping in 06-02/06-03"
---

<objective>
Lay the Phase 6 foundation — the seed-data changes (D-01 fixed owner roster, D-02 seed-volume increase, D-04/D-05/D-06 outcome-aware historical dates) that every later widget depends on — and prove the full stack end-to-end with ONE working widget: the Unit Sales Target gauge (DASH-01). This is the phase's tracer slice: seed data -> derived-metrics module -> chart component -> page -> tab navigation, wired for real and verified, before Plans 06-02/06-03 expand to the remaining 4 widgets.

Purpose: D-01 and D-05 are marked "costly" reversibility in CONTEXT.md — every other Phase 6 widget assumes a closed 5-name owner set and an outcome-dependent closeDate meaning. Getting this foundation right in one focused plan, verified against one real widget, de-risks the remaining 4 widgets built on top of it.
Output: `src/features/dashboard/` feature directory scaffolded (dashboard-config.ts, dashboard-metrics.ts, UnitTargetGauge.tsx, DashboardPage.tsx), seed-data.ts updated per D-01/02/04/05/06, a third "Dashboard" tab in App.tsx showing a working Unit Sales Target gauge, and new CSS chart-color tokens for all 5 widgets (added once here to avoid re-touching index.css in later waves).
</objective>

<assumption_delta_decision>
Noun: sales-rep "owner" identity on a Deal.
Decision: promote.
Rationale: D-01 replaces the previous per-deal `faker.person.fullName()` (an unbounded, ad-hoc random string generated fresh per deal) with a fixed, closed 5-entry `OWNER_ROSTER` promoted to the sole identity source for `Deal.owner`. This is a promote, not an add-alongside: the random-name generator is removed from `buildSeedDeal`, not kept as a fallback or secondary path. Every downstream Phase 6 owner-based widget (Leaderboard DASH-03, stacked bar DASH-05) and the new D-03 color palette are keyed to this fixed roster's index order as their primary identity source, per CONTEXT.md's explicit "replacing" language for D-01.
</assumption_delta_decision>

<execution_context>
@C:/gh-repos/eld/.claude/gsd-core/workflows/execute-plan.md
@C:/gh-repos/eld/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sales-dashboard/06-CONTEXT.md
@.planning/phases/06-sales-dashboard/06-RESEARCH.md
@.planning/phases/06-sales-dashboard/06-UI-SPEC.md
@.planning/phases/06-sales-dashboard/06-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Seed-data foundation (D-01/02/04/05/06) + dashboard-config + computeWonUnits + Unit Sales Target gauge, end-to-end</name>
  <reversibility rating="costly">D-01 fixes owner identity to a closed 5-name roster and D-05 splits closeDate's meaning by outcome; every downstream Phase 6 widget (06-02/06-03) assumes both, so reverting either later means re-touching all of them, not just seed-data.ts.</reversibility>
  <files>src/features/dashboard/dashboard-config.ts, src/data/mock/seed-data.ts, src/features/dashboard/dashboard-metrics.ts, src/features/dashboard/dashboard-metrics.test.ts, src/features/dashboard/components/UnitTargetGauge.tsx, src/features/dashboard/components/DashboardPage.tsx, src/app/App.tsx, src/index.css</files>
  <read_first>
    src/data/mock/seed-data.ts, src/shared/types/deal.ts, src/shared/utils/line-items.ts, src/shared/utils/deal-metrics.ts, src/features/forecast/forecast-metrics.ts, src/features/forecast/components/ForecastPage.tsx, src/features/forecast/components/LostBreakdownChart.tsx, src/app/App.tsx, src/index.css, .planning/phases/06-sales-dashboard/06-UI-SPEC.md, .planning/phases/06-sales-dashboard/06-RESEARCH.md
  </read_first>
  <behavior>
    - computeWonUnits(deals): sums `units` across ALL line items (product + service) of deals with outcome === "won"; open/lost deals contribute 0; empty deals array -> 0; sum is order-invariant (shuffling the input array yields the identical total).
    - computeUnitTargetPct(actual, target): target === 0 -> 0 (never NaN/Infinity); actual === target -> exactly 1; actual > target -> clamped to 1 (never overflows); actual < target -> actual / target.
  </behavior>
  <action>
    1. Create `src/features/dashboard/dashboard-config.ts` exporting: `OWNER_ROSTER = ["Priya Nair", "Marcus Webb", "Elena Torres", "Devon Clarke", "Sana Malik"] as const` (D-01 — the sole owner-identity source, per the assumption-delta decision above); `TRAILING_MONTHS = 12` (D-06); `MONTHLY_UNIT_TARGET = 40` (units/month — Claude's Discretion per RESEARCH Assumption A2, calibrated so the gauge and 06-02's monthly chart sit near 80-110% most months against the finalized 150-deal, faker-seed-20260917 dataset); `ANNUAL_UNIT_TARGET = MONTHLY_UNIT_TARGET * TRAILING_MONTHS` (480 — DASH-01's gauge target).
    2. In `src/data/mock/seed-data.ts`: import `OWNER_ROSTER` and `TRAILING_MONTHS` from `@/features/dashboard/dashboard-config`; add `subMonths` to the existing `import { addMonths, format } from "date-fns"` line. Add `export const SEED_DEAL_COUNT = 150;` near the existing `STAGES`/`FREQUENCIES`/`CURRENCIES` constants (D-02). Add `faker.seed(20260917);` as the first executable statement in the module (before `buildSeedLineItem`/`buildSeedDeal`/`seedDeals` run), with a comment explaining this makes the generated dataset deterministic and reproducible across runs/CI, which is what makes Task 2's "every roster owner has >=1 Won deal" assertion a reliable automated test instead of a flaky probabilistic one. Replace `owner: faker.person.fullName()` with `owner: faker.helpers.arrayElement(OWNER_ROSTER)` (D-01 — remove the random-name call entirely, do not keep it as a fallback). Replace the unconditional `closeDate: faker.date.soon({ days: 90 }).toISOString()` with a 3-way outcome-aware value: for `isLost` deals, `faker.date.between({ from: subMonths(new Date(), TRAILING_MONTHS), to: new Date() }).toISOString()` (D-05/D-06, backdated across the trailing 12 months); for `isWon` deals, a second independent `faker.date.between({ from: subMonths(new Date(), TRAILING_MONTHS), to: new Date() }).toISOString()` call (so a Won deal never shows a future closeDate elsewhere in Pipeline/Forecast, even though DASH-01/02 read `contractSignedDate` instead per D-04); for still-`open` deals, keep the existing forward-looking `faker.date.soon({ days: 90 }).toISOString()` unchanged. Change `contractSignedDate: isWon ? faker.date.recent({ days: 90 }).toISOString().slice(0, 10) : undefined` to use `faker.date.between({ from: subMonths(new Date(), TRAILING_MONTHS), to: new Date() })` in place of `faker.date.recent({ days: 90 })` (D-06). Leave `contractStartDate` unchanged — D-06 does not name it, out of scope. Update both occurrences of the deal count literal (the `faker.helpers.multiple(buildSeedDeal, { count: 40 })` branch and the `Array.from({ length: 40 }, buildSeedDeal)` fallback branch) to `SEED_DEAL_COUNT`.
    3. Create `src/features/dashboard/dashboard-metrics.ts` opening with the same "pure, never-stored, no store/repository import" doc-comment convention as `forecast-metrics.ts`. Export `computeWonUnits(deals: Deal[]): number` per the behavior above, with a JSDoc explicitly contrasting it against `deal-metrics.ts`'s service-only `computeQuantity` (RESEARCH Pitfall 4/Assumption A1 — this function intentionally includes product-type line items too). Export `computeUnitTargetPct(actual: number, target: number): number` per the behavior above.
    4. In `src/index.css`, add ALL of the new chart-color tokens now (gauge + owner + funnel, not just the two the gauge needs — this avoids a second `index.css` edit in Plans 06-02/06-03): immediately after the existing `--chart-lost-bar`/`--chart-lost-grid` lines in both `:root` and `.dark`, add `--chart-gauge-fill`, `--chart-gauge-track`, `--chart-owner-1` through `--chart-owner-5`, `--chart-funnel-1` through `--chart-funnel-5`, using the exact hex values from 06-UI-SPEC.md's "Chart-specific palette" table. Do not touch or reuse the existing generic `--chart-1..5` tokens (D-03 explicitly excludes them).
    5. Create `src/features/dashboard/components/UnitTargetGauge.tsx`: a `Card`/`CardContent`-wrapped widget following `LostBreakdownChart.tsx`'s exact title/empty-state-ternary shape (`<h3 className="text-sm font-semibold">Unit Sales Target</h3>`). Props `{ actual: number; target: number }`. `isEmpty` when the caller has no Won deals (pass a separate `hasWonDeals: boolean` prop, or compute `actual === 0 && target > 0` is NOT sufficient since a Won deal could sum to 0 units — pass `wonDealCount: number` as a third prop and use `wonDealCount === 0` for `isEmpty`); empty copy exactly: "No Won deals yet — the gauge fills in once deals close." When not empty: `ResponsiveContainer` height 220 wrapping a `RadialBarChart` with `startAngle={180} endAngle={0}`, `innerRadius="70%" outerRadius="100%"`, one `RadialBar dataKey="value"` fed a single-row data array `[{ name: "units", value: computeUnitTargetPct(actual, target) * 100 }]`, `background={{ fill: "var(--chart-gauge-track)" }}`, `fill="var(--chart-gauge-fill)"`, `cornerRadius={6}`. Below the `ResponsiveContainer`, render a centered `<div>` with the actual/target numbers as `${actual} / ${target} units` in Display tier (`text-2xl font-semibold tabular-nums`) plus a `text-xs text-muted-foreground` caption "Annual Unit Target (trailing 12 months)".
    6. Create `src/features/dashboard/components/DashboardPage.tsx` mirroring `ForecastPage.tsx`'s docstring/shape: reads `const deals = usePipelineStore((s) => s.deals)` directly (never a filtered subset, RESEARCH Pitfall 5). Container `<div className="mx-auto flex w-[95%] flex-col gap-6 px-6 py-8">`, `<h1 className="font-heading text-lg font-semibold">Dashboard</h1>`, then a Row-1 grid `<div className="grid grid-cols-1 gap-4 lg:grid-cols-2">` containing only `<UnitTargetGauge actual={computeWonUnits(deals)} target={ANNUAL_UNIT_TARGET} wonDealCount={deals.filter((d) => d.outcome === "won").length} />` for this task. Do not add placeholder elements for Row 1's second slot or Rows 2-3 — Plans 06-02/06-03 add real widgets there without any architectural change to this shell.
    7. In `src/app/App.tsx`: import `DashboardPage`; widen `useState<"pipeline" | "forecast">` to `useState<"pipeline" | "forecast" | "dashboard">` (both the generic and the `onValueChange` cast); add `<TabsTrigger value="dashboard">Dashboard</TabsTrigger>` immediately after the Forecast trigger; add a third always-mounted `<div className={tab === "dashboard" ? "" : "hidden"}><DashboardPage /></div>` after the Forecast container, matching the existing mount-always/`hidden`-toggle pattern exactly (no router, no remount-on-switch).
  </action>
  <verify>
    <automated>npx vitest run src/features/dashboard/dashboard-metrics.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/features/dashboard/dashboard-config.ts exports OWNER_ROSTER (exactly 5 entries), TRAILING_MONTHS === 12, MONTHLY_UNIT_TARGET === 40, ANNUAL_UNIT_TARGET === 480
    - `! grep -q "faker.person.fullName" src/data/mock/seed-data.ts` (the random-name generator is fully removed, not kept as a fallback)
    - src/data/mock/seed-data.ts's closeDate assignment is a 3-way outcome-aware expression (isLost / isWon / open), not a single unconditional `faker.date.soon` call
    - `npx vitest run src/features/dashboard/dashboard-metrics.test.ts` passes, covering computeWonUnits' empty-input and order-invariance cases and computeUnitTargetPct's target===0 and actual===target/actual>target clamping cases
    - `npm run build` succeeds (tsc -b && vite build) with the widened 3-tab union and the new src/features/dashboard directory
    - Dashboard tab click shows UnitTargetGauge without unmounting PipelineBoard/ForecastPage (grep for a third `hidden`-toggle div wrapping `<DashboardPage />` in src/app/App.tsx)
  </acceptance_criteria>
  <done>Dashboard tab renders a working Unit Sales Target gauge showing real actual/target units computed from the (now 150-deal, 5-owner) seed data; computeWonUnits/computeUnitTargetPct pass their edge-case tests; the app builds.</done>
</task>

<task type="auto">
  <name>Task 2: Seed-data invariant tests (D-01/02/05/06 automated spot-check)</name>
  <files>src/data/mock/seed-data.test.ts</files>
  <read_first>
    src/data/mock/seed-data.ts (as modified by Task 1), src/features/dashboard/dashboard-config.ts, src/shared/types/deal.ts, src/features/forecast/forecast-breakdown.test.ts
  </read_first>
  <action>
    Create `src/data/mock/seed-data.test.ts` importing `{ seedDeals, SEED_DEAL_COUNT }` from `./seed-data` and `{ OWNER_ROSTER, TRAILING_MONTHS }` from `@/features/dashboard/dashboard-config`, using `describe`/`it`/`expect` from `vitest` (matching `forecast-breakdown.test.ts`'s import style). Because Task 1's `faker.seed(20260917)` call makes generation deterministic, assert as hard facts (not probabilistic spot-checks): (a) `seedDeals.length === SEED_DEAL_COUNT`; (b) every deal's `owner` is one of `OWNER_ROSTER`'s 5 values; (c) every one of the 5 `OWNER_ROSTER` names appears as the `owner` of at least one deal with `outcome === "won"` (D-02's explicit "each rep has enough won deals to be visible on the Leaderboard" criterion); (d) every `outcome === "lost"` deal's `closeDate`, parsed as a Date, is `<= now` and `>= (now minus TRAILING_MONTHS months)` (D-05/D-06 backdating); (e) every `outcome === "open"` deal's `closeDate` is `>= now` (still forward-looking, unchanged); (f) every `outcome === "won"` deal has a defined `contractSignedDate` that is `<= now` and `>= (now minus TRAILING_MONTHS months)` (D-06).
  </action>
  <verify>
    <automated>npx vitest run src/data/mock/seed-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - seedDeals.length equals SEED_DEAL_COUNT (150)
    - every OWNER_ROSTER name has >=1 Won deal (asserted, not sampled)
    - every lost deal's closeDate falls within (now - 12mo, now]; every open deal's closeDate is >= now
    - every won deal's contractSignedDate falls within (now - 12mo, now]
  </acceptance_criteria>
  <done>src/data/mock/seed-data.test.ts passes, proving deterministically that every roster owner has >=1 Won deal, all lost/won "closed" dates are backdated within the trailing 12 months, and open deals remain forward-looking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| In-memory store -> React render tree | The only boundary this phase touches: `usePipelineStore`'s already-loaded, in-process `Deal[]` flowing into pure derived-data functions and then into Recharts SVG output. No network, no user input form, no auth boundary exists anywhere in this phase (frontend-only prototype, PROJECT.md constraint). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering / Information Disclosure | UnitTargetGauge.tsx and every later chart/label/tooltip component | low | mitigate | React's default JSX text escaping is relied on for every dynamic value rendered (owner names, computed numbers); no chart/label/tooltip in this phase renders raw HTML strings — verified by the absence of any raw-HTML-insertion API call in `src/features/dashboard/` |
| T-06-02 | Denial of Service | src/data/mock/seed-data.ts | low | mitigate | Seed volume is fixed at 150 (Task 1), well below the "hundreds/thousands" threshold CLAUDE.md flags for needing `@tanstack/react-virtual` row virtualization — every seeded deal still flows through the existing Pipeline table unchanged |
| T-06-03 | Denial of Service (client-side crash via NaN/Infinity render) | dashboard-metrics.ts ratio functions | medium | mitigate | Every ratio this phase computes (computeUnitTargetPct here; computeConversionFunnel's pct in 06-03) has an explicit divide-by-zero guard mirroring `forecast-metrics.ts`'s established `computeWinRate` pattern, verified by dedicated unit tests |
| T-06-04 | Tampering (data-integrity: misleading business metrics) | DashboardPage.tsx | medium | mitigate | DashboardPage reads `usePipelineStore((s) => s.deals)` directly, never a Pipeline-tab-filtered subset — codified as an acceptance criterion on Task 1 and re-affirmed in must_haves |
| T-06-05 | Information Disclosure (mock data mistaken for real identity) | dashboard-config.ts OWNER_ROSTER | low | accept | Fixed roster uses clearly generic/fictional names unaffiliated with any real iDrive employee (see must_haves.prohibitions) — purely cosmetic demo data, never real PII |

No other STRIDE-relevant threat patterns apply given the phase's read-only, backend-less, single-user, mock-data nature (RESEARCH.md Security Domain).
</threat_model>

<verification>
- `npx vitest run src/features/dashboard/dashboard-metrics.test.ts src/data/mock/seed-data.test.ts` — both new test files pass
- `npm run build` succeeds
- Manual: open the app, click the "Dashboard" tab, confirm the Unit Sales Target gauge renders with real (non-zero, non-NaN) actual/target numbers and switching back to Pipeline/Forecast preserves their state
</verification>

<success_criteria>
- Seed data now generates 150 deals from a fixed 5-name owner roster, deterministically (fixed faker seed), with outcome-aware closeDate/contractSignedDate spanning a trailing 12 months
- A third "Dashboard" tab exists alongside Pipeline and Forecast, showing a working Unit Sales Target gauge (DASH-01) driven by real derived data
- Every one of the 5 roster owners has at least one Won deal (proven by an automated test, not a manual spot-check)
</success_criteria>

<output>
Create `.planning/phases/06-sales-dashboard/06-01-SUMMARY.md` when done
</output>
