---
phase: 06-sales-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/features/dashboard/dashboard-metrics.ts
  - src/features/dashboard/dashboard-metrics.test.ts
  - src/features/dashboard/components/TargetVsActualChart.tsx
  - src/features/dashboard/components/OwnerLeaderboard.tsx
  - src/features/dashboard/components/DashboardPage.tsx
autonomous: true
requirements: [DASH-02, DASH-03]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "computeMonthlyWonUnits(deals) buckets Won-deal unit volume by contractSignedDate month (D-04) into exactly 12 trailing months (D-06), reusing computeWonUnits' per-deal unit-summation rather than re-implementing it"
    - "computeOwnerLeaderboard(deals) pre-seeds all 5 OWNER_ROSTER entries at wonValue=0 before accumulating Won-deal `value`, then sorts descending — a rep with zero Won deals still appears as a real $0 row (deviates from RESEARCH.md's raw Pattern-3 sample, which only included owners with >=1 won deal; UI-SPEC's explicit zero-one-many row requires all 5 always)"
    - { statement: "TargetVsActualChart renders the exact empty-state copy 'No closed-deal history yet — this chart fills in as Won deals accumulate month over month.' when every monthly bucket's actual is 0 (DASH-02 UI-SPEC UI Considerations, resolved via cross-reference since the generic specless-probe row for DASH-02 was unclassified)", verification: explicit }
    - { statement: "OwnerLeaderboard renders the exact empty-state copy 'No Won deals yet — the leaderboard fills in once deals close.' when every entry's wonValue is 0", verification: explicit }
    - "OwnerLeaderboard always renders all 5 fixed OWNER_ROSTER rows (bounded 0-5, never fewer/more), including any rep with $0 Won value (DASH-03 UI Considerations: zero-one-many)"
    - "OwnerLeaderboard owner names render without truncation since all fixed roster names are <=14 chars (DASH-03 UI Considerations: overflow/long-text)"
    - { statement: "computeMonthlyWonUnits pre-seeds all 12 trailing months at actual=0 before accumulating, so a month with zero Won deals still renders as a real zero bar in TargetVsActualChart, never silently skipped (DASH-02/DASH-03 UI Considerations: partial, backstop)", verification: backstop }
    - "TargetVsActualChart plots Actual and Target on one shared y-axis (units) — never a dual-axis chart, since both series are the same unit (RESEARCH anti-pattern)"
    - "Leaderboard bars/rows use a single hue (never per-owner rainbow coloring) — DASH-03 is a magnitude comparison of one series, not an identity display (RESEARCH anti-pattern, UI-SPEC Color section)"
  prohibitions: []
  artifacts:
    - src/features/dashboard/dashboard-metrics.ts (modified — computeMonthlyWonUnits, computeOwnerLeaderboard added)
    - src/features/dashboard/dashboard-metrics.test.ts (modified — new test cases)
    - src/features/dashboard/components/TargetVsActualChart.tsx
    - src/features/dashboard/components/OwnerLeaderboard.tsx
    - src/features/dashboard/components/DashboardPage.tsx (modified — Row 1 completed, Row 2 started)
  key_links:
    - "DashboardPage -> computeMonthlyWonUnits(deals) -> TargetVsActualChart ComposedChart (Row 1, right slot)"
    - "DashboardPage -> computeOwnerLeaderboard(deals) -> OwnerLeaderboard sorted rows (Row 2, left slot)"
---

<objective>
Expand the Dashboard tab with the Target vs. Actual Sales chart (DASH-02) and the Owner Leaderboard (DASH-03), completing Row 1 and starting Row 2 of the UI-SPEC layout on top of the seed-data foundation and gauge shipped in 06-01.

Note on the specless-probe fallback: DASH-02 and DASH-03's generic edge-probe rows both came back "unclassified" (the taxonomy is shaped for numeric algorithms, not chart-rendering requirements). Both are resolved here via 06-UI-SPEC.md's own "UI Considerations" table instead — partial/zero-one-many/overflow/long-text rows that directly apply to these two widgets — lifted into `must_haves` above, not silently dropped.

Purpose: Row 1 (gauge + target-vs-actual) is the UI-SPEC's primary focal pair answering "are we hitting the unit target"; Row 2's leaderboard is the first "who" drill-down.
Output: `computeMonthlyWonUnits`/`computeOwnerLeaderboard` added to dashboard-metrics.ts, two new chart/list components, DashboardPage.tsx grown to show 3 of 5 widgets.
</objective>

<execution_context>
@C:/gh-repos/eld/.claude/gsd-core/workflows/execute-plan.md
@C:/gh-repos/eld/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sales-dashboard/06-CONTEXT.md
@.planning/phases/06-sales-dashboard/06-RESEARCH.md
@.planning/phases/06-sales-dashboard/06-UI-SPEC.md
@.planning/phases/06-sales-dashboard/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dashboard-metrics.ts — computeMonthlyWonUnits (DASH-02) + computeOwnerLeaderboard (DASH-03)</name>
  <files>src/features/dashboard/dashboard-metrics.ts, src/features/dashboard/dashboard-metrics.test.ts</files>
  <read_first>
    src/features/dashboard/dashboard-metrics.ts (as created by 06-01, current computeWonUnits/computeUnitTargetPct implementation), src/features/dashboard/dashboard-config.ts, src/shared/types/deal.ts, .planning/phases/06-sales-dashboard/06-RESEARCH.md
  </read_first>
  <behavior>
    - computeMonthlyWonUnits(deals, months = TRAILING_MONTHS): returns an array of exactly `months` (12) entries `{ key: "yyyy-MM", month: "MMM yyyy", actual: number, target: MONTHLY_UNIT_TARGET }` for the trailing `months` months ending at the current month (inclusive), pre-seeded at `actual: 0`, then accumulated by summing each Won deal's line-item units (D-04: contractSignedDate) into its month's bucket; a month with zero Won deals stays `actual: 0` in the output, never omitted.
    - computeOwnerLeaderboard(deals): returns exactly 5 entries `{ owner: string, wonValue: number }`, one per OWNER_ROSTER name (pre-seeded `wonValue: 0`), sorted descending by `wonValue` after summing each Won deal's `value` into its owner's entry; an owner with zero Won deals appears with `wonValue: 0`, never omitted.
  </behavior>
  <action>
    Using `eachMonthOfInterval`, `startOfMonth`, `subMonths`, `format` from `date-fns` (already imported project-wide, add to this file's own import), implement `computeMonthlyWonUnits` per the behavior above — reuse the existing `computeWonUnits` (call it as `computeWonUnits([deal])` per bucketed deal, or factor a shared private per-deal unit-sum helper if you prefer; do not re-write the line-item summation loop from scratch). Import `OWNER_ROSTER`, `MONTHLY_UNIT_TARGET`, `TRAILING_MONTHS` from `@/features/dashboard/dashboard-config`. Implement `computeOwnerLeaderboard` per the behavior above using a `Map` pre-seeded from `OWNER_ROSTER` at 0, accumulating `d.value` for `d.outcome === "won"`, then `[...map.entries()].map(...).sort((a, b) => b.wonValue - a.wonValue)`. Add JSDoc to both functions documenting the "always pre-seed every bucket/owner, never silently omit a zero" contract explicitly (this is the load-bearing correction vs. RESEARCH.md's raw Pattern 3 sample, which only aggregated owners with >=1 Won deal).
  </action>
  <verify>
    <automated>npx vitest run src/features/dashboard/dashboard-metrics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - computeMonthlyWonUnits(deals) always returns exactly 12 entries regardless of how many months have zero Won deals
    - computeOwnerLeaderboard(deals) always returns exactly 5 entries (one per OWNER_ROSTER name) regardless of how many owners have zero Won deals
    - computeOwnerLeaderboard's output is sorted descending by wonValue
    - npx vitest run src/features/dashboard/dashboard-metrics.test.ts passes, including a case asserting a month/owner with 0 Won deals still appears as an explicit 0 entry (not omitted)
  </acceptance_criteria>
  <done>Both functions implemented, tested, and pre-seeded so no month or owner is ever silently dropped from the output.</done>
</task>

<task type="auto">
  <name>Task 2: TargetVsActualChart (DASH-02) and OwnerLeaderboard (DASH-03) components</name>
  <files>src/features/dashboard/components/TargetVsActualChart.tsx, src/features/dashboard/components/OwnerLeaderboard.tsx</files>
  <read_first>
    src/features/forecast/components/LostBreakdownChart.tsx, src/features/forecast/components/StatTile.tsx, src/features/forecast/components/ForecastPage.tsx, src/features/dashboard/dashboard-metrics.ts (as extended by Task 1), .planning/phases/06-sales-dashboard/06-UI-SPEC.md
  </read_first>
  <action>
    Create `src/features/dashboard/components/TargetVsActualChart.tsx`: Card/CardContent shell with `<h3 className="text-sm font-semibold">Target vs. Actual Sales</h3>` (matching `LostBreakdownChart.tsx`'s title/empty-state-ternary shape exactly). Prop `{ data: ReturnType<typeof computeMonthlyWonUnits> }`. `isEmpty` = `data.every((bucket) => bucket.actual === 0)`; empty copy exactly: "No closed-deal history yet — this chart fills in as Won deals accumulate month over month." When not empty: `ResponsiveContainer` height 300 wrapping a `ComposedChart` with `CartesianGrid strokeDasharray="3 3" stroke="var(--chart-lost-grid)"`, `XAxis dataKey="month"`, `YAxis allowDecimals={false}` with `label={{ value: "Units", angle: -90, position: "insideLeft" }}`, `Tooltip`, `Legend`, a `Bar dataKey="actual" name="Actual" fill="var(--chart-gauge-fill)" radius={[4, 4, 0, 0]}`, and a `Line dataKey="target" name="Target" stroke="var(--chart-lost-grid)" strokeDasharray="4 4" strokeWidth={2} dot={false}` — both series share one y-axis, never a dual-axis composition. Create `src/features/dashboard/components/OwnerLeaderboard.tsx`: Card/CardContent shell with `<h3 className="text-sm font-semibold">Owner Leaderboard</h3>`. Prop `{ entries: ReturnType<typeof computeOwnerLeaderboard> }`. `isEmpty` = `entries.every((e) => e.wonValue === 0)`; empty copy exactly: "No Won deals yet — the leaderboard fills in once deals close." When not empty: render `entries` as up to 5 flex rows, each with a rank badge `rounded-full bg-background/80 px-2 py-1 text-xs font-medium leading-none` showing `index + 1`, the owner name (Body/regular, `text-sm`), and the currency-formatted `wonValue` right-aligned (Display tier styling scaled to row context, `text-sm font-semibold tabular-nums`) via a module-scope `currencyFormatter = new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 })` matching `ForecastPage.tsx`'s exact formatter config. All 5 rows render the same single hue for their value text/rank badge — never a per-owner color (DASH-03 is a magnitude comparison, not an identity display).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - TargetVsActualChart.tsx contains the exact string "No closed-deal history yet — this chart fills in as Won deals accumulate month over month."
    - OwnerLeaderboard.tsx contains the exact string "No Won deals yet — the leaderboard fills in once deals close."
    - TargetVsActualChart.tsx has exactly one Bar and one Line element sharing a single YAxis (no second YAxis/dual-axis)
    - OwnerLeaderboard.tsx renders exactly `entries.length` rows (bounded to 5), never fewer via a filter on wonValue
    - npm run build succeeds
  </acceptance_criteria>
  <done>Both components render correctly against real computeMonthlyWonUnits/computeOwnerLeaderboard output, with UI-SPEC-exact empty-state copy and single-hue coloring.</done>
</task>

<task type="auto">
  <name>Task 3: Wire TargetVsActualChart + OwnerLeaderboard into DashboardPage (completes Row 1, starts Row 2)</name>
  <files>src/features/dashboard/components/DashboardPage.tsx</files>
  <read_first>
    src/features/dashboard/components/DashboardPage.tsx (as created by 06-01), .planning/phases/06-sales-dashboard/06-UI-SPEC.md
  </read_first>
  <action>
    Edit `DashboardPage.tsx`'s existing Row 1 grid div (`grid grid-cols-1 gap-4 lg:grid-cols-2`) to add `<TargetVsActualChart data={computeMonthlyWonUnits(deals)} />` as the second child, alongside the existing `<UnitTargetGauge />`. Add a new Row 2 grid div (`grid grid-cols-1 gap-4 lg:grid-cols-2`) immediately after Row 1, containing `<OwnerLeaderboard entries={computeOwnerLeaderboard(deals)} />` as its first child (the second slot is added by 06-03's ConversionFunnelChart). Import both new components and both new dashboard-metrics functions at the top of the file.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - DashboardPage.tsx renders 3 widgets total: UnitTargetGauge, TargetVsActualChart (Row 1), OwnerLeaderboard (Row 2)
    - Row 1's grid div contains exactly 2 children; Row 2's grid div contains exactly 1 child (leaving room for 06-03's ConversionFunnelChart)
    - npm run build succeeds
  </acceptance_criteria>
  <done>Dashboard tab shows Gauge + Target-vs-Actual + Leaderboard, all driven by real seed data, matching UI-SPEC's Row 1/Row 2 layout so far.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| In-memory store -> React render tree | Same single boundary as 06-01: `usePipelineStore`'s already-loaded `Deal[]` flowing into pure derived-data functions and Recharts SVG output. No network, no user input, no auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering / Information Disclosure | TargetVsActualChart.tsx, OwnerLeaderboard.tsx | low | mitigate | React's default JSX escaping covers all dynamic text (owner names, formatted currency); no raw-HTML tooltip/label content added |
| T-06-03 | Denial of Service (client-side crash via NaN/Infinity render) | computeMonthlyWonUnits, computeOwnerLeaderboard | medium | mitigate | Both functions pre-seed every bucket/owner at a real 0 (never leave a bucket undefined that a chart could read as NaN); no division occurs in either function |
| T-06-04 | Tampering (data-integrity: misleading business metrics) | DashboardPage.tsx | medium | mitigate | Task 3 continues reading `deals` from the same unfiltered `usePipelineStore` selector established in 06-01 — no new read path introduced |

No other STRIDE-relevant threat patterns apply (RESEARCH.md Security Domain).
</threat_model>

<verification>
- `npx vitest run src/features/dashboard/dashboard-metrics.test.ts` passes with the new Task 1 cases
- `npm run build` succeeds
- Manual: Dashboard tab shows Gauge, Target-vs-Actual chart with 12 populated months, and a 5-row Owner Leaderboard sorted by Won value
</verification>

<success_criteria>
- DASH-02: Target vs. Actual Sales chart plots Won-deal unit volume by month against target on one shared axis, across all 12 trailing months (including zero months)
- DASH-03: Leaderboard ranks all 5 owners by total Won deal value, including any owner with $0
</success_criteria>

<output>
Create `.planning/phases/06-sales-dashboard/06-02-SUMMARY.md` when done
</output>
