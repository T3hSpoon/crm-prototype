---
phase: 01-pipeline-board-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/features/pipeline/hooks/usePipelineGroups.ts
  - src/features/pipeline/components/DealTable.tsx
  - src/features/pipeline/components/GroupSection.tsx
  - src/features/pipeline/components/PipelineBoard.tsx
  - src/app/App.tsx
autonomous: true
requirements: [PIPE-01]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "User can view all deals grouped into 5 pipeline-stage sections (Prospect, Lead, Opportunity, Deal/Won, Lost), populated from mock seed data"
    - "Every one of the 5 groups always renders its header (label + deal count + summed value), even when that group currently has zero deals"
    - "Each group's rows are rendered by its own useReactTable instance using getCoreRowModel() only, sharing one columns definition — not TanStack's getGroupedRowModel"
  artifacts:
    - path: "src/features/pipeline/hooks/usePipelineGroups.ts"
      provides: "selector: deals[] -> Record<PipelineGroup, Deal[]>, all 5 keys always present"
    - path: "src/features/pipeline/components/PipelineBoard.tsx"
      provides: "mounts 5x GroupSection, replaces the Plan 02 tracer render in App.tsx"
    - path: "src/features/pipeline/components/GroupSection.tsx"
      provides: "per-group header (label/count/sum) + DealTable"
    - path: "src/features/pipeline/components/DealTable.tsx"
      provides: "headless @tanstack/react-table wrapper (name, company, value, owner, closeDate columns)"
  key_links:
    - from: "src/features/pipeline/components/PipelineBoard.tsx"
      to: "src/features/pipeline/hooks/usePipelineGroups.ts"
      via: "const groups = usePipelineGroups()"
    - from: "src/app/App.tsx"
      to: "src/features/pipeline/components/PipelineBoard.tsx"
      via: "renders <PipelineBoard /> in place of the Plan 02 tracer <ul>"
  prohibitions:
    - statement: "MUST NOT visually clone monday.com's proprietary board/table design — the UI must express its own distinct visual identity (color system, iconography, layout), inspired only by the underlying grouped-table/subitem concept, per PROJECT.md's explicit 'own visual identity, not a clone' decision"
      status: resolved
      verification: judgment
---

<objective>
Build the grouped pipeline board: a selector deriving the 5 fixed stage groups from the store's flat deal list, a headless per-group table, and the board component that mounts all 5 sections and replaces the Plan 02 tracer render. Delivers PIPE-01 in full.

Purpose: this is the primary user-facing view of the entire prototype — the grouped table users spend the most time in.
Output: a running dev server showing all 5 pipeline-stage groups, each populated from the seeded mock data, with its own count and total value.
</objective>

<execution_context>
@C:/gh-repos/eld/.claude/gsd-core/workflows/execute-plan.md
@C:/gh-repos/eld/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/gh-repos/eld/.planning/phases/01-pipeline-board-foundation/01-RESEARCH.md
@C:/gh-repos/eld/.planning/phases/01-pipeline-board-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: usePipelineGroups selector hook</name>
  <read_first>01-RESEARCH.md Architecture Patterns Pattern 1 (full code block, "usePipelineGroups.ts"); src/features/pipeline/store/pipelineStore.ts; src/shared/utils/pipeline-group.ts</read_first>
  <files>src/features/pipeline/hooks/usePipelineGroups.ts</files>
  <action>
    Create `usePipelineGroups()` exporting a hook that reads `deals` from `usePipelineStore((s) => s.deals)` and returns a `useMemo`-computed `Record<PipelineGroup, Deal[]>`, recomputed only when `deals` changes. Initialize a fixed-order `GROUPS` array `["prospect", "lead", "opportunity", "deal", "lost"]` and seed the result object with every key mapped to `[]` before iterating `deals` — this guarantees all 5 keys are always present in the returned record even when a group has zero members. Push each deal into `result[toPipelineGroup(deal)]`. Do not use `getGroupedRowModel` anywhere in this file or in `DealTable.tsx` (Task 2) — TanStack's grouping API groups by column value dynamically and is the wrong fit for 5 fixed, always-present, independently-actionable named sections (01-RESEARCH.md Architecture Patterns Pattern 1 / Anti-Patterns).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The returned record always has exactly the 5 keys `prospect`, `lead`, `opportunity`, `deal`, `lost` — even for an empty `deals` array
    - The hook is memoized on `deals`, not recomputed on every render
  </acceptance_criteria>
  <done>usePipelineGroups() returns a Record<PipelineGroup, Deal[]> with all 5 groups always present.</done>
</task>

<task type="auto">
  <name>Task 2: DealTable and GroupSection components</name>
  <read_first>01-RESEARCH.md Architecture Patterns Pattern 1 (row-model guidance); 01-CONTEXT.md "Pipeline row content" discretion note (baseline row fields: name/company, value, owner, close date, stage; group-level count/value totals in each section header)</read_first>
  <files>src/features/pipeline/components/DealTable.tsx, src/features/pipeline/components/GroupSection.tsx</files>
  <action>
    Create `DealTable.tsx` accepting `{ deals: Deal[] }`. Define a `columnHelper<Deal>()`-based `columns` array with accessor columns for `name`, `company`, `value` (render formatted as currency, e.g. via `Intl.NumberFormat("en-US", { style: "currency", currency: "USD" })`), `owner`, and `closeDate` (render formatted via `date-fns`'s `format(parseISO(deal.closeDate), "MMM d, yyyy")`). Call `useReactTable({ data: deals, columns, getCoreRowModel: getCoreRowModel() })` — `getCoreRowModel()` only, no sorting/filtering row models this phase (those are Phase 4's PIPE-05/06). Render a plain Tailwind-styled `<table>` iterating `table.getHeaderGroups()` for `<thead>` and `table.getRowModel().rows` for `<tbody>`, using `flexRender` for each cell. If `deals.length === 0`, render the `<thead>` normally and a single `<tbody>` row with an empty-state message spanning all columns — never omit the table shell entirely.

    Create `GroupSection.tsx` accepting `{ group: PipelineGroup, deals: Deal[] }`. Map `group` to a display label: `"prospect"` -> "Prospect", `"lead"` -> "Lead", `"opportunity"` -> "Opportunity", `"deal"` -> "Deal / Won", `"lost"` -> "Lost". Render a header showing the label, `deals.length` as the count, and the group's summed value (`deals.reduce((sum, d) => sum + d.value, 0)`, currency-formatted) — this header must render identically whether `deals.length` is 0 or nonzero, never conditionally hidden. Render `<DealTable deals={deals} />` below the header. Style with Tailwind using an original layout/color system (do not reproduce monday.com's specific status-pill colors or column-header styling from the reference screenshot — invent this project's own palette and section-header treatment, per PROJECT.md's "own visual identity, not a clone" decision).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `DealTable` renders via `getCoreRowModel()` only — no `getGroupedRowModel`, `getSortedRowModel`, or `getFilteredRowModel` imported in this file
    - `GroupSection` renders its header (label, count, summed value) unconditionally, independent of whether `deals` is empty
  </acceptance_criteria>
  <done>GroupSection renders a header (label/count/sum) and its DealTable for any given group's deals, including an empty group.</done>
</task>

<task type="auto">
  <name>Task 3: PipelineBoard mounts all 5 groups; wire into App.tsx</name>
  <read_first>01-RESEARCH.md System Architecture Diagram (Phase 1 slice); src/app/App.tsx (current tracer content from 01-02-PLAN.md Task 2)</read_first>
  <files>src/features/pipeline/components/PipelineBoard.tsx, src/app/App.tsx</files>
  <action>
    Create `PipelineBoard.tsx` calling `usePipelineGroups()` and rendering one `<GroupSection group={g} deals={groups[g]} key={g} />` per entry in the fixed order `["prospect", "lead", "opportunity", "deal", "lost"]`, each section visually distinct (its own bordered/spaced container). Replace `src/app/App.tsx`'s Plan-02 tracer `<h1>Pipeline (tracer)</h1>` + `<ul>` content with `<PipelineBoard />`, keeping the existing `useEffect`-driven `usePipelineStore().load()` call on mount unchanged.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run dev &amp; DEV_PID=$!; sleep 3; node -e "fetch('http://localhost:5173').then(r=&gt;process.exit(r.ok?0:1)).catch(()=&gt;process.exit(1))"; kill $DEV_PID 2&gt;/dev/null</automated>
    <human-check>Visit the running dev server. Confirm all 5 groups (Prospect, Lead, Opportunity, Deal / Won, Lost) render as visually distinct sections, each populated with seeded deals and its own count/total, and that the overall layout reads as its own design rather than a monday.com screenshot clone.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; dev server responds 200
    - All 5 `GroupSection` instances render simultaneously on a single screen with no scroll-hidden or collapsed-by-default group
    - `App.tsx` no longer contains the Plan-02 tracer `<ul>` markup
  </acceptance_criteria>
  <done>All 5 pipeline-stage groups render simultaneously from mock seed data, each showing its own deal rows, count, and total value — PIPE-01 is satisfied.</done>
</task>

</tasks>

## Artifacts This Phase Produces (this plan's contribution)

- `src/features/pipeline/hooks/usePipelineGroups.ts` — `usePipelineGroups()`
- `src/features/pipeline/components/DealTable.tsx` — headless `@tanstack/react-table` wrapper
- `src/features/pipeline/components/GroupSection.tsx` — per-group header + table
- `src/features/pipeline/components/PipelineBoard.tsx` — mounts all 5 `GroupSection`s

## Flagged Assumptions (specless probe fallback — edge coverage unresolved)

The deterministic edge-probe returned **PIPE-01 as `unclassified`/`unresolved`** — it could not auto-derive acceptance edges from the requirement text. Per the spec-less probe fallback protocol, this stays unresolved (never silently auto-resolved) and is surfaced here as an explicit flagged assumption for human confirmation at verify time.

**Reasoned judgment of "done" for PIPE-01:** all 5 groups (Prospect, Lead, Opportunity, Deal/Won, Lost) render as visually distinct sections simultaneously on one screen, populated from the ~40-deal `@faker-js/faker` seed set (CONTEXT.md's "Claude's Discretion" note on seed data profile), with every group showing at least one deal given the seed distribution, and — per Task 1/2's acceptance criteria above — a group that ever reaches zero deals through later interaction still renders its header with a `0` count and an empty table body rather than disappearing. A human should confirm this matches their expectation of "view deals grouped by pipeline stage," and specifically confirm the visual design reads as distinct from the monday.com reference screenshot (PROJECT.md's anti-clone decision), since no source artifact specifies exact colors/layout beyond "own visual identity."

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed/mock data -> rendered table cells | Deal names/companies/owners (faker-generated) are interpolated into JSX text content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Information Disclosure | `DealTable`/`GroupSection` rendering deal name/company/owner text | low | accept | React's default JSX text interpolation auto-escapes all rendered strings; no `dangerouslySetInnerHTML` is used anywhere in this plan's components — XSS-class injection via rendered deal fields is structurally not possible without it. (Canon-referral: generic XSS/injection hardening is OWASP canon, owned by `/gsd-secure-phase` + eslint security rules, not re-minted as a bespoke prohibition here.) |

</threat_model>

<verification>
- `npm run build` exits 0
- Dev server renders all 5 groups simultaneously, each sourced from `usePipelineGroups()`, none hardcoded or hidden
- No file in this plan imports `getGroupedRowModel`
</verification>

<success_criteria>
A user visiting the dev server sees the sales pipeline as a grouped table: 5 named sections, each populated from mock seed data, each with a count and total value header — PIPE-01 fully satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-pipeline-board-foundation/01-03-SUMMARY.md` when done
</output>
