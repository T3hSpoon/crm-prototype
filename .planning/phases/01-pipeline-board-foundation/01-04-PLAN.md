---
phase: 01-pipeline-board-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/features/pipeline/components/AddDealDialog.tsx
  - src/features/pipeline/components/add-deal-schema.ts
  - src/features/pipeline/components/StageSelect.tsx
  - src/features/pipeline/components/PipelineBoard.tsx
  - src/features/pipeline/components/DealTable.tsx
autonomous: true
requirements: [PIPE-02, DEAL-01]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "User can add a new deal via a modal triggered by an Add Deal button; Name, Company, Value, Owner, Close Date, and Stage are all required; on submit the deal appears in its selected group immediately and the modal closes (D-01 through D-04)"
    - "Submitting the Add Deal form with a required field empty, or Value non-positive, blocks submission and shows an inline validation error without closing the modal"
    - "User can move a deal from one pipeline stage to another via a per-row Select control, and it relocates to the new group immediately with no page reload (PIPE-02)"
    - "Adding or moving a deal never issues a network request (fetch/XHR/axios) anywhere in src/features/pipeline — the entire flow resolves through the in-memory MockDealsRepository"
  artifacts:
    - path: "src/features/pipeline/components/AddDealDialog.tsx"
      provides: "shadcn Dialog + react-hook-form + zod form implementing D-01 through D-04"
    - path: "src/features/pipeline/components/add-deal-schema.ts"
      provides: "addDealSchema (zod) — the single source of validation + NewDealInput shape"
    - path: "src/features/pipeline/components/StageSelect.tsx"
      provides: "per-row <Select> calling moveStage(dealId, group)"
  key_links:
    - from: "src/features/pipeline/components/AddDealDialog.tsx"
      to: "src/features/pipeline/store/pipelineStore.ts"
      via: "usePipelineStore().addDeal(values) on valid submit"
    - from: "src/features/pipeline/components/StageSelect.tsx"
      to: "src/features/pipeline/store/pipelineStore.ts"
      via: "usePipelineStore.getState().moveStage(dealId, group) on Select onValueChange"
  prohibitions:
    - statement: "MUST NOT issue any network/fetch/XHR/axios call anywhere in the add-deal or stage-move path — both must resolve entirely through the in-memory MockDealsRepository this phase, per PROJECT.md's explicit frontend-only/no-API-calls constraint"
      status: resolved
      verification: judgment
---

<objective>
Build the Add Deal modal form (D-01 through D-04) and the per-row stage-move control (RESEARCH.md Pattern 3), then wire both into the board built in 01-03-PLAN.md. Delivers DEAL-01 and PIPE-02 together, completing every requirement this phase.

Purpose: these are the two user-initiated write paths of the walking skeleton — the "one real UI interaction wired to the API [seam]" the Phase-1 skeleton requires.
Output: a user can add a deal through a modal and move any deal between the 5 groups from its row, both fully working against the mock repository.
</objective>

<execution_context>
@C:/gh-repos/eld/.claude/gsd-core/workflows/execute-plan.md
@C:/gh-repos/eld/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/gh-repos/eld/.planning/phases/01-pipeline-board-foundation/01-CONTEXT.md
@C:/gh-repos/eld/.planning/phases/01-pipeline-board-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AddDealDialog — modal form implementing D-01 through D-04</name>
  <read_first>01-CONTEXT.md decisions D-01, D-02, D-03, D-04 (Add-Deal Form Scope); 01-RESEARCH.md Code Examples "react-hook-form + zod add-deal form inside a shadcn Dialog" (full code block); 01-RESEARCH.md State of the Art note on the current shadcn Controller/Field composition (do not assume the older FormField/FormItem pattern); the actual file generated by `npx shadcn@latest add form` in 01-01-PLAN.md Task 3 (src/components/ui/form.tsx) — verify the real generated API before writing Controller usage, since shadcn ships copied source that can drift from any cited snippet</read_first>
  <files>src/features/pipeline/components/add-deal-schema.ts, src/features/pipeline/components/AddDealDialog.tsx</files>
  <action>
    Create `add-deal-schema.ts` exporting `addDealSchema` (zod object) with `name: z.string().min(1, "Name is required")`, `company: z.string().min(1, "Company is required")`, `value: z.coerce.number().positive("Value must be positive")`, `owner: z.string().min(1, "Owner is required")`, `closeDate: z.string().min(1, "Close date is required")`, `group: z.enum(["prospect", "lead", "opportunity", "deal", "lost"])` — all five intake fields plus the stage selector are required, matching D-01's "full intake, no partial-add path" and D-02's stage selector. Export the inferred type as `AddDealFormValues = z.infer<typeof addDealSchema>`.

    Create `AddDealDialog.tsx` accepting `{ open: boolean; onOpenChange: (open: boolean) => void }` (D-03: this is a modal/dialog, triggered by an "Add Deal" button owned by the parent — not an inline-row-in-group or slide-over panel). Build the form with `useForm<AddDealFormValues>({ resolver: zodResolver(addDealSchema), defaultValues: { group: "prospect" } })` inside shadcn's `Dialog`/`DialogContent`, using the Controller+Field composition actually present in the generated `src/components/ui/form.tsx` (read that file first — do not assume it matches any cited snippet verbatim). Render inputs for Name, Company, Value, Owner, Close Date (shadcn `Input`) and a Stage `<Select>` labeled "Prospect"/"Lead"/"Opportunity"/"Deal / Won"/"Lost" for the `group` field. On valid submit: call `usePipelineStore().addDeal(values)`, then `onOpenChange(false)` to close the dialog (D-04: the deal appears in its selected group immediately and the modal closes — no batch-add/keep-open mode). On invalid submit: block the store call, show each field's inline validation error (from `fieldState.error`/`FieldError`), and keep the dialog open.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `addDealSchema` requires all 5 intake fields plus `group`; `value` rejects 0 and negative numbers
    - Submitting with any required field empty does not call `addDeal` and does not close the dialog
    - Submitting with all fields valid calls `addDeal(values)` exactly once and closes the dialog
  </acceptance_criteria>
  <done>AddDealDialog enforces all 5 required fields + stage selector (D-01/D-02) as a modal (D-03), and closes while reflecting the new deal immediately on valid submit (D-04).</done>
  <reversibility rating="reversible">Purely a form-validation rule change, no data model impact (carried forward verbatim from 01-CONTEXT.md D-01).</reversibility>
</task>

<task type="auto">
  <name>Task 2: StageSelect — per-row stage-move control</name>
  <read_first>01-RESEARCH.md Architecture Patterns Pattern 3 (full code block and trade-off rationale); src/features/pipeline/store/pipelineStore.ts</read_first>
  <files>src/features/pipeline/components/StageSelect.tsx</files>
  <action>
    Create `StageSelect.tsx` accepting `{ dealId: string; currentGroup: PipelineGroup }`. Render a shadcn `Select` pre-set to `currentGroup`, with the same 5 labeled options as the Add Deal dialog's stage selector. On `onValueChange`, call `usePipelineStore.getState().moveStage(dealId, group as PipelineGroup)`. This is the CONTEXT.md Claude's Discretion "ship the simpler mechanism first" path — do not build any `@dnd-kit` drag-and-drop wiring in this task or this plan. The classic `@dnd-kit/core`+`@dnd-kit/sortable` packages installed in 01-01-PLAN.md remain unused code this phase, reserved for a same-`moveStage`-action Phase 2+ fast-follow per 01-RESEARCH.md Pattern 3.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>grep -c "moveStage" src/features/pipeline/components/StageSelect.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Selecting a new option calls `moveStage(dealId, group)` exactly once
    - No `DndContext`, `useSortable`, `useDroppable`, or `@dnd-kit` import appears in this file
  </acceptance_criteria>
  <done>StageSelect relocates a deal to a new group by calling the existing moveStage store action, with no drag-and-drop code this phase.</done>
</task>

<task type="auto">
  <name>Task 3: Wire AddDealDialog and StageSelect into the board</name>
  <read_first>src/features/pipeline/components/PipelineBoard.tsx and src/features/pipeline/components/DealTable.tsx (current content from 01-03-PLAN.md)</read_first>
  <files>src/features/pipeline/components/PipelineBoard.tsx, src/features/pipeline/components/DealTable.tsx</files>
  <action>
    In `PipelineBoard.tsx`, add an "Add Deal" button in the board's header area (D-03) that opens `AddDealDialog` via a local `useState<boolean>(false)` open flag passed as `open`/`onOpenChange`. In `DealTable.tsx`, add a trailing column to the existing `columns` array rendering `<StageSelect dealId={row.original.id} currentGroup={toPipelineGroup(row.original)} />` for each row (import `toPipelineGroup` from `src/shared/utils/pipeline-group.ts`).
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run dev &amp; DEV_PID=$!; sleep 3; node -e "fetch('http://localhost:5173').then(r=&gt;process.exit(r.ok?0:1)).catch(()=&gt;process.exit(1))"; kill $DEV_PID 2&gt;/dev/null</automated>
    <automated>grep -rlE "axios|XMLHttpRequest" src/features/pipeline 2&gt;/dev/null | wc -l</automated>
    <human-check>From the running board, click Add Deal and submit a new deal into the Lead group — confirm it appears under Lead immediately and the modal closes. Then use that deal's row Select to move it to Opportunity — confirm it relocates to the Opportunity section with no page reload. Try submitting the Add Deal form with a required field left empty — confirm it is blocked with a visible inline error.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; dev server responds 200
    - The `axios`/`XMLHttpRequest` grep across `src/features/pipeline` returns 0
    - A deal added via the modal appears in its selected `GroupSection` without a page reload
    - A deal moved via `StageSelect` disappears from its origin `GroupSection` and appears in the destination `GroupSection` without a page reload
  </acceptance_criteria>
  <done>A user can add a deal via the modal and move any deal between the 5 groups via its row's Select — DEAL-01 and PIPE-02 are both satisfied, completing every requirement this phase.</done>
</task>

</tasks>

## Artifacts This Phase Produces (this plan's contribution)

- `src/features/pipeline/components/add-deal-schema.ts` — `addDealSchema`, `AddDealFormValues`
- `src/features/pipeline/components/AddDealDialog.tsx` — `AddDealDialog`
- `src/features/pipeline/components/StageSelect.tsx` — `StageSelect`
- `src/features/pipeline/components/PipelineBoard.tsx` (edited) — Add Deal button + dialog wiring
- `src/features/pipeline/components/DealTable.tsx` (edited) — trailing `StageSelect` column

## Flagged Assumptions (specless probe fallback — edge coverage unresolved)

The deterministic edge-probe returned **PIPE-02** and **DEAL-01** as `unclassified`/`unresolved` — it could not auto-derive acceptance edges from the requirement text for either. Per the spec-less probe fallback protocol, both stay unresolved (never silently auto-resolved) and are surfaced here as explicit flagged assumptions for human confirmation at verify time.

**Reasoned judgment of "done" for PIPE-02:** moving a deal via its row's Stage Select immediately relocates it out of its current `GroupSection` and into the destination `GroupSection`'s `DealTable`, with no full-page reload and no visible failure state. Per 01-CONTEXT.md's Claude's Discretion and 01-RESEARCH.md Pattern 3, the interaction mechanism this phase is a dropdown, not drag-and-drop — a human should confirm this satisfies "move a deal from one pipeline stage to another," since RESEARCH.md flags drag-and-drop as an acceptable-but-not-required Phase 2+ alternative. A related, deliberate scope boundary: moving a deal directly into the "Lost" group this phase requires **no** reason (no gating prompt) — LOST-01's required-reason gate is explicitly Phase 3's job (ROADMAP.md), so a deal can be moved to Lost with zero friction this phase; a human should confirm this ungated Lost-move is acceptable for a Phase 1 demo rather than expecting Phase 3's gate to already be present.

**Reasoned judgment of "done" for DEAL-01:** clicking "Add Deal" opens a modal (D-03) with 5 required fields (Name, Company, Value, Owner, Close Date) plus a stage selector (D-02); submitting with all fields valid closes the modal and the new deal appears immediately in its selected group (D-04); submitting with any required field empty, or a non-positive Value, blocks submission and shows an inline validation error without closing the modal. A human should confirm this validation behavior — and specifically the exact wording/placement of the inline error messages, which no source artifact specifies beyond the zod messages authored in Task 1 — matches their expectations.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (user-typed form input) -> in-memory Zustand store | The Add Deal form is this phase's only genuinely untrusted-input trust boundary — a human types free text/numbers that reach `usePipelineStore.addDeal()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | `AddDealDialog` form input reaching `addDeal()` (negative/zero value, empty required field bypassing client JS) | medium | mitigate | `addDealSchema` (zod) enforced via `zodResolver` before `addDeal()` is ever called — Task 1's acceptance criteria require invalid submissions to never reach the store |
| T-01-06 | Tampering | `AddDealDialog`/`StageSelect` accidentally wired to a real network call | medium | mitigate | Task 3's grep gate (`axios`/`XMLHttpRequest` = 0 matches across `src/features/pipeline`) plus the `must_haves.prohibitions` entry above; both write paths resolve only through `dealsRepository` (`MockDealsRepository`) |
| T-01-07 | Spoofing / Elevation of Privilege | entire app — no authentication exists | low | accept | Single-user, no backend, no session this phase — explicitly out of scope for this milestone per PROJECT.md; nothing to spoof or escalate without an identity system |
| T-01-08 | Tampering | client-side-only validation bypass (calling `usePipelineStore.getState().addDeal(...)` directly from devtools, skipping the form's zod validation) | low | accept | Acceptable at ASVS L1 for a no-backend, single-user local prototype — there is no server-side state to corrupt beyond the user's own in-memory browser session; documented as an accepted risk, not silently omitted |

</threat_model>

<verification>
- `npm run build` exits 0
- Add Deal modal enforces D-01 through D-04 exactly
- StageSelect relocates a deal between groups with no page reload
- `axios`/`XMLHttpRequest` grep across `src/features/pipeline` returns 0
</verification>

<success_criteria>
A user can add a new deal through the modal form and move any deal between the 5 pipeline groups from its row — DEAL-01 and PIPE-02 are both satisfied, and Phase 1's three requirements (PIPE-01, PIPE-02, DEAL-01) are all complete.
</success_criteria>

<output>
Create `.planning/phases/01-pipeline-board-foundation/01-04-SUMMARY.md` when done
</output>
