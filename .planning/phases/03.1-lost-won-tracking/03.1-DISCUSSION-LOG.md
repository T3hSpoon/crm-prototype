# Phase 3.1: Lost & Won Tracking - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-10
**Phase:** 3.1-Lost & Won Tracking
**Areas discussed:** Lost-move gating mechanism

---

## Gray Areas Presented

| Area | Selected for discussion |
|------|--------------------------|
| Lost-move gating mechanism | ✓ |
| Lost reason input type | (folded into the area above) |
| Won-trigger & contract-terms form UI | Left to Claude's discretion |
| Contract-terms fields vs. Phase 3's deal-terms fields | Left to Claude's discretion |

---

## Lost-move gating mechanism

### Q1: How should the "requires a reason" gate work mechanically?

| Option | Description | Selected |
|--------|-------------|----------|
| Inline popover at the dropdown | Selecting Lost opens a popover anchored to StageSelect; submitting it commits the move | ✓ |
| Modal dialog | Selecting Lost opens a full modal, like Add Deal | |
| Something else | User-described alternative | |

**User's choice:** Inline popover at the dropdown.

### Q2: What should the reason input inside the popover look like?

| Option | Description | Selected |
|--------|-------------|----------|
| Fixed list + optional note | A short selector of common reasons plus an optional free-text note | ✓ |
| Free text only | A single open text field, no preset list | |
| Fixed list only | Pick from a preset list, no freeform note | |

**User's choice:** Fixed list + optional note.

### Q3: What should the fixed list of lost-reason options be?

| Option | Description | Selected |
|--------|-------------|----------|
| Price, Timing, Competitor, No Budget, Other | Common CRM-standard categories | ✓ |
| Shorter list: Price, Competitor, Other | Minimal set | |
| Something else | User's own list | |

**User's choice:** Price, Timing, Competitor, No Budget, Other.

### Q4: What happens if the popover is dismissed without a reason?

| Option | Description | Selected |
|--------|-------------|----------|
| Revert to original stage | Dropdown snaps back, deal record untouched | ✓ |
| Leave dropdown showing Lost but deal unmoved | Visual/data mismatch risk | |

**User's choice:** Revert to original stage.

**Notes:** User confirmed this is fully decided after 4 questions and chose to move on rather than continue exploring this area further.

---

## Claude's Discretion

- **Won-trigger & contract-terms form UI** — user explicitly declined to discuss this despite acknowledgment that it's a significant, not low-stakes, decision. ROADMAP.md describes a "Next instead of Save" modal flow that doesn't map to any existing UI (Phase 2 removed the old edit modal/drawer entirely). Default: reuse StageSelect as trigger, open a full modal (AddDealDialog-pattern) for the 5-field contract-terms capture.
- **Contract-terms fields vs. Phase 3's deal-terms fields** — user explicitly declined to discuss whether WON-01's fields (end date/term length, payment terms, final contract value) overlap with Phase 3's already-captured Contract Term/Frequency/Value. Defaults and open questions documented in CONTEXT.md's Claude's Discretion section — flagged for planner/researcher to raise again if needed.
- Exact new field/type names for the contract-terms data — follow the existing flat-field-on-Deal convention (Phase 3 precedent).
- The new "Won" `PipelineGroup` value — not treated as a genuine open question; `DealOutcome` already has `"won"` unused, strongly implying the intended implementation shape.

## Deferred Ideas

None — discussion stayed within phase scope. No scope-creep suggestions came up.
