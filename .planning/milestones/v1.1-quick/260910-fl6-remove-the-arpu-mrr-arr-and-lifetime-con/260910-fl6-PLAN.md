---
phase: 260910-fl6-remove-the-arpu-mrr-arr-and-lifetime-con
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/pipeline/components/AddDealDialog.tsx
  - src/features/pipeline/components/add-deal-schema.ts
  - src/shared/types/deal.ts
  - src/data/mock/mock-deals-repository.ts
  - src/data/mock/seed-data.ts
  - src/shared/utils/deal-metrics.ts
  - src/features/pipeline/components/DealTable.tsx
autonomous: true
requirements: [DEAL-06]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Add Deal wizard step 2 no longer renders ARPU, MRR, ARR, or Lifetime Contract Value inputs; Customer Type and Confidence Level remain required Selects with their existing defaults ('Similar' / 'Open to RFP Bids'), untouched"
    - "Deal and NewDealInput no longer declare arpu/mrr/arr/lifetimeContractValue fields; MockDealsRepository.create() and seed-data.ts's buildSeedDeal() no longer generate or pass through those fields — all 40 seed deals satisfy the trimmed type"
    - "The main pipeline table renders 4 new columns — MRR, ARR, Lifetime Contract Value, ARPU — computed live from each deal's stored value/frequency/contractTermMonths/lineItems, formatted with the same Intl.NumberFormat currency convention the existing Value column already uses"
    - "A deal with zero line items displays an em dash ('—') in its ARPU cell instead of 0, NaN, or Infinity"
    - "Editing a deal's Value inline (or its line items) changes the displayed MRR/ARR/Lifetime Contract Value/ARPU on the next render, since none of the four is a stored field — they are computed on demand from src/shared/utils/deal-metrics.ts"
  artifacts:
    - path: "src/shared/utils/deal-metrics.ts"
      provides: "New pure derived-value module: computeMrr, computeArr, computeLifetimeContractValue, computeArpu — mirrors line-items.ts's Pick<Deal,...>/named-export/no-side-effects convention"
    - path: "src/features/pipeline/components/DealTable.tsx"
      provides: "4 new columnHelper.display() columns (MRR, ARR, Lifetime Contract Value, ARPU) wired to the deal-metrics.ts functions"
    - path: "src/shared/types/deal.ts"
      provides: "Deal and NewDealInput trimmed back to their pre-260910-ec8-financial-fields shape (customerType/confidenceLevel kept)"
    - path: "src/features/pipeline/components/add-deal-schema.ts"
      provides: "addDealStep2Schema trimmed to 7 fields (5 original deal-terms + customerType + confidenceLevel), no financial-metric validation"
    - path: "src/features/pipeline/components/AddDealDialog.tsx"
      provides: "Step 2 renders 7 fields only; the MRR-driven auto-calc effect and its useEffect/useWatch/FieldDescription imports are gone"
    - path: "src/data/mock/mock-deals-repository.ts"
      provides: "create() passes through only customerType/confidenceLevel from the deal-terms block, not the 4 financial fields"
    - path: "src/data/mock/seed-data.ts"
      provides: "buildSeedDeal() no longer generates arpu/mrr/arr/lifetimeContractValue or the orphan-prone hoisted mrr const"
  key_links:
    - from: "src/features/pipeline/components/DealTable.tsx"
      to: "src/shared/utils/deal-metrics.ts"
      via: "columnHelper.display({ id, header, cell: (info) => currencyFormatter.format(computeX(info.row.original)) }) for each of the 4 new columns, matching the existing stage/id display-column pattern"
      pattern: "columnHelper.display"
    - from: "src/shared/utils/deal-metrics.ts"
      to: "src/shared/types/deal.ts"
      via: "Pick<Deal, \"value\" | \"frequency\" | \"contractTermMonths\" | \"lineItems\"> parameter typing, same discipline as line-items.ts's hasManualOverride"
      pattern: "Pick<Deal, ...>"
    - from: "src/features/pipeline/components/AddDealDialog.tsx"
      to: "src/features/pipeline/components/add-deal-schema.ts"
      via: "addDealStep2Schema.shape still spreads into addDealSchema (no .merge()) — now with 4 fewer fields, so AddDealFormValues/NewDealInput shrink automatically with no extra wiring"
      pattern: "addDealSchema"
---

<objective>
Correct quick task 260910-ec8: remove ARPU, MRR, ARR, and Lifetime Contract Value as manually-entered Add Deal wizard step-2 inputs (and as stored `Deal`/`NewDealInput` fields), and replace them with a pure derived-value utility that computes all four from data already on the deal (`value`, `frequency`, `contractTermMonths`, `lineItems`), displayed as 4 new columns in the main pipeline table. Customer Type and Confidence Level — also added by 260910-ec8 — are explicitly out of scope and must be left exactly as they are.

Purpose: These four values should never be hand-typed at deal creation (they're arithmetically derived from other fields already captured) and should never go stale relative to `value` when it later changes via inline table editing or line-item edits — the same "derived, never stored" contract Phase 2's `sumLineItems`/`hasManualOverride` already established for `Deal.value`.

Output: A trimmed Add Deal wizard (7 step-2 fields, down from 11), a trimmed `Deal`/`NewDealInput` type and mock data layer, a new `src/shared/utils/deal-metrics.ts` module, and 4 new computed columns in `DealTable.tsx`.
</objective>

<execution_context>
@C:/gh-repos/eld/.claude/gsd-core/workflows/execute-plan.md
@C:/gh-repos/eld/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/gh-repos/eld/.planning/STATE.md
@C:/gh-repos/eld/.claude/CLAUDE.md
@C:/gh-repos/eld/.planning/quick/260910-fl6-remove-the-arpu-mrr-arr-and-lifetime-con/260910-fl6-CONTEXT.md
@C:/gh-repos/eld/.planning/quick/260910-fl6-remove-the-arpu-mrr-arr-and-lifetime-con/260910-fl6-RESEARCH.md
@C:/gh-repos/eld/src/shared/types/deal.ts
@C:/gh-repos/eld/src/features/pipeline/components/add-deal-schema.ts
@C:/gh-repos/eld/src/features/pipeline/components/AddDealDialog.tsx
@C:/gh-repos/eld/src/data/mock/mock-deals-repository.ts
@C:/gh-repos/eld/src/data/mock/seed-data.ts
@C:/gh-repos/eld/src/shared/utils/line-items.ts
@C:/gh-repos/eld/src/features/pipeline/components/DealTable.tsx
@C:/gh-repos/eld/src/features/pipeline/components/EditableCell.tsx

Notes carried from RESEARCH.md (do not re-derive):
- `tsconfig.app.json`/`tsconfig.node.json` both set `noUnusedLocals: true`/`noUnusedParameters: true`, and `npm run build` runs `tsc -b && vite build` — so any import or local left dangling by Task 1's removal (e.g. `useEffect`/`useWatch`/`FieldDescription` in `AddDealDialog.tsx`, the hoisted `mrr` const in `seed-data.ts`) hard-fails the build. This is the primary safety net; use it, don't duplicate it with fragile string-literal checks.
- `DealTable.tsx` uses `legacyCreateColumnHelper<Deal>()` from `@tanstack/react-table/legacy` (the `/legacy` compat subpath, per STATE.md's Phase 1 decision) — the 4 new columns must use this same `columnHelper` instance and stay on the `/legacy` import path, not `@tanstack/react-table`'s native v9 API.
- `mrr`/`arr` are not property names on `Deal` after this task — they only exist as local function names inside `deal-metrics.ts`. A blunt `grep -c 'mrr'` across `src/` would match both legitimately (the new utility) and would be ambiguous evidence either way; this plan's verify steps use positive checks (kept identifiers, exported function names, line-count deltas) rather than fragile string-absence greps.
- No shared `formatCurrency` helper exists in this codebase. `EditableCell.tsx` and `GroupSection.tsx` each independently declare a module-level `currencyFormatter = new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 })` — a 4th near-identical const in `DealTable.tsx` is the established convention here, not a refactor-worthy duplication.
- The existing "—" (em dash) empty-cell convention lives in `EditableCell.tsx`'s `closeDate` display branch (`value ? format(...) : "—"`) — reuse that literal for ARPU's zero-line-items case.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert the 4 financial-field wizard inputs and stored fields</name>
  <precondition>Quick task 260910-ec8's financial-field additions (arpu/mrr/arr/lifetimeContractValue on Deal/NewDealInput, addDealStep2Schema, AddDealDialog.tsx's Controllers/DEFAULT_VALUES/auto-calc effect, MockDealsRepository.create()'s pass-through, seed-data.ts's buildSeedDeal() generation) are present exactly as RESEARCH.md documents, unmodified since this session's reads.</precondition>
  <read_first>src/features/pipeline/components/AddDealDialog.tsx (full — imports at lines 1-11, DEFAULT_VALUES, the useWatch/useEffect auto-calc block, the 4 financial Controller blocks at the end of the step-2 FieldGroup); src/features/pipeline/components/add-deal-schema.ts (full — addDealStep2Schema's arpu/mrr/arr/lifetimeContractValue chains); src/shared/types/deal.ts (full — Deal/NewDealInput's financial fields and their doc comments); src/data/mock/mock-deals-repository.ts (full — create()'s deal-terms pass-through block); src/data/mock/seed-data.ts (full — buildSeedDeal()'s hoisted mrr/contractTermMonths consts and the financial-field return-object lines)</read_first>
  <files>src/features/pipeline/components/AddDealDialog.tsx, src/features/pipeline/components/add-deal-schema.ts, src/shared/types/deal.ts, src/data/mock/mock-deals-repository.ts, src/data/mock/seed-data.ts</files>
  <action>
    Edit in this order (UI first, types last, so schema/type files can be trimmed without a still-referencing consumer):

    In `AddDealDialog.tsx`: remove `useEffect` from the `react` import (keep `useState`); remove `useWatch` from the `react-hook-form` import (keep `Controller, useForm`); remove `FieldDescription` from the `@/components/ui/field` import (keep `Field, FieldError, FieldGroup, FieldLabel`). Remove the 4 financial-field entries from `DEFAULT_VALUES` (keep every other entry including `customerType: "similar"` and `confidenceLevel: "open-to-rfp"`). Delete the entire `useWatch({ control: form.control, name: ["mrr", "contractTermMonths"] })` call and its following `useEffect` block (the whole live auto-calc mechanism — comment, destructure, effect body, eslint-disable line). In the step-2 `FieldGroup`, delete the 4 `Controller` blocks that follow the `confidenceLevel` Controller (the ones rendering the financial-metric number inputs), leaving `confidenceLevel`'s Controller as the last field in the group before the closing `</FieldGroup>` tag. Do not touch the `customerType` or `confidenceLevel` Controllers, their option-array constants, or step 1 at all.

    In `add-deal-schema.ts`'s `addDealStep2Schema`: delete the 4 financial-field `z.coerce.number()...optional()` chains (including their inline sizing-rationale comments, which become moot once the fields are gone), leaving `confidenceLevel` as the schema's last field.

    In `deal.ts`: delete the 4 financial-field declarations from both `Deal` and `NewDealInput`. Rewrite the doc comment directly above `customerType`/`confidenceLevel` on both interfaces to describe only what remains: Customer Type / Confidence Level are captured via the Add Deal wizard's step 2 (Quick task 260910-ec8); note that MRR/ARR/Lifetime Contract Value/ARPU are intentionally not fields here — they are derived on demand by `src/shared/utils/deal-metrics.ts` (Task 2 of this quick task, 260910-fl6) from `value`/`frequency`/`contractTermMonths`/`lineItems`, so they can never go stale relative to those fields.

    In `mock-deals-repository.ts`'s `create()`: delete the 4 financial-field pass-through lines, keeping `customerType: input.customerType, confidenceLevel: input.confidenceLevel,`. Update the preceding comment to reference only Customer Type / Confidence Level, dropping the "financial metrics" mention.

    In `seed-data.ts`'s `buildSeedDeal()`: delete the hoisted `const mrr = faker.number.int({ min: 0, max: 50_000 });` line entirely (it has no remaining reader once the financial fields below are gone). Inline `contractTermMonths` back into the return object as `contractTermMonths: faker.number.int({ min: 0, max: 60 }),` (it no longer needs to be hoisted — nothing else references it now that `mrr` is gone) and delete the separate hoisted `contractTermMonths` const line and its now-inapplicable "hoisted so both..." comment. Delete the 4 financial-field return-object lines (`arpu:`, `mrr,`, `arr:`, `lifetimeContractValue:`). Update the preceding comment to note only Customer Type / Confidence Level are generated here, since MRR/ARR/Lifetime Contract Value/ARPU are now derived at read time by `deal-metrics.ts` rather than generated per-seed-deal.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
    <automated>grep -c "customerType" src/shared/types/deal.ts src/features/pipeline/components/add-deal-schema.ts src/features/pipeline/components/AddDealDialog.tsx src/data/mock/mock-deals-repository.ts src/data/mock/seed-data.ts | awk -F: '{s+=$2} END {exit (s>=5?0:1)}'</automated>
    <automated>test "$(wc -l < src/features/pipeline/components/AddDealDialog.tsx)" -lt 450</automated>
    <human-check>Open Add Deal, advance to step 2 — confirm it renders exactly 7 fields (Prorata, Grace Period, Contract Term, Frequency, Currency, Customer Type, Confidence Level) with no ARPU/MRR/ARR/Lifetime-Contract-Value inputs anywhere. Fill required fields and submit — confirm the deal is created with no console/type errors.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` and `npm run lint` both exit 0
    - `Deal` and `NewDealInput` each declare exactly the pre-260910-ec8-financial-fields shape plus `customerType`/`confidenceLevel`
    - `addDealStep2Schema` validates 7 fields (5 original deal-terms + `customerType` + `confidenceLevel`), nothing financial
    - `AddDealDialog.tsx`'s step 2 renders those same 7 fields with no dangling `useEffect`/`useWatch`/`FieldDescription` imports
    - `MockDealsRepository.create()` and `buildSeedDeal()` pass through/generate only `customerType`/`confidenceLevel` from the deal-terms block; all 40 seed deals satisfy the trimmed `Deal` type
  </acceptance_criteria>
  <done>Add Deal wizard step 2 is back to 7 fields (Prorata/Grace Period/Contract Term/Frequency/Currency/Customer Type/Confidence Level); `Deal`/`NewDealInput` no longer carry `arpu`/`mrr`/`arr`/`lifetimeContractValue`; the mock repository and seed data no longer reference them; `npm run build` and `npm run lint` both pass.</done>
  <reversibility rating="costly">Touches `AddDealDialog.tsx`, `add-deal-schema.ts`, `Deal`/`NewDealInput`, the mock repository, and all 40 seed deals. Re-adding these fields later means redoing quick task 260910-ec8's changes from scratch, though no persisted/external schema exists to migrate in this in-memory prototype.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Derived-value utility + 4 new pipeline-table columns</name>
  <read_first>src/shared/utils/line-items.ts (full — the Pick&lt;Deal,...&gt;/named-export/no-side-effects convention to mirror); src/features/pipeline/components/DealTable.tsx (full — the columnHelper/legacy import, the existing accessor columns and the stage/id display() columns to mirror, the columns array's exact structure); src/features/pipeline/components/EditableCell.tsx (lines 7-11 and 44-54 — the currencyFormatter Intl.NumberFormat construction and the "—" empty-cell fallback to copy verbatim); src/shared/types/deal.ts (as trimmed by Task 1 — confirm `DealFrequency`'s 5 members and `LineItem.units`)</read_first>
  <files>src/shared/utils/deal-metrics.ts, src/features/pipeline/components/DealTable.tsx</files>
  <action>
    Create `src/shared/utils/deal-metrics.ts` as a new sibling file to `line-items.ts`, following its exact convention (named exports, plain pure functions, `Pick<Deal, "...">` parameter types, a file-header doc comment stating the "never stored, always derived" contract, no side effects, no store/repository imports). Add a module-level `PERIOD_MONTHS` mapping typed `Record<Deal["frequency"], number>` with the 5 `DealFrequency` members as keys: `monthly: 1, quarterly: 3, quadrimestral: 4, "semi-annual": 6, annually: 12`. Export `computeMrr(deal: Pick<Deal, "value" | "frequency">): number` returning `deal.value / PERIOD_MONTHS[deal.frequency]`. Export `computeArr(deal: Pick<Deal, "value" | "frequency">): number` returning `computeMrr(deal) * 12`. Export `computeLifetimeContractValue(deal: Pick<Deal, "value" | "frequency" | "contractTermMonths">): number` returning `computeMrr(deal) * deal.contractTermMonths`. Export `computeArpu(deal: Pick<Deal, "value" | "lineItems">): number | null` that sums `units` across `deal.lineItems` and returns `null` (not 0/NaN/Infinity) when that sum is 0, otherwise `deal.value / totalUnits` — doc-comment this null-on-zero-units contract explicitly since the caller must branch on it.

    In `DealTable.tsx`: import `computeMrr, computeArr, computeLifetimeContractValue, computeArpu` from `@/shared/utils/deal-metrics`. Add a module-level `currencyFormatter = new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 })` const (copy `EditableCell.tsx`'s exact construction), placed near the top of the file alongside the existing `columnHelper` const. In the `columns` array, insert 4 new `columnHelper.display({ id, header, cell })` entries directly after the `closeDate` accessor column and before the `stage` display column (grouping the new computed/non-editable columns with the other display-only columns that follow them), in this order: `{ id: "mrr", header: "MRR", cell: (info) => currencyFormatter.format(computeMrr(info.row.original)) }`; `{ id: "arr", header: "ARR", cell: (info) => currencyFormatter.format(computeArr(info.row.original)) }`; `{ id: "lifetimeContractValue", header: "Lifetime Contract Value", cell: (info) => currencyFormatter.format(computeLifetimeContractValue(info.row.original)) }`; `{ id: "arpu", header: "ARPU", cell: (info) => { const arpu = computeArpu(info.row.original); return arpu === null ? "—" : currencyFormatter.format(arpu); } }` — the last one reusing `EditableCell.tsx`'s exact em-dash fallback for the zero-line-items case. Every `cell` reads the full `Deal` from `info.row.original` since none of these 4 values is a real accessor path on `Deal`.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
    <automated>grep -c "export function compute" src/shared/utils/deal-metrics.ts | grep -qx 4</automated>
    <automated>grep -c "columnHelper\.display(" src/features/pipeline/components/DealTable.tsx | grep -qx 7</automated>
    <human-check>Open the pipeline board — confirm MRR, ARR, Lifetime Contract Value, and ARPU columns render for every deal, currency-formatted the same way as the Value column. Expand a deal with zero line items and confirm its ARPU cell shows "—" instead of a number. Inline-edit a deal's Value and confirm MRR/ARR/Lifetime Contract Value update on next render with no page reload.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` and `npm run lint` both exit 0
    - `src/shared/utils/deal-metrics.ts` exports `computeMrr`, `computeArr`, `computeLifetimeContractValue`, `computeArpu` as pure `Pick<Deal, ...>`-typed functions
    - `DealTable.tsx` renders 4 new `columnHelper.display()` columns (MRR, ARR, Lifetime Contract Value, ARPU) using the same `Intl.NumberFormat` currency convention as the Value column
    - ARPU renders "—" for any deal whose line items sum to 0 units, never 0/NaN/Infinity
  </acceptance_criteria>
  <done>Pipeline table shows MRR/ARR/Lifetime Contract Value/ARPU as live-computed columns for every deal, matching Value's currency formatting; a deal with no line items shows an em dash for ARPU; none of the four values is a stored `Deal` field, so they can never go stale relative to `value`/`frequency`/`contractTermMonths`/`lineItems`.</done>
  <reversibility rating="reversible">Purely additive — a new file plus new display columns, no schema/type/repository change. Trivially removable without touching persisted or seed data.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Add Deal wizard, pre-Task-1) → react-hook-form/zod → in-memory Zustand store → mock repository | Task 1 removes 4 fields' worth of untrusted numeric-input surface from this existing boundary entirely — it does not add a new boundary |
| `DealTable.tsx` cell renderers → `deal-metrics.ts` pure functions | New in-process (not network) data flow reading only already-validated, already-stored `Deal` fields — no new untrusted-input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260910fl6-01 | Tampering | Removed ARPU/MRR/ARR/Lifetime-Contract-Value manual numeric inputs | low | mitigate | Task 1 deletes the entire input surface (Controllers + schema fields), eliminating the previously-bounded-but-still-present unbounded/negative-number tampering vector from 260910-ec8 rather than merely re-bounding it |
| T-260910fl6-02 | Tampering | `deal-metrics.ts`'s `PERIOD_MONTHS[deal.frequency]` lookup on an out-of-union `frequency` value | low | mitigate | `PERIOD_MONTHS` is typed `Record<Deal["frequency"], number>` (exhaustive over `DealFrequency`'s 5 members, TS mapped-type checked), and `frequency` is already gated at every write path by `addDealStep2Schema`'s `z.enum(...)` — no code path in this app can construct a `Deal` with an out-of-union `frequency` |
| T-260910fl6-03 | Information Disclosure | 4 new pipeline-table columns display computed financial values (MRR/ARR/Lifetime Contract Value/ARPU) | low | accept | Same single-user, no-auth, mock-data-only trust model as every other column in this frontend-only prototype (PROJECT.md); no new data is exposed that `value`/`frequency`/`contractTermMonths`/`lineItems` didn't already expose |

</threat_model>

<verification>
- `npm run build` (`tsc -b && vite build`) exits 0 after both tasks
- `npm run lint` exits 0
- Add Deal wizard step 2 shows exactly 7 fields (no financial-metric inputs); Customer Type/Confidence Level unchanged
- All 40 seed deals satisfy the trimmed `Deal` type
- Pipeline table renders 4 new computed columns (MRR/ARR/Lifetime Contract Value/ARPU), currency-formatted like Value, with ARPU showing "—" for zero-line-item deals
- Changing a deal's `value` (inline edit) changes its MRR/ARR/Lifetime Contract Value/ARPU display on next render — no stale duplicate data
</verification>

<success_criteria>
The 4 financial metrics are no longer manually entered at deal creation — they are computed on demand from `value`/`frequency`/`contractTermMonths`/`lineItems` via `src/shared/utils/deal-metrics.ts` and rendered as 4 new pipeline-table columns, completing this quick task's full corrective scope with no change to Customer Type/Confidence Level or any other existing wizard field.
</success_criteria>

<output>
Create `.planning/quick/260910-fl6-remove-the-arpu-mrr-arr-and-lifetime-con/260910-fl6-SUMMARY.md` when done
</output>
