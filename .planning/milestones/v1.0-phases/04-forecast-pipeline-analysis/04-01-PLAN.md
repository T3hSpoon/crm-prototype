---
phase: 04-forecast-pipeline-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/pipeline/components/PipelineToolbar.tsx
  - src/features/pipeline/components/DealTable.tsx
  - src/features/pipeline/components/PipelineBoard.tsx
  - src/features/pipeline/components/GroupSection.tsx
  - src/components/ui/toggle-group.tsx
autonomous: true
requirements: [PIPE-04, PIPE-05, PIPE-06]

estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Typing in the shared search box (PipelineToolbar) filters rows by name/company substring match, case-insensitively, across all 6 group tables simultaneously via TanStack's `includesString` global filter fn restricted to the name/company columns (`enableGlobalFilter: false` on every other column) — search never matches on owner, value, close date, or stage (D-04, D-05, PIPE-04)."
    - "An empty search input matches all deals (no filtering applied); a search that matches zero deals in a group shows that group's existing empty-state copy, never an error (resolved edge, PIPE-04 empty)."
    - "Search matching is case-insensitive substring matching against deal name and company via TanStack's built-in `includesString` global filter fn — no Unicode normalization/grapheme-cluster handling is added for this dataset (resolved edge, PIPE-04 encoding)."
    - "The owner filter is a dropdown populated dynamically from the live, deduplicated, sorted list of owners across all deals (never a hardcoded list), the value filter is two number inputs bound to TanStack's `inNumberRange`, and the close-date filter is two `<input type=\"date\">` fields bound to `inDateRange` — all three apply identically across all 6 group tables from the one shared toolbar (D-05, PIPE-05)."
    - "All active filters (search text, owner, value range, close-date range) combine with AND semantics — a deal must match every currently-active constraint to appear in a group's table; the group-visibility toggle operates independently at the group level, not the row level (resolved assumption, PIPE-05 unclassified — REQUIREMENTS.md did not specify filter-combination logic; encoded here as the explicit contract)."
    - "PIPE-05's stage filter is a `ToggleGroup` (type=\"multiple\") of the 6 group labels (Prospect/Lead/Opportunity/Deal/Won/Lost) that shows/hides entire `GroupSection` blocks — it never changes which group a deal belongs to, only which of the 6 sections currently render (D-06)."
    - "Clicking the Value, Close Date, or Owner column header in any group's table sorts all 6 group tables by that column and direction simultaneously (one shared `SortingState` lifted to `PipelineBoard`), with a visible ascending/descending arrow on the active sort column — never independent per-group sort state (D-07, PIPE-06)."
    - "When sort-key values compare equal, relative row order is stable (unchanged from pre-sort order) — guaranteed by TanStack's spec-stable-sort-based `getSortedRowModel` (resolved edge, PIPE-06 ordering)."
    - "Equal sort-key values (e.g. identical value or close date) are never merged or deduplicated — each remains its own row (resolved edge, PIPE-06 adjacency)."
    - "Sorting a group with 0 deals renders that group's existing empty state, unaffected by the active sort; sorting a group with exactly 1 deal is a no-op (resolved edge, PIPE-06 empty)."
    - "GroupSection's count badge and $ total recompute from the same filtered/sorted row set each DealTable currently renders (`table.getRowModel().rows`, lifted up via an `onVisibleRowsChange` callback) — never from the group's full pre-partition `deals` array — so the badge always reflects what's actually visible as search/filter/sort narrow results (UI-SPEC zero-one-many)."
    - "Deselecting every group in the visibility toggle shows the page-level empty state ('No pipeline stages selected.' / 'Choose at least one stage above to see deals.') instead of rendering a blank board with zero sections (UI-SPEC empty, pipeline-board all hidden)."
    - "A value-range or close-date-range filter with an invalid combination (min greater than max, or end before start) shows inline validation copy below the control ('Enter a valid range.' / 'End date must be on or after the start date.') and is never silently applied as a filter matching zero rows — the typed text stays visible and the filter itself contributes no active constraint until corrected (UI-SPEC error state)."
    - "A range filter with only one bound set (e.g. min filled, max blank) is treated as an open-ended bound (>= min or <= max) — not an error, not ignored (UI-SPEC partial)."
    - "A 'Clear filters' action resets search text, owner/value/close-date filters, sorting, and the group-visibility toggle back to their defaults in one click (Copywriting Contract)."
  artifacts:
    - path: "src/features/pipeline/components/PipelineToolbar.tsx"
      provides: "Shared search input, owner/value/close-date filter controls, group-visibility ToggleGroup, Clear filters button — the single D-05 toolbar all 6 groups read from"
    - path: "src/features/pipeline/components/DealTable.tsx"
      provides: "Controlled globalFilter/columnFilters/sorting state via TanStack's /legacy getFilteredRowModel + getSortedRowModel, plus an onVisibleRowsChange callback lifting the final row set to GroupSection"
    - path: "src/features/pipeline/components/PipelineBoard.tsx"
      provides: "Lifted search/filter/sort/visibleGroups state, derived columnFilters, visibleGroups-filtered GROUPS render, all-hidden empty state"
    - path: "src/features/pipeline/components/GroupSection.tsx"
      provides: "Filtered-row-driven count badge/$ total, sourced from DealTable's onVisibleRowsChange, not the raw deals prop"
    - path: "src/components/ui/toggle-group.tsx"
      provides: "ToggleGroup/ToggleGroupItem primitives (shadcn official registry)"
  key_links:
    - from: "src/features/pipeline/components/PipelineToolbar.tsx"
      to: "src/features/pipeline/components/PipelineBoard.tsx"
      via: "onGlobalFilterChange/onOwnerChange/onValueMinChange/onValueMaxChange/onCloseDateMinChange/onCloseDateMaxChange/onVisibleGroupsChange/onClearFilters callbacks lift raw toolbar state up"
      pattern: "controlled component, lifted state"
    - from: "src/features/pipeline/components/PipelineBoard.tsx"
      to: "src/features/pipeline/components/DealTable.tsx"
      via: "globalFilter/columnFilters(derived via useMemo)/sorting props passed identically through GroupSection into every one of the 6 DealTable instances"
      pattern: "one shared controlled TanStack Table state (04-RESEARCH.md Pattern 1)"
    - from: "src/features/pipeline/components/DealTable.tsx"
      to: "src/features/pipeline/components/GroupSection.tsx"
      via: "onVisibleRowsChange(table.getRowModel().rows.map(r => r.original)) fired on every row-model recompute"
      pattern: "lift-state-up for the zero-one-many badge/total wiring"
  prohibitions:
    - statement: "Filtering (search text, owner/value/close-date column filters) and the group-visibility toggle MUST NOT mutate `Deal` records or reassign the store's canonical `deals` array — these are display-only transforms over TanStack's `getFilteredRowModel()` and a local `visibleGroups` array; no filter/toggle code path may import or call a `usePipelineStore` write action (`addDeal`/`moveStage`/`moveToLost`/`moveToWon`/`updateDeal`) or directly mutate `deals`."
      status: resolved
      verification: judgment
    - statement: "Sorting MUST NOT mutate the store's canonical `deals` array order — `Array.prototype.sort()` sorts in place, so header-click sorting must go through TanStack's `getSortedRowModel()` (which produces a new sorted row-model, never mutating the input array in place) and must never call `.sort()` directly on `usePipelineStore`'s `deals` array or any direct reference to it."
      status: resolved
      verification: judgment
---

<objective>
**As a** salesperson working a 40+ deal pipeline, **I want to** search, filter, and sort the board, **so that** I can find and focus on the deals that matter right now instead of scrolling six full stage tables.

This plan delivers PIPE-04, PIPE-05, and PIPE-06 as one vertical slice. Task 1 (tracer) wires the entire search path end to end — a new shared `PipelineToolbar`, controlled `globalFilter` state lifted to `PipelineBoard`, TanStack's `getFilteredRowModel` wired into `DealTable`, and `GroupSection`'s count badge/$ total recomputed from what's actually visible — proving the "one toolbar drives all 6 tables" architecture with just search. Task 2 (expansion) adds owner/value/close-date column filters and shared header-click sorting on the same controlled-state contract. Task 3 (expansion) adds the group-visibility toggle, the "Clear filters" action, and the all-hidden empty state.

Purpose: closes the pipeline-table half of Phase 4 so the board scales past demo-sized seed data.
Output: one shared toolbar above the pipeline board that searches, filters, and sorts all 6 stage groups in sync, with badges/totals that always reflect what's currently visible.
</objective>

<execution_context>
@C:/gh-repos/eld/.claude/gsd-core/workflows/execute-plan.md
@C:/gh-repos/eld/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/gh-repos/eld/.planning/PROJECT.md
@C:/gh-repos/eld/.planning/ROADMAP.md
@C:/gh-repos/eld/.planning/STATE.md
@C:/gh-repos/eld/.planning/phases/04-forecast-pipeline-analysis/04-CONTEXT.md
@C:/gh-repos/eld/.planning/phases/04-forecast-pipeline-analysis/04-RESEARCH.md
@C:/gh-repos/eld/.planning/phases/04-forecast-pipeline-analysis/04-PATTERNS.md
@C:/gh-repos/eld/.planning/phases/04-forecast-pipeline-analysis/04-UI-SPEC.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end pipeline search — shared toolbar, all 6 tables, filtered-row-driven badges</name>
  <reversibility rating="costly">Per CONTEXT.md D-04: search/filter/sort operate within each of the 6 existing per-group tables, not a flattened single table. This commits to the per-group `DealTable` architecture; flattening later would mean redesigning PipelineBoard/GroupSection's core layout.</reversibility>
  <read_first>src/features/pipeline/components/DealTable.tsx (full 192-line file — the exact `getCoreRowModel()`-only table instantiation and its own comment naming the exact `/legacy` APIs this task adds); src/features/pipeline/components/PipelineBoard.tsx (full 31-line file — the exact header/shell structure and `GROUPS.map` call being extended); src/features/pipeline/components/GroupSection.tsx (full 100-line file — the exact `deals.reduce`/`deals.length` badge computation at lines 74/88-89 being replaced); src/features/pipeline/hooks/usePipelineGroups.ts (confirm `GROUPS` constant and the pre-partition selector are unaffected by this task); src/shared/types/deal.ts (confirm `Deal`/`PipelineGroup` field names, no change needed)</read_first>
  <files>src/features/pipeline/components/PipelineToolbar.tsx, src/features/pipeline/components/DealTable.tsx, src/features/pipeline/components/PipelineBoard.tsx, src/features/pipeline/components/GroupSection.tsx</files>
  <action>
    In `DealTable.tsx`: add `getFilteredRowModel` to the existing `import { getCoreRowModel, legacyCreateColumnHelper, useLegacyTable } from "@tanstack/react-table/legacy"` line. Add `globalFilter: string` and an optional `onVisibleRowsChange?: (rows: Deal[]) => void` to `DealTableProps`, and destructure both in the component signature. On the `name` and `company` accessor columns, add `enableGlobalFilter: true`. On every other column definition — `value`, `owner`, `closeDate` accessors, the `lifetimeContractValue`/`stage`/`id` display columns, and the `expandColumn` display column defined inside the component — add `enableGlobalFilter: false`, so search only ever matches name/company (PIPE-04's literal scope, not the whole row). Update the `useLegacyTable(...)` call to add `state: { globalFilter }`, `onGlobalFilterChange: () => {}` (this component has no in-table search UI of its own — global filter only ever changes via the toolbar above; the no-op change handler is required to keep TanStack treating `globalFilter` as controlled rather than falling back to internal state, per 04-RESEARCH.md Pitfall 3), `getFilteredRowModel: getFilteredRowModel()`, and `globalFilterFn: "includesString"`. Add `useEffect` to the existing `import { Fragment, useState } from "react"` line, and add a `useEffect(() => { onVisibleRowsChange?.(table.getRowModel().rows.map((row) => row.original)); }, [table.getRowModel().rows])` after the `table` is constructed — use `table.getRowModel()` (the final row model in whatever pipeline is wired), not `table.getFilteredRowModel()` directly, so this effect stays correct once Task 2/3 add column filters and sorting to the same pipeline without needing to touch this effect again.

    In `GroupSection.tsx`: add `useState` to the `import type { LucideIcon } from "lucide-react"` block's sibling React import (add `import { useState } from "react"` — currently absent from this file). Add `globalFilter: string` to `GroupSectionProps`. Add `const [visibleDeals, setVisibleDeals] = useState<Deal[]>(deals)`. Change the `<DealTable group={group} deals={deals} />` call to `<DealTable group={group} deals={deals} globalFilter={globalFilter} onVisibleRowsChange={setVisibleDeals} />`. Replace the existing `const total = deals.reduce((sum, d) => sum + d.value, 0)` with `const total = visibleDeals.reduce((sum, d) => sum + d.value, 0)`, and replace the count badge's `{deals.length}` with `{visibleDeals.length}`. Note: `visibleDeals` starts equal to `deals` and self-corrects one render tick after `deals` itself changes (e.g. a new deal is added elsewhere) via `DealTable`'s effect firing — this is expected, not a regression.

    In `PipelineBoard.tsx`: add `const [globalFilter, setGlobalFilter] = useState("")`. Render `<PipelineToolbar globalFilter={globalFilter} onGlobalFilterChange={setGlobalFilter} />` as a new row immediately below the existing header `div` (the `h1`/"Add Deal" row) and above the `GROUPS.map(...)` call. Pass `globalFilter={globalFilter}` into every `<GroupSection key={group} group={group} deals={groups[group]} globalFilter={globalFilter} />` call.

    Create `src/features/pipeline/components/PipelineToolbar.tsx`. Props for this task: `{ globalFilter: string; onGlobalFilterChange: (value: string) => void }` (Task 2 and Task 3 widen this same component's props — do not rename it). Import `Search` from `lucide-react` and `Input` from `@/components/ui/input`. Render `<div className="flex flex-wrap items-center gap-3 rounded-lg border border-border bg-card px-4 py-3"><div className="relative flex items-center"><Search className="pointer-events-none absolute left-2.5 size-4 text-muted-foreground" aria-hidden="true" /><Input type="search" placeholder="Search by name or company..." value={globalFilter} onChange={(e) => onGlobalFilterChange(e.target.value)} className="max-w-xs pl-8" /></div></div>`.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>grep -v '^\s*//' src/features/pipeline/components/DealTable.tsx | grep -c "getFilteredRowModel" | grep -qx 2</automated>
    <automated>grep -v '^\s*//' src/features/pipeline/components/DealTable.tsx | grep -c "enableGlobalFilter: false" | grep -qx 7</automated>
    <automated>grep -v '^\s*//' src/features/pipeline/components/GroupSection.tsx | grep -c "visibleDeals" | grep -qx 3</automated>
    <human-check>Type a partial deal name into the toolbar's search box — confirm matching rows stay visible and non-matching rows disappear across all 6 group sections at once, and each affected group's count badge/$ total updates to reflect only the visible rows. Type text that matches an owner's name but not any deal name/company — confirm it does NOT filter anything (search is name/company only). Clear the search box — confirm all deals reappear and every badge/total returns to its original value.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `DealTable.tsx` imports `getFilteredRowModel`, sets `enableGlobalFilter: false` on all 7 non-name/company columns (`value`, `owner`, `closeDate`, `lifetimeContractValue`, `stage`, `id`, `expandColumn`), and wires `state: { globalFilter }` + `onGlobalFilterChange` + `getFilteredRowModel: getFilteredRowModel()` + `globalFilterFn: "includesString"` into `useLegacyTable`
    - `DealTable.tsx` calls `onVisibleRowsChange` from a `useEffect` keyed on `table.getRowModel().rows`
    - `GroupSection.tsx`'s count badge and $ total are computed from `visibleDeals` state (populated via `onVisibleRowsChange`), not the raw `deals` prop
    - `PipelineToolbar.tsx` renders a controlled search `Input` bound to `globalFilter`/`onGlobalFilterChange`
  </acceptance_criteria>
  <done>Typing in the one shared toolbar search box filters name/company matches live across all 6 pipeline-stage tables simultaneously, and every affected group's header badge/total reflects only what's currently visible — PIPE-04 complete end-to-end for the happy path, with the controlled-state architecture Task 2/3 extend already proven.</done>
</task>

<task type="auto">
  <name>Task 2: Owner/value/close-date column filters + shared header-click sorting</name>
  <read_first>src/features/pipeline/components/PipelineToolbar.tsx (as built by Task 1 — the exact props/JSX being widened, not replaced); src/features/pipeline/components/DealTable.tsx (as built by Task 1 — the exact `useLegacyTable` config and column defs being extended); src/features/pipeline/components/PipelineBoard.tsx (as built by Task 1 — the exact `globalFilter` state/prop-threading pattern to mirror for the new states); src/features/pipeline/components/add-deal-schema.ts (confirms `owner` is free text, not an enum — the owner filter's option list must be derived from live deal data, never hardcoded, per 04-RESEARCH.md Pitfall 2); src/features/pipeline/components/EditableCell.tsx (confirms `closeDate` is stored as a full ISO datetime string, not a bare date — the close-date filter's upper bound must account for time-of-day, see action below)</read_first>
  <files>src/features/pipeline/components/PipelineToolbar.tsx, src/features/pipeline/components/PipelineBoard.tsx, src/features/pipeline/components/DealTable.tsx</files>
  <action>
    In `PipelineBoard.tsx`: import `useMemo` alongside the existing `useState`. Add `const deals = usePipelineStore((s) => s.deals);` (import `usePipelineStore` from `@/features/pipeline/store/pipelineStore`) for deriving filter options across the whole dataset, independent of `usePipelineGroups()`'s per-group partition. Add `const ownerOptions = useMemo(() => [...new Set(deals.map((d) => d.owner))].sort(), [deals]);`. Add `useState`s: `owner` (string, default `""`), `valueMin`/`valueMax` (string, default `""`), `closeDateMin`/`closeDateMax` (string, default `""`), `sorting` (import type `SortingState` from `@tanstack/react-table`; default `[]`). Add `const isValueRangeValid = !(valueMin !== "" && valueMax !== "" && Number(valueMin) > Number(valueMax));` and `const isCloseDateRangeValid = !(closeDateMin !== "" && closeDateMax !== "" && closeDateMin > closeDateMax);` (plain string comparison is correct for `YYYY-MM-DD`-shaped values). Add, importing type `ColumnFiltersState` from `@tanstack/react-table`: `const columnFilters: ColumnFiltersState = useMemo(() => { const filters: ColumnFiltersState = []; if (owner) filters.push({ id: "owner", value: owner }); if (isValueRangeValid && (valueMin !== "" || valueMax !== "")) filters.push({ id: "value", value: [valueMin === "" ? undefined : Number(valueMin), valueMax === "" ? undefined : Number(valueMax)] }); if (isCloseDateRangeValid && (closeDateMin !== "" || closeDateMax !== "")) filters.push({ id: "closeDate", value: [closeDateMin === "" ? undefined : closeDateMin, closeDateMax === "" ? undefined : \`${closeDateMax}T23:59:59\`] }); return filters; }, [owner, valueMin, valueMax, closeDateMin, closeDateMax, isValueRangeValid, isCloseDateRangeValid]);` — the close-date upper bound appends `T23:59:59` so a deal whose `closeDate` timestamp falls anywhere within the selected end day is included, not excluded because its time-of-day is after midnight. Pass `columnFilters`, `sorting`, and `onSortingChange={setSorting}` into every `GroupSection`/`DealTable` call alongside the existing `globalFilter`. Pass `owner`, `onOwnerChange={setOwner}`, `ownerOptions`, `valueMin`, `onValueMinChange={setValueMin}`, `valueMax`, `onValueMaxChange={setValueMax}`, `closeDateMin`, `onCloseDateMinChange={setCloseDateMin}`, `closeDateMax`, `onCloseDateMaxChange={setCloseDateMax}`, `isValueRangeValid`, `isCloseDateRangeValid` into `<PipelineToolbar>`.

    In `PipelineToolbar.tsx`: widen the props interface to also accept the 13 new props listed above. Import `Select, SelectContent, SelectItem, SelectTrigger, SelectValue` from `@/components/ui/select`. After the existing search input, render: an owner `Select` (`value={owner}` `onValueChange={onOwnerChange}`) with a `SelectItem value="">All owners</SelectItem>` plus one `SelectItem` per `ownerOptions` entry; two `Input type="number"` bound to `valueMin`/`valueMax` (`placeholder="Min value"` / `"Max value"`, `onChange={(e) => onValueMinChange(e.target.value)}` etc.), with `{!isValueRangeValid && <p className="text-sm text-destructive">Enter a valid range.</p>}` rendered directly beneath that pair; two `Input type="date"` bound to `closeDateMin`/`closeDateMax`, with `{!isCloseDateRangeValid && <p className="text-sm text-destructive">End date must be on or after the start date.</p>}` beneath that pair.

    In `DealTable.tsx`: add `columnFilters: ColumnFiltersState` (import type from `@tanstack/react-table`), `sorting: SortingState`, and `onSortingChange: OnChangeFn<SortingState>` (import `OnChangeFn` type from `@tanstack/react-table`) to `DealTableProps`. Add `getSortedRowModel` to the existing `/legacy` import. Update `useLegacyTable(...)`: `state: { globalFilter, columnFilters, sorting }`, add `onColumnFiltersChange: () => {}` (no in-table column-filter UI exists — filters only ever change via the toolbar, same no-op rationale as `onGlobalFilterChange`), keep the real `onSortingChange` prop (header clicks DO mutate sorting, via `column.getToggleSortingHandler()`, so this one must be the real parent setter, not a no-op), add `getSortedRowModel: getSortedRowModel()`. Add `filterFn: "equalsString"` to the `owner` accessor column, `filterFn: "inNumberRange"` to `value`, `filterFn: "inDateRange"` to `closeDate`. Change the `header` of the `value`, `owner`, and `closeDate` accessor columns from a plain string to a function: e.g. for `value`, `header: ({ column }) => (<button type="button" className={cn("flex items-center gap-1", column.getIsSorted() && "text-primary")} onClick={column.getToggleSortingHandler()}>Value{column.getIsSorted() === "asc" ? " \u25B2" : column.getIsSorted() === "desc" ? " \u25BC" : ""}</button>)` — same pattern for the `Owner`/`Close Date` headers with their own label text (`cn` is already imported in this file).
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>grep -v '^\s*//' src/features/pipeline/components/DealTable.tsx | grep -c "inNumberRange" | grep -qx 1</automated>
    <automated>grep -v '^\s*//' src/features/pipeline/components/DealTable.tsx | grep -c "inDateRange" | grep -qx 1</automated>
    <automated>grep -v '^\s*//' src/features/pipeline/components/DealTable.tsx | grep -c "getSortedRowModel" | grep -qx 2</automated>
    <automated>grep -v '^\s*//' src/features/pipeline/components/PipelineToolbar.tsx | grep -c "Enter a valid range." | grep -qx 1</automated>
    <automated>grep -v '^\s*//' src/features/pipeline/components/PipelineToolbar.tsx | grep -c "End date must be on or after the start date." | grep -qx 1</automated>
    <human-check>Select a specific owner from the dropdown — confirm only that owner's deals appear across every relevant group. Set the value-min field higher than value-max — confirm "Enter a valid range." appears beneath the two inputs and the value filter has no effect (all deals still show, subject to other active filters). Click a group's "Value" column header — confirm an ascending arrow appears and all 6 groups reorder by value ascending; click again — confirm descending order and arrow flip, synchronized across all 6 groups.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `owner`/`value`/`closeDate` columns declare `filterFn: "equalsString"` / `"inNumberRange"` / `"inDateRange"` respectively
    - `DealTable.tsx` wires `getSortedRowModel` and a real `onSortingChange` (not a no-op) alongside no-op `onGlobalFilterChange`/`onColumnFiltersChange`
    - `PipelineToolbar.tsx` renders the exact inline validation strings "Enter a valid range." and "End date must be on or after the start date." conditioned on `isValueRangeValid`/`isCloseDateRangeValid`
    - The owner filter's options are derived from `deals.map(d => d.owner)`, never a hardcoded array
  </acceptance_criteria>
  <done>Owner, value-range, and close-date-range filters combine (AND semantics) with the existing search across all 6 tables, and clicking any group's Value/Owner/Close Date header sorts all 6 tables together with a visible direction arrow — PIPE-05 (owner/value/close-date) and PIPE-06 complete end-to-end.</done>
</task>

<task type="auto">
  <name>Task 3: Group-visibility toggle, Clear filters, and all-hidden empty state</name>
  <read_first>src/features/pipeline/components/PipelineBoard.tsx (as built by Task 2 — the exact state list being extended, and the `GROUPS.map` render loop being made conditional); src/features/pipeline/components/PipelineToolbar.tsx (as built by Task 2 — the exact prop list/JSX being widened); src/features/pipeline/hooks/usePipelineGroups.ts (confirms `GROUPS` is the canonical 6-group array/order to reuse for the toggle's option list, never re-derived)</read_first>
  <files>src/features/pipeline/components/PipelineBoard.tsx, src/features/pipeline/components/PipelineToolbar.tsx, src/components/ui/toggle-group.tsx</files>
  <action>
    Run `npx shadcn@latest add toggle-group` (official shadcn registry only — 04-UI-SPEC.md Registry Safety confirms no third-party source, vetting gate not triggered). This generates `src/components/ui/toggle-group.tsx` exporting `ToggleGroup`/`ToggleGroupItem`.

    In `PipelineBoard.tsx`: import `GROUPS` is already present via `usePipelineGroups`; import `PipelineGroup` type from `@/shared/types/deal`. Add `const [visibleGroups, setVisibleGroups] = useState<PipelineGroup[]>([...GROUPS]);`. Replace the render loop's source array: `const groupsToRender = GROUPS.filter((g) => visibleGroups.includes(g));` and map over `groupsToRender` instead of `GROUPS` directly. When `groupsToRender.length === 0`, render, in place of any `GroupSection`, `<div className="rounded-xl border border-border bg-card p-8 text-center"><p className="font-heading text-sm font-semibold">No pipeline stages selected.</p><p className="text-sm text-muted-foreground">Choose at least one stage above to see deals.</p></div>`. Add `const handleClearFilters = () => { setGlobalFilter(""); setOwner(""); setValueMin(""); setValueMax(""); setCloseDateMin(""); setCloseDateMax(""); setSorting([]); setVisibleGroups([...GROUPS]); };`. Pass `visibleGroups`, `onVisibleGroupsChange={setVisibleGroups}`, and `onClearFilters={handleClearFilters}` into `<PipelineToolbar>`.

    In `PipelineToolbar.tsx`: import `ToggleGroup, ToggleGroupItem` from `@/components/ui/toggle-group` and `Button` from `@/components/ui/button`. Widen props to add `visibleGroups: PipelineGroup[]; onVisibleGroupsChange: (groups: PipelineGroup[]) => void; onClearFilters: () => void`. Define a local `const GROUP_LABELS: Record<PipelineGroup, string> = { prospect: "Prospect", lead: "Lead", opportunity: "Opportunity", deal: "Deal", won: "Won", lost: "Lost" };` (import `PipelineGroup` type from `@/shared/types/deal` and `GROUPS` from `@/features/pipeline/hooks/usePipelineGroups`). Render, at the end of the toolbar row: `<ToggleGroup type="multiple" value={visibleGroups} onValueChange={(next) => onVisibleGroupsChange(next as PipelineGroup[])}>{GROUPS.map((group) => (<ToggleGroupItem key={group} value={group} aria-label={GROUP_LABELS[group]}>{GROUP_LABELS[group]}</ToggleGroupItem>))}</ToggleGroup>` followed by `<Button type="button" variant="ghost" size="sm" onClick={onClearFilters}>Clear filters</Button>`.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>test -f src/components/ui/toggle-group.tsx</automated>
    <automated>grep -v '^\s*//' src/features/pipeline/components/PipelineBoard.tsx | grep -c "No pipeline stages selected." | grep -qx 1</automated>
    <automated>grep -c "usePipelineStore" src/features/pipeline/components/PipelineToolbar.tsx | grep -qx 0</automated>
    <automated>! grep -Eq '\.sort\(' src/features/pipeline/components/DealTable.tsx</automated>
    <human-check>Uncheck the "Won" and "Lost" pills in the toggle group — confirm those two sections disappear from the board while the other four remain, fully interactive. Uncheck every pill — confirm the "No pipeline stages selected." message appears in place of any section. Set a search term, an owner filter, and a sort column, then click "Clear filters" — confirm the search box empties, the owner dropdown returns to "All owners", sort arrows disappear, and all 6 stage pills are active again.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `src/components/ui/toggle-group.tsx` exists and exports `ToggleGroup`/`ToggleGroupItem`
    - `PipelineBoard.tsx` renders "No pipeline stages selected." only when `groupsToRender.length === 0`
    - `PipelineToolbar.tsx` contains zero references to `usePipelineStore` (a pure props-in/callbacks-out component — this plan's data-mutation prohibition holds by construction)
    - `DealTable.tsx` contains no direct `.sort(` call (sorting only ever goes through `getSortedRowModel()`) — this plan's sort-mutation prohibition holds by construction
  </acceptance_criteria>
  <done>The stage-visibility toggle shows/hides whole groups without altering deal data, a single "Clear filters" action resets every control to its default, and deselecting every stage shows a clear empty-state message instead of a blank board — PIPE-05's stage filter complete, and this plan's two data-integrity prohibitions verified by construction.</done>
</task>

</tasks>

## Artifacts This Phase Produces (this plan)

- `src/features/pipeline/components/PipelineToolbar.tsx` (new) — search input, owner/value/close-date filter controls, group-visibility ToggleGroup, Clear filters button
- `src/features/pipeline/components/DealTable.tsx` (edited) — controlled `globalFilter`/`columnFilters`/`sorting` state, `getFilteredRowModel`/`getSortedRowModel` wiring, `onVisibleRowsChange` callback
- `src/features/pipeline/components/PipelineBoard.tsx` (edited) — lifted search/filter/sort/visibleGroups state, derived `columnFilters`, `groupsToRender`, all-hidden empty state, `handleClearFilters`
- `src/features/pipeline/components/GroupSection.tsx` (edited) — `visibleDeals`-driven count badge/$ total
- `src/components/ui/toggle-group.tsx` (new, via shadcn) — `ToggleGroup`, `ToggleGroupItem`

## Flagged Assumptions (specless probe fallback — surfaced, not dropped)

- **Edge probe, PIPE-05, `unclassified` (unresolved):** the probe classifier could not map PIPE-05 as a whole to a single data-shape edge category — the real ambiguity is how multiple simultaneous filters (search, owner, value range, close-date range) combine. REQUIREMENTS.md does not specify filter-combination logic. Resolved here as an explicit contract, not left ambiguous in code: all active filters combine with AND semantics (TanStack's default `getFilteredRowModel` behavior when both `globalFilterFn` and per-column `filterFn`s are present — no special configuration needed) — see `must_haves.truths` and Task 2's `columnFilters` derivation, which only ever adds constraints, never substitutes one for another.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (user-typed search/filter text, clicked sort headers, clicked toggle pills) -> React component state -> TanStack Table row-model computation -> rendered DOM | This plan's entire untrusted-input surface — no network boundary exists in this frontend-only prototype |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering / Information Disclosure | Search/filter text and "no results" messaging rendered back into the DOM | low | mitigate | React's default JSX interpolation (`{value}`) auto-escapes every rendered value; no `dangerouslySetInnerHTML` is introduced anywhere search text or filter chips are displayed |
| T-04-02 | Tampering / Integrity | Filter/sort logic accidentally mutating `usePipelineStore`'s canonical `deals` array or writing through a store action | medium | mitigate | `PipelineToolbar` is a pure props-in/callbacks-out component with no `usePipelineStore` import; sorting goes through `getSortedRowModel()` (never `Array.prototype.sort()` on the store's array); verified by construction in Task 3's acceptance criteria and this plan's `must_haves.prohibitions` |
| T-04-03 | Denial of Service | A malformed numeric/date filter bound (e.g. non-numeric text coerced by `Number()`) silently matching zero rows or throwing during row-model computation | low | mitigate | `type="number"`/`type="date"` native inputs constrain entry; `isValueRangeValid`/`isCloseDateRangeValid` guard the min>max case before a filter is ever added to `columnFilters`, so an invalid combination never reaches `inNumberRange`/`inDateRange` |

</threat_model>

<verification>
- `npm run build` exits 0 after all 3 tasks
- Search, owner, value-range, and close-date-range filters all combine (AND) and apply identically across all 6 pipeline-stage tables from one shared toolbar
- Header-click sorting on Value/Owner/Close Date applies to all 6 tables simultaneously with a visible direction indicator
- The group-visibility toggle shows/hides whole `GroupSection` blocks without altering any deal's stored data
- Every group's count badge/$ total always matches its currently-visible (filtered/sorted) rows, not its full pre-partition array
</verification>

<success_criteria>
A user can search by name/company, filter by owner/value/stage/close-date, and sort by value/close-date/owner across the entire pipeline board from one shared toolbar — satisfying PIPE-04, PIPE-05, and PIPE-06 end to end, with badges/totals that never lie about what's currently visible.
</success_criteria>

<output>
Create `.planning/phases/04-forecast-pipeline-analysis/04-01-SUMMARY.md` when done
</output>
