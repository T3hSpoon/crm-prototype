---
phase: 04-forecast-pipeline-analysis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/utils/pipeline-group.ts
  - src/features/pipeline/store/pipelineStore.ts
  - src/data/mock/mock-deals-repository.ts
  - src/features/forecast/forecast-metrics.ts
  - src/features/forecast/components/StatTile.tsx
  - src/features/forecast/components/ForecastPage.tsx
  - src/features/forecast/components/LostBreakdownChart.tsx
  - src/app/App.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/card.tsx
  - src/index.css
  - package.json
  - package-lock.json
autonomous: true
requirements: [FCST-01, FCST-02]

estimate:
  tokens: 98000
  raw_tokens: 98000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A new 'Forecast' tab sits alongside 'Pipeline' as a local view-state toggle (no router); it reuses PipelineBoard's `w-[95%]` page shell so it reads as a sibling view, not a different app (D-01)."
    - "Switching from Pipeline to Forecast and back never resets the Pipeline board's in-progress search/filter/sort/expanded-row state — both views stay mounted at all times, visibility toggled via a CSS `hidden` class, never a conditional unmount/remount (D-02)."
    - "The Forecast page has no 'Add Deal' button or any other write action — it is a read-only analysis view (D-03)."
    - "Moving a deal away from Lost (e.g. back to Prospect) clears its `lostReason`; moving a deal away from Won clears all 4 contract-term fields; a deal moved Lost -> Prospect -> Lost again with a new reason shows only the latest reason, never a stale one from the earlier Lost transition — enforced by one centralized `clearPatchFor` helper called from `moveStage`, `moveToLost`, and `moveToWon` alike, so the 3 call sites cannot drift (D-12, D-13, CR-01 resolved)."
    - "'Raw Pipeline Value' sums `Deal.value` across open deals only (`outcome === \"open\"`) — Won and Lost deals are excluded (D-08, FCST-01)."
    - "'Weighted Value' sums `deal.value` times confidence weight over the same open-deals scope, using the locked Confidence Level mapping 100%->1.0, 80%->0.8, 50%->0.5, Open to RFP Bids->0.0 (D-09, FCST-01)."
    - "'Win Rate' = won / (won + lost), counting only deals that reached a terminal outcome; still-open deals are excluded from both numerator and denominator; when no deal has reached a terminal outcome yet, the tile shows '—' and 'No closed deals yet.' instead of '0%' or 'NaN%' (D-10, FCST-01, resolved edge FCST-01 boundary/precision)."
    - "Win rate and weighted-value figures round to the nearest whole unit via standard round-half-up (`Math.round`), matching the existing whole-dollar, no-decimal `Intl.NumberFormat` convention used elsewhere in the app (resolved edge, FCST-01 precision)."
    - "A deal at 0% confidence (Open to RFP Bids) contributes $0 to weighted pipeline value; a deal at 100% confidence contributes its full raw value — the formula is a direct value times weight product with no additional floor/ceiling clamp (resolved edge, FCST-01 boundary)."
    - "The lost-by-reason breakdown groups by the `lostReason` category prefix (split on the first ':') — never the raw '{category}: {note}' string and never the free-text note portion itself as a chart category (resolved assumption, FCST-02 unclassified — REQUIREMENTS.md did not specify the aggregation key; encoded here as the explicit contract)."
    - "The lost-by-stage breakdown reads `deal.pipelineStage` directly, never `toPipelineGroup(deal)` — a lost deal retains the stage it fell through from, so 'lost' is never itself treated as a stage bucket (FCST-02)."
    - "Zero lost deals renders each chart's empty-state copy ('No lost deals recorded yet — this chart fills in once a deal is marked Lost.') in place of an axis with no bars (UI-SPEC empty)."
    - statement: "More than 6 distinct `lostReason` categories fold everything ranked below the top 5 by count into a single 'Other' bucket, keeping the lost-by-reason chart's x-axis to at most 6 bars."
      verification: backstop
    - statement: "Category tick labels longer than ~14 characters truncate with an ellipsis in the axis tick and show the full string in that bar's tooltip."
      verification: backstop
    - "Stat-tile values use `tabular-nums` and wrap to a second line rather than clip when a 7-8 digit total would overflow the tile (UI-SPEC long-text)."
  artifacts:
    - path: "src/shared/utils/pipeline-group.ts"
      provides: "clearPatchFor(group): Partial<Deal> — the D-13 centralized clear-patch helper"
    - path: "src/features/pipeline/store/pipelineStore.ts"
      provides: "moveStage/moveToLost/moveToWon all spread clearPatchFor(group) into their patch object"
    - path: "src/data/mock/mock-deals-repository.ts"
      provides: "update()'s Pick<Deal, ...> widened to include the 4 contract-term fields, matching DealsRepository's interface"
    - path: "src/features/forecast/forecast-metrics.ts"
      provides: "computeRawPipelineValue, computeWeightedPipelineValue, computeWinRate, computeLostByReason, computeLostByStage, bucketTopCategories — pure, never-store-importing derived-value functions"
    - path: "src/features/forecast/components/ForecastPage.tsx"
      provides: "The read-only Forecast tab content: 3 stat tiles + 2 Recharts bar charts, reading usePipelineStore's full deals array directly"
    - path: "src/features/forecast/components/LostBreakdownChart.tsx"
      provides: "Single-series Recharts BarChart wrapper for the lost-by-reason/lost-by-stage breakdowns"
    - path: "src/app/App.tsx"
      provides: "Pipeline/Forecast tab switch (shadcn Tabs), both views kept mounted"
  key_links:
    - from: "src/features/pipeline/store/pipelineStore.ts"
      to: "src/shared/utils/pipeline-group.ts"
      via: "moveStage/moveToLost/moveToWon each spread ...clearPatchFor(group) into their patch object alongside fromPipelineGroup(...)"
      pattern: "centralized clear-patch helper (04-RESEARCH.md Pattern 3)"
    - from: "src/features/forecast/components/ForecastPage.tsx"
      to: "src/features/pipeline/store/pipelineStore.ts"
      via: "const deals = usePipelineStore((s) => s.deals) — the full, unfiltered canonical array, independent of any Pipeline-tab toolbar state"
      pattern: "direct store read, no filtered intermediary"
    - from: "src/features/forecast/components/ForecastPage.tsx"
      to: "src/features/forecast/forecast-metrics.ts"
      via: "computeRawPipelineValue(deals) / computeWeightedPipelineValue(deals) / computeWinRate(deals) / computeLostByReason(deals) / computeLostByStage(deals)"
      pattern: "pure derived-value module (mirrors deal-metrics.ts)"
    - from: "src/app/App.tsx"
      to: "src/features/pipeline/components/PipelineBoard.tsx / src/features/forecast/components/ForecastPage.tsx"
      via: "both rendered unconditionally, visibility toggled with a `hidden` class keyed on local tab state"
      pattern: "stay-mounted tab switch (D-02)"
  prohibitions:
    - statement: "The Forecast page's 'Weighted Value' stat tile MUST NOT be captioned or styled in a way that implies committed/guaranteed revenue — it is a probability-weighted estimate (deal value times confidence weight, D-09); its caption must read 'Weighted Value' or equivalent estimate-framing copy, never 'Guaranteed' or 'Committed'."
      status: resolved
      verification: judgment
    - statement: "Forecast calculations (raw pipeline value, weighted value, win rate, lost-by-reason, lost-by-stage) MUST NOT be computed over a search/filter/group-visibility-narrowed subset of deals — `forecast-metrics.ts`'s functions must always receive the full, unfiltered `deals` array read directly from `usePipelineStore`, independent of any Pipeline-tab toolbar state, so Forecast numbers never silently reflect a forgotten filter."
      status: resolved
      verification: judgment
    - statement: "The lost-by-reason and lost-by-stage bar charts MUST NOT visually or textually imply they represent lost-deal dollar VALUE — `computeLostByReason`/`computeLostByStage` return deal COUNTS (Record<string, number>), and each chart's Y-axis must be explicitly labeled as a count ('Number of Lost Deals'), never rendered with a currency formatter."
      status: resolved
      verification: judgment
---

<objective>
**As a** sales manager reviewing the pipeline, **I want to** see raw and weighted pipeline value, win rate, and where deals are being lost, **so that** I can gauge how much of the pipeline is real versus optimistic and where the pipeline is leaking, without exporting data anywhere else.

This plan delivers FCST-01 and FCST-02 as one vertical slice, plus the CR-01 data-integrity prerequisite `03.1-REVIEW.md` flagged specifically for this phase. Task 1 (tracer) fixes the stale-field bug end to end (a centralized `clearPatchFor` helper wired into all 3 stage-transition store actions), builds the core `forecast-metrics.ts` pure functions, and wires a genuinely reachable "Forecast" tab showing 3 live stat tiles — proving the store-fix -> pure-computation -> UI architecture with real data before any chart exists. Task 2 (expansion) adds the two required Recharts bar charts. Task 3 (expansion) adds the UI-SPEC's overflow/long-text/empty-state polish.

Purpose: closes the analysis half of Phase 4 and permanently resolves the lost/won stale-field gap flagged since Phase 3.1.
Output: a "Forecast" tab, reachable without losing Pipeline-tab state, showing raw/weighted pipeline value, win rate, and two lost-deal breakdown charts, all computed from clean, non-stale deal data.
</objective>

<execution_context>
@C:/gh-repos/eld/.claude/gsd-core/workflows/execute-plan.md
@C:/gh-repos/eld/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/gh-repos/eld/.planning/PROJECT.md
@C:/gh-repos/eld/.planning/ROADMAP.md
@C:/gh-repos/eld/.planning/STATE.md
@C:/gh-repos/eld/.planning/phases/04-forecast-pipeline-analysis/04-CONTEXT.md
@C:/gh-repos/eld/.planning/phases/04-forecast-pipeline-analysis/04-RESEARCH.md
@C:/gh-repos/eld/.planning/phases/04-forecast-pipeline-analysis/04-PATTERNS.md
@C:/gh-repos/eld/.planning/phases/04-forecast-pipeline-analysis/04-UI-SPEC.md
@C:/gh-repos/eld/.planning/phases/03.1-lost-won-tracking/03.1-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end Forecast tab — CR-01 stale-field fix, core stat tiles, live data</name>
  <reversibility rating="reversible">D-13: clearPatchFor is an additive store-layer helper with no Deal shape change. D-09: forecast-metrics.ts is a pure computed-value module with no persisted state. Both are local, cheaply-undone changes behind stable interfaces.</reversibility>
  <read_first>src/shared/utils/pipeline-group.ts (full 35-line file — the exact `toPipelineGroup`/`fromPipelineGroup` bodies `clearPatchFor` is added alongside); src/features/pipeline/store/pipelineStore.ts (full 107-line file — the exact `moveStage`/`moveToLost`/`moveToWon` patch-object composition at each of the 3 call sites being extended); src/data/mock/mock-deals-repository.ts (full file — the exact `update()` Pick type being widened); src/data/deals-repository.ts (confirm the interface already includes all 4 contract-term keys, so only the mock implementation's Pick needs widening); src/shared/types/deal.ts (full file — `Deal`/`ConfidenceLevel`/`DealOutcome`/`PipelineStage` shapes, confirm `confidenceLevel` field and its 4 literal values); src/shared/utils/deal-metrics.ts (full 51-line file — the exact pure-function-module pattern `forecast-metrics.ts` mirrors: type-only import, no store/repository import, `.filter().reduce()` shape); src/app/App.tsx (full 20-line current file); src/features/pipeline/components/PipelineBoard.tsx (confirm the `mx-auto flex w-[95%] flex-col gap-6 px-6 py-8` page-shell class to mirror in ForecastPage); src/features/pipeline/components/GroupSection.tsx (confirm the `currencyFormatter` `Intl.NumberFormat` pattern to mirror in StatTile/ForecastPage, since no shared formatter module exists yet)</read_first>
  <files>src/shared/utils/pipeline-group.ts, src/features/pipeline/store/pipelineStore.ts, src/data/mock/mock-deals-repository.ts, src/features/forecast/forecast-metrics.ts, src/features/forecast/components/StatTile.tsx, src/features/forecast/components/ForecastPage.tsx, src/app/App.tsx</files>
  <action>
    In `pipeline-group.ts`: add `Deal` to the existing `import type { Deal, DealOutcome, PipelineGroup, PipelineStage } from "@/shared/types/deal";` line (currently missing `Deal`). Add `export function clearPatchFor(group: PipelineGroup): Partial<Deal> { const patch: Partial<Deal> = {}; if (group !== "lost") patch.lostReason = undefined; if (group !== "won") { patch.contractStartDate = undefined; patch.contractEndDate = undefined; patch.contractSignedDate = undefined; patch.paymentTerms = undefined; } return patch; }` after `fromPipelineGroup`.

    In `pipelineStore.ts`: add `clearPatchFor` to the existing `import { fromPipelineGroup } from "@/shared/utils/pipeline-group";` line. In `moveStage`, change `const patch = fromPipelineGroup(group, current?.pipelineStage);` to `const patch = { ...fromPipelineGroup(group, current?.pipelineStage), ...clearPatchFor(group) };`. In `moveToLost`, change the patch object to `const patch = { ...fromPipelineGroup("lost", current?.pipelineStage), lostReason: note ? \`${category}: ${note}\` : category, ...clearPatchFor("lost") };` — note `clearPatchFor("lost")` only ever returns the 4 contract fields (never touches `lostReason` since `group === "lost"`), so the spread order after the `lostReason:` assignment is safe by construction; keep this ordering anyway so a future edit to `clearPatchFor` cannot silently reintroduce a clobbering hazard. In `moveToWon`, change to `const patch = { ...fromPipelineGroup("won", current?.pipelineStage), ...terms, ...clearPatchFor("won") };`.

    In `mock-deals-repository.ts`'s `update()` signature: widen the `Pick<Deal, ...>` union from `"pipelineStage" | "outcome" | "lostReason" | "name" | "value" | "owner" | "closeDate" | "lineItems"` to also include `"contractStartDate" | "contractEndDate" | "contractSignedDate" | "paymentTerms"`, matching `DealsRepository`'s already-wider interface (bundled low-cost fix, 04-RESEARCH.md Pitfall 4 / `03.1-REVIEW.md` WR-01 — this task already touches this exact call site).

    Create `src/features/forecast/forecast-metrics.ts` mirroring `deal-metrics.ts`'s pattern exactly: `import type { Deal, ConfidenceLevel } from "@/shared/types/deal";`, no store/repository import. Define `const CONFIDENCE_WEIGHT: Record<ConfidenceLevel, number> = { "100": 1.0, "80": 0.8, "50": 0.5, "open-to-rfp": 0.0 };` (D-09's locked mapping). Export `function computeRawPipelineValue(deals: Deal[]): number` returning the sum of `value` over `deals.filter((d) => d.outcome === "open")` (D-08). Export `function computeWeightedPipelineValue(deals: Deal[]): number` returning the sum of `d.value * CONFIDENCE_WEIGHT[d.confidenceLevel]` over the same `outcome === "open"` filter. Export `function computeWinRate(deals: Deal[]): number` computing `won`/`lost` counts via `deals.filter((d) => d.outcome === "won").length` / `"lost"`, returning `won + lost === 0 ? 0 : won / (won + lost)` (D-10, divide-by-zero guard).

    Create `src/features/forecast/components/StatTile.tsx`. Props: `{ label: string; value: string; caption?: string }` (a purely presentational tile — callers pre-format `value`, e.g. via `currencyFormatter.format(...)` or a percent string or "—"). Render: `<Card><CardContent className="flex flex-col gap-1 p-4"><span className="text-xs font-semibold text-muted-foreground">{label}</span><span className="text-2xl font-semibold tabular-nums break-words">{value}</span>{caption && <span className="text-xs text-muted-foreground">{caption}</span>}</CardContent></Card>` (import `Card, CardContent` from `@/components/ui/card`, installed by this task — see below).

    Create `src/features/forecast/components/ForecastPage.tsx`. `import { usePipelineStore } from "@/features/pipeline/store/pipelineStore";` and `const deals = usePipelineStore((s) => s.deals);` — the full, unfiltered canonical array (never a filtered/narrowed subset — this plan's data-integrity prohibition). Define a local `const currencyFormatter = new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 });` (matches `GroupSection`'s existing pattern; no shared formatter module exists). Compute `const raw = computeRawPipelineValue(deals); const weighted = computeWeightedPipelineValue(deals); const won = deals.filter((d) => d.outcome === "won").length; const lost = deals.filter((d) => d.outcome === "lost").length; const winRate = computeWinRate(deals);`. Render root `<div className="mx-auto flex w-[95%] flex-col gap-6 px-6 py-8">` (mirrors `PipelineBoard`'s exact shell class, D-01) containing `<h1 className="font-heading text-lg font-semibold">Forecast</h1>` and a stat-tile row `<div className="grid grid-cols-1 gap-4 sm:grid-cols-3">` with 3 `StatTile`s: `label="Raw Pipeline Value"` `value={currencyFormatter.format(raw)}`; `label="Weighted Value"` `value={currencyFormatter.format(weighted)}` (caption text stays "Weighted Value" — never "Guaranteed"/"Committed", this plan's FCST-01 prohibition); `label="Win Rate"` `value={won + lost === 0 ? "—" : \`${Math.round(winRate * 100)}%\`}` `caption={won + lost === 0 ? "No closed deals yet." : undefined}`.

    In `App.tsx`: run `npx shadcn@latest add tabs` and `npx shadcn@latest add card` (official shadcn registry only — 04-UI-SPEC.md Registry Safety confirms no third-party source, vetting gate not triggered). Import `Tabs, TabsList, TabsTrigger` from `@/components/ui/tabs`; import `ForecastPage` from `@/features/forecast/components/ForecastPage`. Add `const [tab, setTab] = useState<"pipeline" | "forecast">("pipeline");` (import `useState`). Replace the current `return <PipelineBoard />;` with a fragment rendering: `<Tabs value={tab} onValueChange={(v) => setTab(v as "pipeline" | "forecast")} className="mx-auto w-[95%] pt-4"><TabsList><TabsTrigger value="pipeline">Pipeline</TabsTrigger><TabsTrigger value="forecast">Forecast</TabsTrigger></TabsList></Tabs>` immediately followed by `<div className={tab === "pipeline" ? "" : "hidden"}><PipelineBoard /></div>` and `<div className={tab === "forecast" ? "" : "hidden"}><ForecastPage /></div>` — both views are rendered unconditionally at all times (never `{tab === "x" && <Component />}`), with only CSS `hidden` toggling visibility, so switching tabs never remounts `PipelineBoard` and never resets any in-flight search/filter/sort state it holds (D-02).
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>grep -v '^\s*//' src/features/pipeline/store/pipelineStore.ts | grep -o "clearPatchFor(" | wc -l | grep -qx 3</automated>
    <automated>grep -v '^\s*//' src/data/mock/mock-deals-repository.ts | grep -c "contractStartDate" | grep -qx 1</automated>
    <automated>grep -c "export function computeRawPipelineValue\|export function computeWeightedPipelineValue\|export function computeWinRate" src/features/forecast/forecast-metrics.ts | grep -qx 3</automated>
    <automated>! grep -Eq "usePipelineStore|PipelineToolbar" src/features/forecast/forecast-metrics.ts</automated>
    <automated>grep -c 'tab === "pipeline" ? "" : "hidden"' src/app/App.tsx | grep -qx 1</automated>
    <automated>grep -c 'tab === "forecast" ? "" : "hidden"' src/app/App.tsx | grep -qx 1</automated>
    <human-check>Click "Forecast" — confirm a new tab shows 3 stat tiles (Raw Pipeline Value, Weighted Value, Win Rate) with real numbers derived from the seeded deals. Go to Pipeline, type something in the search box (once 04-01-PLAN.md is also executed) or expand a deal's line items, switch to Forecast and back — confirm the Pipeline tab's state is unchanged. Mark a deal Lost, then move it back to an active stage, then mark it Lost again with a different reason — confirm (by reopening its stage dropdown or checking the Forecast page's lost breakdown once Task 2 lands) that only the latest reason is ever recorded.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `clearPatchFor` is exported from `pipeline-group.ts` and spread into the patch object in each of `moveStage`, `moveToLost`, `moveToWon` (exactly 3 call sites)
    - `mock-deals-repository.ts`'s `update()` Pick type includes all 4 contract-term field names
    - `forecast-metrics.ts` exports `computeRawPipelineValue`, `computeWeightedPipelineValue`, `computeWinRate` and imports no store module
    - `ForecastPage.tsx` reads `deals` directly via `usePipelineStore((s) => s.deals)` with no reference to any Pipeline-tab filter/toggle state
    - `App.tsx` renders both `PipelineBoard` and `ForecastPage` unconditionally, toggling visibility via a `hidden` class, never a conditional `&&` render
  </acceptance_criteria>
  <done>A new "Forecast" tab is reachable alongside "Pipeline" without a router; it shows 3 live stat tiles computed from the store's full deal list; moving a deal Lost -> Prospect -> Lost again (or Won -> Prospect) now clears the opposite terminal state's stale fields via one centralized helper called from all 3 stage-transition actions (CR-01 resolved); switching tabs never resets Pipeline board state — FCST-01's core stat tiles complete end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lost-by-reason and lost-by-stage bar charts</name>
  <behavior>
    - `computeLostByReason([{ outcome: "lost", lostReason: "Price: too expensive" }, { outcome: "lost", lostReason: "Price" }, { outcome: "open", lostReason: undefined }])` returns `{ Price: 2 }` — groups by the category prefix before the first `:`, ignores open deals, and a bare-category `lostReason` (no `:`) counts under its own value unchanged.
    - `computeLostByReason([{ outcome: "lost", lostReason: undefined }])` returns `{ Unknown: 1 }` — a lost deal with no recorded reason still counts, under an "Unknown" bucket, never silently dropped.
    - `computeLostByStage([{ outcome: "lost", pipelineStage: "opportunity" }, { outcome: "won", pipelineStage: "opportunity" }])` returns `{ prospect: 0, lead: 0, opportunity: 1, deal: 0 }` — reads `pipelineStage` directly (never `toPipelineGroup`, which would return `"lost"`/`"won"` and destroy the stage information), and only counts `outcome === "lost"` deals.
  </behavior>
  <read_first>src/features/forecast/forecast-metrics.ts (as built by Task 1 — the exact module pattern/imports being extended, not replaced); src/shared/utils/pipeline-group.ts (the `toPipelineGroup` doc comment confirming a lost deal retains its `pipelineStage` — this is why the by-stage breakdown must read `pipelineStage` directly); src/index.css (the exact `:root { ... }` / `.dark { ... }` blocks and existing `--chart-1`..`--chart-5` custom-property pattern the new `--chart-lost-bar`/`--chart-lost-grid` properties are added alongside); 04-UI-SPEC.md Color section (single sequential-hue bar fill, light `#2a78d6` / dark `#3987e5`, never a per-bar categorical rainbow, and the Y-axis must read as a count, never a currency value — read via the phase context already loaded, no separate file read needed)</read_first>
  <files>src/index.css, src/features/forecast/forecast-metrics.ts, src/features/forecast/components/LostBreakdownChart.tsx, src/features/forecast/components/ForecastPage.tsx, package.json, package-lock.json</files>
  <action>
    Run `npm install recharts` (adds `recharts@^3.10.1` — 04-RESEARCH.md Package Legitimacy Audit verdict: OK, approved).

    In `src/index.css`: add `--chart-lost-bar: #2a78d6;` and `--chart-lost-grid: #e1e0d9;` to the existing `:root { ... }` block, alongside the existing `--chart-1`..`--chart-5` declarations (these are the 04-UI-SPEC.md Chart Color table's light-mode values). Add `--chart-lost-bar: #3987e5;` and `--chart-lost-grid: #2c2c2a;` to the existing `.dark { ... }` block at the equivalent position (the same table's dark-mode values). These two new custom properties flip automatically whenever an ancestor carries the `.dark` class, exactly like every other theme token already in this file — `LostBreakdownChart` below reads them via `var()` and needs no theme-detection logic of its own.

    In `forecast-metrics.ts`, add `PipelineStage` to the existing type-only import line. Add: `export function computeLostByReason(deals: Deal[]): Record<string, number> { const counts: Record<string, number> = {}; for (const d of deals.filter((deal) => deal.outcome === "lost")) { const category = d.lostReason?.split(":")[0]?.trim() ?? "Unknown"; counts[category] = (counts[category] ?? 0) + 1; } return counts; }` and `export function computeLostByStage(deals: Deal[]): Record<PipelineStage, number> { const counts: Record<PipelineStage, number> = { prospect: 0, lead: 0, opportunity: 0, deal: 0 }; for (const d of deals.filter((deal) => deal.outcome === "lost")) { counts[d.pipelineStage] += 1; } return counts; }`.

    Create `src/features/forecast/components/LostBreakdownChart.tsx`: `import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";` and `import { Card, CardContent } from "@/components/ui/card";`. Props: `{ title: string; counts: Record<string, number> }`. Define `function toChartData(counts: Record<string, number>) { return Object.entries(counts).map(([name, count]) => ({ name, count })); }`. Render: `<Card><CardContent className="p-4"><h3 className="text-sm font-semibold">{title}</h3><ResponsiveContainer width="100%" height={260}><BarChart data={toChartData(counts)}><CartesianGrid strokeDasharray="3 3" stroke="var(--chart-lost-grid)" /><XAxis dataKey="name" tick={{ fill: "#898781", fontSize: 12 }} /><YAxis allowDecimals={false} tick={{ fill: "#898781", fontSize: 12 }} label={{ value: "Number of Lost Deals", angle: -90, position: "insideLeft", fill: "#898781", fontSize: 12 }} /><Tooltip /><Bar dataKey="count" fill="var(--chart-lost-bar)" radius={[4, 4, 0, 0]} /></BarChart></ResponsiveContainer></CardContent></Card>` — Recharts passes `fill`/`stroke` straight through as SVG presentation attributes, which resolve CSS custom properties the same as any CSS value, so `var(--chart-lost-bar)` renders `#2a78d6` in light mode and automatically switches to `#3987e5` (gridline: `#e1e0d9` -> `#2c2c2a`) under `.dark`, with zero React-side theme branching. A single sequential hue fills every bar in either theme; never introduce a per-bar categorical color array, and never format the Y-axis or Tooltip value with a currency formatter (this plan's FCST-02 count-vs-value prohibition, made concrete by the explicit "Number of Lost Deals" axis label).

    In `ForecastPage.tsx`: import `computeLostByReason, computeLostByStage` and `LostBreakdownChart`. Compute `const lostByReason = computeLostByReason(deals); const lostByStage = computeLostByStage(deals);`. Render a 2-column chart row below the stat tiles: `<div className="grid grid-cols-1 gap-4 lg:grid-cols-2"><LostBreakdownChart title="Lost Deals by Reason" counts={lostByReason} /><LostBreakdownChart title="Lost Deals by Stage" counts={lostByStage} /></div>`.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>grep -c '"recharts"' package.json | grep -qx 1</automated>
    <automated>grep -v '^\s*//' src/features/forecast/forecast-metrics.ts | grep -c 'split(":")' | grep -qx 1</automated>
    <automated>grep -v '^\s*//' src/features/forecast/forecast-metrics.ts | grep -c "d.pipelineStage" | grep -qx 1</automated>
    <automated>grep -c "Number of Lost Deals" src/features/forecast/components/LostBreakdownChart.tsx | grep -qx 1</automated>
    <automated>! grep -q "Intl.NumberFormat" src/features/forecast/components/LostBreakdownChart.tsx</automated>
    <automated>grep -c -- "--chart-lost-bar" src/index.css | grep -qx 2</automated>
    <automated>grep -c -- "--chart-lost-grid" src/index.css | grep -qx 2</automated>
    <automated>grep -c "var(--chart-lost-bar)" src/features/forecast/components/LostBreakdownChart.tsx | grep -qx 1</automated>
    <automated>grep -c "var(--chart-lost-grid)" src/features/forecast/components/LostBreakdownChart.tsx | grep -qx 1</automated>
    <human-check>On the Forecast tab, confirm two bar charts render below the stat tiles: "Lost Deals by Reason" (bars per category: Price/Timing/Competitor/No Budget/Other) and "Lost Deals by Stage" (bars per Prospect/Lead/Opportunity/Deal). Hover a bar and confirm the tooltip shows a plain count, not a dollar figure. Mark a deal Lost with a note (e.g. category "Other", note "test note") and confirm the chart's "Other" bar count increases by 1 and no new bar appears for the note text itself. Add a `dark` class to the `<html>` element via devtools and confirm the bar fill switches to the lighter blue (`#3987e5`) and the gridline darkens — no code change required to see this, it's the CSS variable flip.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `recharts` appears in `package.json` dependencies
    - `computeLostByReason` splits `lostReason` on `":"` and never keys by the full raw string
    - `computeLostByStage` reads `d.pipelineStage` directly, never `toPipelineGroup`
    - `LostBreakdownChart.tsx` renders a `YAxis` labeled "Number of Lost Deals" and contains no `Intl.NumberFormat` currency formatting
    - Both charts use the single fill color driven by `var(--chart-lost-bar)` (light `#2a78d6` / dark `#3987e5` per 04-UI-SPEC.md) — no per-bar categorical color array, and no hardcoded dark-mode-blind hex in the chart component itself
    - `src/index.css` declares `--chart-lost-bar`/`--chart-lost-grid` in both `:root` and `.dark`, matching 04-UI-SPEC.md's Chart Color table exactly
  </acceptance_criteria>
  <done>The Forecast page shows two Recharts bar charts (lost-by-reason, lost-by-stage) alongside the existing stat tiles, both count-based (never dollar-value-based), both correctly reading `pipelineStage` and the `lostReason` category prefix per the locked data-shape rules — the Recharts-bar-chart-plus-stat-tiles layout decision (D-11) and FCST-02 are complete for the happy path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Overflow/long-text chart polish, empty states, prohibition re-verification</name>
  <behavior>
    - `bucketTopCategories({ Price: 10, Timing: 8, Competitor: 6, "No Budget": 5, Other: 4, Custom1: 3, Custom2: 2 }, 5)` (7 keys, limit 5) returns exactly 6 entries: the top 5 by count unchanged, plus a single `"Other"` entry summing the remainder.
    - `bucketTopCategories({ Price: 3, Timing: 1 }, 5)` (2 keys, under the limit) returns the input unchanged — no bucketing when there's nothing to fold.
  </behavior>
  <read_first>src/features/forecast/components/LostBreakdownChart.tsx (as built by Task 2 — the exact chart JSX being extended with tick-truncation and an empty-state branch); src/features/forecast/forecast-metrics.ts (as built by Task 1/2 — the exact module pattern this task's `bucketTopCategories` extends); src/features/forecast/components/StatTile.tsx (as built by Task 1 — confirm `tabular-nums break-words` is already present on the value span); src/features/forecast/components/ForecastPage.tsx (as built by Task 2 — the exact chart-row JSX being wrapped with the new bucket call)</read_first>
  <files>src/features/forecast/forecast-metrics.ts, src/features/forecast/components/LostBreakdownChart.tsx, src/features/forecast/components/ForecastPage.tsx</files>
  <action>
    In `forecast-metrics.ts`, add: `export function bucketTopCategories(counts: Record<string, number>, limit = 5): Record<string, number> { const entries = Object.entries(counts).sort((a, b) => b[1] - a[1]); if (entries.length <= limit + 1) return counts; const top = entries.slice(0, limit); const otherTotal = entries.slice(limit).reduce((sum, [, count]) => sum + count, 0); return Object.fromEntries([...top, ["Other", otherTotal]]); }` (UI-SPEC overflow backstop: folds everything beyond the top 5 by count into a single "Other" bucket so the x-axis never exceeds 6 bars).

    In `ForecastPage.tsx`: import `bucketTopCategories` and wrap only the by-reason chart's data: `<LostBreakdownChart title="Lost Deals by Reason" counts={bucketTopCategories(lostByReason)} />` — leave the by-stage chart unbucketed (only 4 possible stage categories, never exceeds the 6-bar limit).

    In `LostBreakdownChart.tsx`: add a custom tick renderer to `XAxis` for long category-name truncation: replace the plain `<XAxis dataKey="name" tick={{ fill: "#898781", fontSize: 12 }} />` with a `tick` render function that truncates any label longer than 14 characters to its first 14 characters plus an ellipsis for display, while Recharts' default `Tooltip` continues to show the full, untruncated `name` from the underlying data point (no separate wiring needed there — `Tooltip` already reads the complete `name` field from `data`, only the `XAxis` tick label needs shortening). Add an empty-state branch: when `Object.keys(counts).length === 0`, render `<p className="p-8 text-center text-sm text-muted-foreground">No lost deals recorded yet — this chart fills in once a deal is marked Lost.</p>` inside the `CardContent` instead of the `ResponsiveContainer`/`BarChart` (UI-SPEC empty state, Copywriting Contract).

    Re-verify (no code change expected, confirm only) that `ForecastPage.tsx` and `forecast-metrics.ts` still contain zero references to `PipelineToolbar`/`columnFilters`/`visibleGroups` after this task's edits — this plan's FCST-01/FCST-02 unfiltered-data prohibition must still hold.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>grep -c "bucketTopCategories" src/features/forecast/forecast-metrics.ts | grep -qx 1</automated>
    <automated>grep -c "bucketTopCategories" src/features/forecast/components/ForecastPage.tsx | grep -qx 1</automated>
    <automated>grep -c "No lost deals recorded yet" src/features/forecast/components/LostBreakdownChart.tsx | grep -qx 1</automated>
    <automated>! grep -Eq "PipelineToolbar|columnFilters|visibleGroups" src/features/forecast/components/ForecastPage.tsx</automated>
    <automated>! grep -Eq "PipelineToolbar|columnFilters|visibleGroups" src/features/forecast/forecast-metrics.ts</automated>
    <human-check>If seed data has 7+ distinct lost-reason categories among the 5 fixed enum values it can't (there are only 5 possible categories: Price/Timing/Competitor/No Budget/Other) — instead, confirm by code review that `bucketTopCategories` is wired and would fold a 7th+ category into "Other" if the enum ever grows. Delete all lost deals (or check a fresh/empty dataset) and confirm both charts show "No lost deals recorded yet — this chart fills in once a deal is marked Lost." instead of an empty axis. Confirm the Win Rate stat tile reads "— / No closed deals yet." before any deal has reached Won/Lost, never "0%" or "NaN%".</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `bucketTopCategories` is exported from `forecast-metrics.ts` and called from `ForecastPage.tsx` for the by-reason chart only
    - `LostBreakdownChart.tsx` renders the exact empty-state copy "No lost deals recorded yet — this chart fills in once a deal is marked Lost." when `counts` is empty
    - `ForecastPage.tsx` and `forecast-metrics.ts` still contain zero references to `PipelineToolbar`/`columnFilters`/`visibleGroups`
  </acceptance_criteria>
  <done>FCST-02's UI-SPEC backstop considerations (overflow bucketing, long-label truncation with full-string tooltip, chart empty state) are implemented; the Forecast page's stat tiles and charts handle the zero-data, overflow, and long-text states without breaking layout or misleading the user — Phase 4's Forecast page is feature-complete for FCST-01/FCST-02.</done>
</task>

</tasks>

## Artifacts This Phase Produces (this plan)

- `src/shared/utils/pipeline-group.ts` (edited) — adds `clearPatchFor`
- `src/features/pipeline/store/pipelineStore.ts` (edited) — `moveStage`/`moveToLost`/`moveToWon` call `clearPatchFor`
- `src/data/mock/mock-deals-repository.ts` (edited) — widened `update()` Pick type
- `src/features/forecast/forecast-metrics.ts` (new) — `computeRawPipelineValue`, `computeWeightedPipelineValue`, `computeWinRate`, `computeLostByReason`, `computeLostByStage`, `bucketTopCategories`
- `src/features/forecast/components/StatTile.tsx` (new)
- `src/features/forecast/components/ForecastPage.tsx` (new)
- `src/features/forecast/components/LostBreakdownChart.tsx` (new)
- `src/app/App.tsx` (edited) — Tabs nav, both views kept mounted
- `src/components/ui/tabs.tsx`, `src/components/ui/card.tsx` (new, via shadcn)
- `src/index.css` (edited) — adds `--chart-lost-bar`/`--chart-lost-grid` light+dark custom properties
- `package.json` (edited) — adds `recharts`

## Flagged Assumptions (specless probe fallback — surfaced, not dropped)

- **Edge probe, FCST-02, `unclassified` (unresolved):** the probe classifier could not map FCST-02 as a whole to a single data-shape edge category — the real ambiguity is the lost-by-reason aggregation key. REQUIREMENTS.md does not specify it. Resolved here as an explicit contract, not left ambiguous in code: group by the `lostReason` category prefix (split on the first `:`), never the raw `'{category}: {note}'` string and never the free-text `note` portion as its own chart category — see `must_haves.truths` and Task 2's `computeLostByReason`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Zustand store's in-memory `deals`, read by ForecastPage/forecast-metrics.ts, rendered via Recharts SVG) | This plan's entire data surface — no network boundary exists in this frontend-only prototype |
| npm registry -> local `node_modules` | `npm install recharts` (Task 2) pulls third-party code onto the dev machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering / Information Disclosure | Free-text `lostReason`/`paymentTerms` values rendered into chart tooltips/labels | low | mitigate | React's default JSX interpolation auto-escapes; Recharts renders tick/tooltip text as React-managed SVG `<text>` nodes, never via `dangerouslySetInnerHTML` |
| T-04-05 | Tampering / Integrity | Stale `lostReason`/contract-term data (CR-01) silently corrupting FCST-02's aggregate counts | medium | mitigate | Task 1's centralized `clearPatchFor` helper, called from all 3 stage-transition store actions, closes this at the source before any forecast math is written |
| T-04-06 | Repudiation | Forecast stat tiles/charts present computed figures with no audit trail of which deals/fields fed them | low | accept | Single-user, frontend-only prototype with no persistence layer; consistent with this project's existing disposition for judgment-tier, non-security concerns (e.g. `03.1-SECURITY.md`'s accepted-risk items) |
| T-04-SC | Tampering | npm install of `recharts` | high | mitigate | 04-RESEARCH.md's Package Legitimacy Audit already vetted `recharts` with an OK verdict (40.45M weekly downloads, real source repo, no postinstall script) — not `[ASSUMED]`/`[SUS]`, so no blocking human-verify checkpoint is required before this task's `npm install recharts` |

</threat_model>

<verification>
- `npm run build` exits 0 after all 3 tasks
- `clearPatchFor` is called from `moveStage`, `moveToLost`, and `moveToWon` — a deal bounced Lost -> Prospect -> Lost again never carries a stale reason
- The Forecast tab shows 3 stat tiles (raw value, weighted value, win rate) and 2 bar charts (lost-by-reason, lost-by-stage), all computed from the store's full, unfiltered `deals` array
- Switching between Pipeline and Forecast tabs never remounts either view
- Zero-lost-deals and zero-closed-deals states render dedicated copy, never "0%"/"NaN%"/a blank chart
</verification>

<success_criteria>
A user can open a read-only Forecast tab showing raw pipeline value, weighted/projected value, and win rate, plus a lost-deal breakdown by reason and by stage — satisfying FCST-01 and FCST-02 end to end — built on deal data that is no longer at risk of stale lost/won fields (CR-01 resolved).
</success_criteria>

<output>
Create `.planning/phases/04-forecast-pipeline-analysis/04-02-SUMMARY.md` when done
</output>
