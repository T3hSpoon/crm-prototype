---
phase: 01-pipeline-board-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - src/index.css
  - src/main.tsx
  - index.html
  - components.json
autonomous: false
requirements: [PIPE-01, PIPE-02, DEAL-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "node --version reports >=20.19.0 or >=22.12.0 before any Vite command runs"
    - "npm run build compiles the scaffold cleanly with typescript 5.9.3 pinned (not 7.0.2)"
    - "npm run dev boots a Vite dev server without errors, served over the @tailwindcss/vite + @vitejs/plugin-react pipeline"
    - "Tailwind v4 is wired CSS-first (@import \"tailwindcss\" in src/index.css) with no tailwind.config.js file in the repo root"
  artifacts:
    - path: "package.json"
      provides: "pinned dependency versions per 01-RESEARCH.md Standard Stack, including the typescript@5.9.3 and @dnd-kit/sortable@10.0.0 corrections to CLAUDE.md"
    - path: "vite.config.ts"
      provides: "react() + tailwindcss() plugins, \"@\" alias resolving to ./src"
    - path: "src/index.css"
      provides: "Tailwind v4 CSS-first entrypoint"
  key_links:
    - from: "vite.config.ts"
      to: "src/"
      via: "resolve.alias \"@\" -> path.resolve(__dirname, \"./src\")"
---

## Phase Goal

**As a** salesperson using the iDrive CRM prototype, **I want to** view every deal grouped by its pipeline stage and add or move deals through that pipeline, **so that** I always know where each prospect stands and can demo a working pipeline view before this merges into the real iDrive project.

> Note: ROADMAP.md's `**Goal:**` line for this phase ("Users can view the sales pipeline as a grouped table and manage deals moving through it") is not written in strict `As a / I want to / so that` syntax. The story above is assembled directly from that Goal line, its 3 Success Criteria, and PROJECT.md's Core Value statement — not invented — since this is a single-user, single-role prototype with no ambiguity about actor or outcome. Run `/gsd mvp-phase 1` if you want ROADMAP.md's Goal line itself reformatted to this syntax for consistency with later phases.

<objective>
Environment blocker resolution and project scaffold for the iDrive CRM Prototype's Walking Skeleton. Verifies/upgrades Node.js to Vite 8's minimum (RESEARCH.md's must-fix-first blocker), gates package installation behind a human legitimacy review, then scaffolds React 19.2 + Vite 8 + TypeScript 5.9.3, wires Tailwind v4 CSS-first + shadcn/ui, and installs every runtime/dev dependency this phase needs at the corrected versions RESEARCH.md verified against npm.

Purpose: nothing else in Phase 1 can run until Node meets Vite 8's minimum and the scaffold exists — this plan is the substrate every later plan builds on.
Output: a building, dev-server-runnable Vite + React + TS + Tailwind v4 + shadcn/ui project with all Phase 1 dependencies installed at their verified/corrected versions.
</objective>

<execution_context>
@C:/gh-repos/eld/.claude/gsd-core/workflows/execute-plan.md
@C:/gh-repos/eld/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/gh-repos/eld/.planning/PROJECT.md
@C:/gh-repos/eld/.planning/phases/01-pipeline-board-foundation/01-CONTEXT.md
@C:/gh-repos/eld/.planning/phases/01-pipeline-board-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and, if needed, upgrade Node.js to Vite 8's minimum</name>
  <read_first>01-RESEARCH.md Common Pitfalls #4 ("Local machine's Node.js version is below Vite 8's minimum"); 01-RESEARCH.md Environment Availability table</read_first>
  <files>N/A — environment check only, no repo files modified</files>
  <action>
    This MUST be the first task, before any scaffold command runs. Run `node --version` and parse it. Vite 8 requires Node `>=20.19.0` OR `>=22.12.0` (RESEARCH.md, vite.dev/guide). RESEARCH.md recorded the local baseline as v20.12.1, which is below the minimum. If the current version already satisfies the minimum, skip straight to Task 2. Otherwise upgrade in this order, re-checking `node --version` after each attempt: (a) if `nvm` (nvm-windows) is available, run `nvm install 22.12.0` then `nvm use 22.12.0`; (b) else attempt `winget install OpenJS.NodeJS.LTS -e --silent --accept-package-agreements --accept-source-agreements`, then open a fresh shell so PATH picks up the new install; (c) if neither path is available or `node --version` still fails the minimum after both attempts, stop and raise a `checkpoint:human-action` (gate="blocking-human") asking the user to install Node 20.19+/22.12+ from nodejs.org and confirm, then re-run the version check before proceeding. Do not run any `npm create vite` or `npm install` command until this check passes.
  </action>
  <verify>
    <automated>node -e "const [maj,min]=process.version.slice(1).split('.').map(Number); process.exit((maj>22)||(maj===22&&min>=12)||(maj===20&&min>=19)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --version` reports a version satisfying `>=20.19.0` or `>=22.12.0` before Task 2 begins
    - If an upgrade was required, the method used (nvm / winget / manual) is recorded in the plan's SUMMARY
  </acceptance_criteria>
  <done>Node.js meets Vite 8's minimum version requirement, confirmed before any scaffold step.</done>
  <reversibility rating="reversible">A local dev-machine Node version bump — no code or data impact, freely downgradable.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Package legitimacy pre-install review</name>
  <what-built>
    01-RESEARCH.md's Package Legitimacy Audit already ran `gsd-tools query package-legitimacy check` against the live npm registry for every package this phase installs. Seven packages were flagged heuristic `[SUS]` purely because their latest published version landed inside the audit tool's "too-new" window — not because they are actually new or untrusted. Evidence from RESEARCH.md's audit table:
    - vite — first published 2020-04-21, 175.9M weekly downloads, github.com/vitejs/vite
    - @tanstack/react-table — first published 2022-01-19, 19.9M weekly downloads, github.com/TanStack/table
    - zustand — first published 2019-04-09, 54.1M weekly downloads, github.com/pmndrs/zustand
    - react-hook-form — first published 2019-03-20, 60.4M weekly downloads, github.com/react-hook-form/react-hook-form
    - @hookform/resolvers — first published 2020-05-20, 50.8M weekly downloads, github.com/react-hook-form/resolvers
    - @faker-js/faker — first published 2022-01-10, 18.5M weekly downloads, github.com/faker-js/faker
    - @vitejs/plugin-react — first published 2021-09-20, 84.6M weekly downloads, github.com/vitejs/vite-plugin-react
    No `[SLOP]` verdicts were returned for any package this phase installs. No package in the install list carries a `postinstall` script.
  </what-built>
  <how-to-verify>
    Spot-check any/all of the seven packages above on npmjs.com before approving install — e.g. npmjs.com/package/vite, npmjs.com/package/zustand, npmjs.com/package/react-hook-form, npmjs.com/package/@tanstack/react-table, npmjs.com/package/@hookform/resolvers, npmjs.com/package/@faker-js/faker, npmjs.com/package/@vitejs/plugin-react. Confirm publisher identity and download counts roughly match the figures above. Given RESEARCH.md already classified every one of these as a documented false positive (long-established, high-trust, official-org-owned), this is intended as a fast rubber-stamp, not a deep investigation — unless something looks wrong on inspection.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with Task 3's install, or name any specific package you want re-investigated before install runs.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Scaffold Vite + React + TS, pin corrected stack versions, wire Tailwind v4 + shadcn/ui</name>
  <read_first>01-RESEARCH.md Code Examples ("Vite 8 + Tailwind v4 + shadcn/ui scaffold sequence", full command/config blocks); 01-RESEARCH.md Standard Stack table (Core + Supporting, including the two corrections vs CLAUDE.md); .claude/CLAUDE.md Technology Stack section (for packages RESEARCH.md did not correct)</read_first>
  <files>package.json, package-lock.json, vite.config.ts, tsconfig.json, tsconfig.app.json, src/index.css, src/main.tsx, index.html, components.json</files>
  <action>
    In the repo root, run `npm create vite@latest . -- --template react-ts`. Immediately override the scaffold's default TypeScript version: `npm install typescript@5.9.3 --save-exact` — NOT 7.0.2 (RESEARCH.md Pitfall 1: `typescript-eslint@8.68.0`'s peer range is `>=4.8.4 <6.1.0`, incompatible with TS 7's native compiler). Install runtime dependencies at the corrected versions: `@tanstack/react-table@9.2.3 @dnd-kit/core@6.3.1 @dnd-kit/sortable@10.0.0 @dnd-kit/utilities@3.2.2 zustand@5.0.15 react-hook-form@7.86.0 zod@4.4.3 @hookform/resolvers@5.9.1 @faker-js/faker@10.6.0 date-fns@4.4.0 clsx tailwind-merge`. Do NOT pin `@dnd-kit/sortable` to match `@dnd-kit/core`'s version string — RESEARCH.md Pitfall 3 confirms `@dnd-kit/sortable` is independently versioned at major 10, not 6.x. Both `@dnd-kit/core` and `@dnd-kit/sortable` are the classic/"Legacy" API generation (`DndContext`/`SortableContext`/`useSortable`) — a separate, independently-versioned next-generation dnd-kit package built around a `DragDropProvider` API exists but is NOT installed by this command and must never be imported this phase (RESEARCH.md Pitfall 2). Install `tailwindcss @tailwindcss/vite`; replace the entire contents of `src/index.css` with only the Tailwind v4 CSS-first entrypoint (delete the scaffold's default demo CSS). Edit `vite.config.ts` to import and register the `react()` and `tailwindcss()` plugins and add a `resolve.alias` mapping `"@"` to `path.resolve(__dirname, "./src")`. Add `baseUrl: "."` and `paths: { "@/*": ["./src/*"] }` to `compilerOptions` in both `tsconfig.json` and `tsconfig.app.json`. Install `-D @types/node` (required for the `path` import in `vite.config.ts`). Run `npx shadcn@latest init` (accept the Vite-project defaults), then `npx shadcn@latest add button dialog select input label form` — this generates `components.json` and the shadcn component source under `src/components/ui/`. Install dev dependencies: `-D eslint typescript-eslint prettier @vitejs/plugin-react`. Do NOT install `lucide-react` this phase — RESEARCH.md confirms no Phase 1 component uses icons; defer.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>node -e "process.exit(require('./package.json').devDependencies.typescript==='5.9.3'?0:1)"</automated>
    <automated>node -e "const fs=require('fs'); process.exit(fs.existsSync('tailwind.config.js')?1:0)"</automated>
    <human-check>Run `npm run dev`, visit the printed local URL, and confirm the default scaffold page renders with no console errors before Task 1 of 01-02-PLAN.md begins overwriting it.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `package.json` `devDependencies.typescript` is exactly `5.9.3`
    - `package.json` dependencies include `@dnd-kit/core@6.3.1` and a `@dnd-kit/sortable` entry independent of (not string-matching) the core version
    - `src/index.css` contains the Tailwind v4 `@import "tailwindcss";` line and no `@tailwind base/components/utilities` v3-era directives
    - No `tailwind.config.js` file exists in the repo root
    - `npm run dev` boots without error
  </acceptance_criteria>
  <done>A building, dev-server-runnable Vite + React 19.2 + TypeScript 5.9.3 project exists with Tailwind v4 + shadcn/ui wired and every Phase 1 runtime/dev dependency installed at its corrected version.</done>
</task>

</tasks>

## Artifacts This Phase Produces (this plan's contribution)

- `package.json` / `package-lock.json` — pinned dependency manifest (typescript 5.9.3, @dnd-kit/sortable independently versioned at major 10, all other RESEARCH.md-verified versions)
- `vite.config.ts` — `react()` + `tailwindcss()` plugins, `"@"` path alias
- `tsconfig.json` / `tsconfig.app.json` — `"@/*"` path mapping
- `src/index.css` — Tailwind v4 CSS-first entrypoint
- `components.json` — shadcn/ui CLI configuration
- `src/components/ui/{button,dialog,select,input,label,form}.tsx` — shadcn-generated component source (consumed by later plans)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local node_modules | Every `npm install`/`npx shadcn` command pulls third-party code onto the dev machine; this is the only real trust boundary this plan crosses (no user input, no network calls in the app itself yet) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (all Phase 1 packages, incl. the 7 heuristic-SUS ones) | high | mitigate | 01-RESEARCH.md's Package Legitimacy Audit (npm-registry-verified download counts, publish dates, repo URLs, no postinstall scripts) + Task 2's `checkpoint:human-verify gate="blocking-human"` runs BEFORE Task 3's install — never auto-approved, even in yolo/auto mode |
| T-01-01 | Information Disclosure | scaffold config files (vite.config.ts, tsconfig*.json, components.json) | low | accept | No secrets, API keys, or env vars are written by this plan — nothing here can leak; rationale documented, no mitigation needed |

</threat_model>

<verification>
- `npm run build` exits 0
- `npm run dev` starts a dev server that responds 200 on its printed local URL
- `package.json` reflects every version correction from 01-RESEARCH.md (typescript 5.9.3, @dnd-kit/sortable independently versioned)
- No `tailwind.config.js` present
</verification>

<success_criteria>
The repository contains a scaffolded, building, dev-server-runnable Vite + React + TypeScript project with Tailwind v4 + shadcn/ui wired and every Phase 1 dependency installed at its RESEARCH.md-verified version — ready for 01-02-PLAN.md to build the data seam on top of it.
</success_criteria>

<output>
Create `.planning/phases/01-pipeline-board-foundation/01-01-SUMMARY.md` when done
</output>
