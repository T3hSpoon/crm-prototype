---
phase: 01-pipeline-board-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/shared/types/deal.ts
  - src/shared/utils/pipeline-group.ts
  - src/data/deals-repository.ts
  - src/data/mock/mock-deals-repository.ts
  - src/data/mock/seed-data.ts
  - src/data/index.ts
  - src/features/pipeline/store/pipelineStore.ts
  - src/app/App.tsx
  - src/main.tsx
  - src/App.tsx
autonomous: true
requirements: [PIPE-01, PIPE-02, DEAL-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Deal models pipelineStage and outcome as two separate fields; PipelineGroup is always derived via toPipelineGroup, never stored directly on Deal"
    - "Every Deal.id (seed and newly created) is a crypto.randomUUID()/faker.string.uuid() value, never an array index or sequential counter"
    - "MockDealsRepository.list() resolves ~40 seeded deals distributed across all 4 pipelineStage values with some marked outcome: lost"
    - "usePipelineStore().load() populates deals from MockDealsRepository, and App.tsx renders those deals on screen through a real (not stubbed) call chain"
    - "No component or store file imports @dnd-kit/react or DragDropProvider"
  artifacts:
    - path: "src/shared/types/deal.ts"
      provides: "Deal, PipelineStage, DealOutcome, PipelineGroup, NewDealInput types"
    - path: "src/data/deals-repository.ts"
      provides: "DealsRepository interface (list/create/update)"
    - path: "src/data/mock/mock-deals-repository.ts"
      provides: "in-memory DealsRepository implementation seeded from seed-data.ts"
    - path: "src/features/pipeline/store/pipelineStore.ts"
      provides: "usePipelineStore Zustand store (deals, status, load, addDeal, moveStage)"
    - path: "src/app/App.tsx"
      provides: "tracer render proving the seed -> repository -> store -> screen path end-to-end"
  key_links:
    - from: "src/features/pipeline/store/pipelineStore.ts"
      to: "src/data/index.ts"
      via: "imports the dealsRepository singleton, never mock-deals-repository.ts directly"
    - from: "src/app/App.tsx"
      to: "src/features/pipeline/store/pipelineStore.ts"
      via: "useEffect calls usePipelineStore().load() on mount"
---

<objective>
Define the Deal data model and the repository seam per research/ARCHITECTURE.md and 01-RESEARCH.md Pattern 2, implement the mock repository and seed data, wire the Zustand store, and prove the entire read path end-to-end with a real render in the running app. This is the phase's tracer slice — the first task where a real user sees real seeded data reach the screen.

Purpose: every later plan (grouped board, add-deal, stage-move) is built entirely on top of this seam; getting the pipelineStage/outcome split right now avoids a data-model rework in Phase 3 (research/PITFALLS.md Pitfall 3).
Output: a working, typed data layer + store, and a dev server that visibly renders real seeded deals.
</objective>

<execution_context>
@C:/gh-repos/eld/.claude/gsd-core/workflows/execute-plan.md
@C:/gh-repos/eld/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/gh-repos/eld/.planning/phases/01-pipeline-board-foundation/01-RESEARCH.md
@C:/gh-repos/eld/.planning/phases/01-pipeline-board-foundation/01-PATTERNS.md
@C:/gh-repos/eld/.planning/phases/01-pipeline-board-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data contracts — Deal types, pipeline-group derivation, repository interface, mock implementation, seed data</name>
  <read_first>01-RESEARCH.md Architecture Patterns Pattern 2 (full code block: deal.ts + pipeline-group.ts); 01-RESEARCH.md "Don't Hand-Roll" table row 2 (stable ID rule); 01-RESEARCH.md Code Examples "@faker-js/faker v10 seed data generation" (full code block); 01-PATTERNS.md "pipelineStage / outcome split" and "Stable IDs" shared-pattern sections</read_first>
  <files>src/shared/types/deal.ts, src/shared/utils/pipeline-group.ts, src/data/deals-repository.ts, src/data/mock/mock-deals-repository.ts, src/data/mock/seed-data.ts, src/data/index.ts</files>
  <action>
    Create `src/shared/types/deal.ts` exporting: `PipelineStage` as the union `"prospect" | "lead" | "opportunity" | "deal"`; `DealOutcome` as `"open" | "won" | "lost"`; `PipelineGroup` as `"prospect" | "lead" | "opportunity" | "deal" | "lost"`; the `Deal` interface with fields `id: string`, `name: string`, `company: string`, `value: number`, `owner: string`, `closeDate: string` (ISO 8601), `pipelineStage: PipelineStage`, `outcome: DealOutcome`, `lostReason?: string`, `createdAt: string`; and `NewDealInput` covering the add-deal form's five required fields (`name`, `company`, `value`, `owner`, `closeDate`) plus `group: PipelineGroup` for the stage selector (per CONTEXT.md D-01/D-02).

    Create `src/shared/utils/pipeline-group.ts` exporting `toPipelineGroup(deal: Deal): PipelineGroup` — returns `"lost"` when `deal.outcome === "lost"`, otherwise returns `deal.pipelineStage` — and `fromPipelineGroup(group: PipelineGroup, previousStage?: PipelineStage): { pipelineStage: PipelineStage; outcome: DealOutcome }` — when `group === "lost"`, returns `{ pipelineStage: previousStage ?? "prospect", outcome: "lost" }` (preserves prior stage per research Assumption A2; defaults to `"prospect"` only when there is no prior stage, e.g. a brand-new deal added directly into Lost); otherwise returns `{ pipelineStage: group, outcome: "open" }`.

    Create `src/data/deals-repository.ts` exporting the `DealsRepository` interface: `list(): Promise<Deal[]>`, `create(input: NewDealInput): Promise<Deal>`, `update(id: string, patch: Partial<Pick<Deal, "pipelineStage" | "outcome" | "lostReason">>): Promise<Deal>`.

    Create `src/data/mock/seed-data.ts` exporting `seedDeals: Deal[]` — generate 40 deals via `faker.helpers.multiple(() => ..., { count: 40 })` (fall back to `Array.from({ length: 40 }, () => ...)` if `faker.helpers.multiple` is unavailable in the installed faker version). Every `id` via `faker.string.uuid()` — never a sequential counter or array index (Pitfall 1/5). Distribute `pipelineStage` evenly across the 4 values via `faker.helpers.arrayElement`, and set `outcome: "lost"` with roughly 15% probability (`faker.datatype.boolean({ probability: 0.15 })`), `outcome: "open"` otherwise (never generate seed deals with `outcome: "won"` this phase — RESEARCH.md Open Question 2 defers the explicit won-transition to Phase 3). Use `faker.company.buzzPhrase()` for `name`, `faker.company.name()` for `company`, `faker.person.fullName()` for `owner`, `faker.date.soon({ days: 90 }).toISOString()` for `closeDate`, `faker.number.int({ min: 5000, max: 250000 })` for `value`, `faker.date.recent({ days: 60 }).toISOString()` for `createdAt` — fictional generators only, never real prospect/company/person names.

    Create `src/data/mock/mock-deals-repository.ts` implementing `DealsRepository` as a class or factory closing over an in-memory `Deal[]` array initialized from `seedDeals`. `list()` resolves a copy of the array. `create(input)` builds a new `Deal` with `id: crypto.randomUUID()`, `createdAt: new Date().toISOString()`, and `pipelineStage`/`outcome` derived from `input.group` via `fromPipelineGroup(input.group)`, pushes it into the array, and resolves it. `update(id, patch)` finds the deal by `id` (never by array index — Pitfall 5), merges `patch`, and resolves the updated deal; if no deal matches `id`, reject/throw.

    Create `src/data/index.ts` exporting a single `dealsRepository` constant — an instance of the mock implementation, satisfying `DealsRepository`. This is the only file any component or the store may import from `src/data/`; nothing outside `src/data/mock/` may import `mock-deals-repository.ts` directly (research/ARCHITECTURE.md repository-seam rule, restated in 01-PATTERNS.md).
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>grep -c "outcome" src/shared/types/deal.ts</automated>
    <automated>grep -rl "@dnd-kit/react" src 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `Deal` declares both `pipelineStage` and `outcome` as distinct fields (confirmed by the grep above returning >=1 for `outcome` alongside `pipelineStage` appearing in the same file)
    - `MockDealsRepository` seeds from `seedDeals`, never from a hardcoded literal array in the repository file itself
    - Every generated `id` in `seed-data.ts` is produced via `faker.string.uuid()`, and `mock-deals-repository.ts`'s `create()` uses `crypto.randomUUID()` — neither uses `Date.now()`, an incrementing counter, or array index as an id
    - The `@dnd-kit/react` grep returns 0 matches (classic dnd-kit API only, unused this phase)
  </acceptance_criteria>
  <done>Deal/PipelineStage/DealOutcome/PipelineGroup types, the derivation utilities, the DealsRepository interface, the mock implementation, and 40 seeded deals all exist and compile.</done>
  <reversibility rating="costly">The pipelineStage/outcome split (not a flat stage field) is the exact shape Phase 3's lost-reason gate and won-transition are designed against; changing it later means touching every file that reads pipelineStage/outcome (store, all components, seed data) — a coordinated multi-file change, though no external contract or persisted data exists yet to migrate.</reversibility>
</task>

<task type="tracer">
  <name>Task 2: Zustand store + real end-to-end render — the phase's tracer slice</name>
  <read_first>01-RESEARCH.md Code Examples "Zustand 5 store with a repository-calling async action" (full code block); 01-RESEARCH.md System Architecture Diagram (Phase 1 slice); src/App.tsx (current scaffold-generated content from 01-01-PLAN.md, to be replaced)</read_first>
  <files>src/features/pipeline/store/pipelineStore.ts, src/app/App.tsx, src/main.tsx, src/App.tsx</files>
  <action>
    Create `src/features/pipeline/store/pipelineStore.ts` exporting `usePipelineStore` via Zustand's `create<PipelineState>()()` curried pattern. State: `deals: Deal[]` (initially `[]`), `status: "idle" | "loading" | "ready"` (initially `"idle"`). Actions: `load()` — sets `status: "loading"`, awaits `dealsRepository.list()`, sets `deals` and `status: "ready"`; `addDeal(input: NewDealInput)` — awaits `dealsRepository.create(input)`, appends the result to `deals` via `set({ deals: [...get().deals, created] })`; `moveStage(dealId: string, group: PipelineGroup)` — looks up the current deal by `id` in `get().deals`, computes the patch via `fromPipelineGroup(group, current?.pipelineStage)`, awaits `dealsRepository.update(dealId, patch)`, then replaces the matching deal in `deals` by `id` (never by array index) via `.map()`. Import `dealsRepository` only from `src/data/index.ts`.

    Move `src/App.tsx` to `src/app/App.tsx`, deleting the scaffold's default counter-button content entirely. The new `App.tsx` calls `usePipelineStore().load()` inside a `useEffect(() => { load(); }, [])` on mount, and — for this tracer only — renders a plain `<h1>Pipeline (tracer)</h1>` followed by a `<ul>` mapping `deals` to `<li key={d.id}>{d.name} — {d.company} — {toPipelineGroup(d)}</li>`. This is deliberately the thinnest real proof that seed data flows through `MockDealsRepository` -> `usePipelineStore` -> the screen; 01-03-PLAN.md replaces this raw list with the actual 5-group board — do not build any grouping/table UI in this task.

    Update `src/main.tsx`'s import to reference the new `./app/App` path (delete the old `src/App.tsx` file after the move).
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run dev &amp; DEV_PID=$!; sleep 3; node -e "fetch('http://localhost:5173').then(r=&gt;process.exit(r.ok?0:1)).catch(()=&gt;process.exit(1))"; kill $DEV_PID 2&gt;/dev/null</automated>
    <human-check>Visit the running dev server. Confirm a list of roughly 40 deal name/company/group rows renders under an "Pipeline (tracer)" heading — not a blank page and not the default Vite counter demo.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - The dev server responds 200 on its local URL
    - `App.tsx` calls `usePipelineStore().load()` in a mount-time `useEffect`, and the rendered list length matches `seedDeals.length` (40)
    - No file outside `src/data/` imports `mock-deals-repository.ts` directly (only `src/data/index.ts` does)
  </acceptance_criteria>
  <done>The dev server renders a real list of seeded deals sourced through MockDealsRepository -> usePipelineStore -> App.tsx — the data seam is proven end-to-end.</done>
</task>

</tasks>

## Artifacts This Phase Produces (this plan's contribution)

- `src/shared/types/deal.ts` — `Deal`, `PipelineStage`, `DealOutcome`, `PipelineGroup`, `NewDealInput`
- `src/shared/utils/pipeline-group.ts` — `toPipelineGroup(deal)`, `fromPipelineGroup(group, previousStage?)`
- `src/data/deals-repository.ts` — `DealsRepository` interface
- `src/data/mock/mock-deals-repository.ts` — `MockDealsRepository` in-memory implementation
- `src/data/mock/seed-data.ts` — `seedDeals: Deal[]`
- `src/data/index.ts` — `dealsRepository` singleton
- `src/features/pipeline/store/pipelineStore.ts` — `usePipelineStore` (`deals`, `status`, `load`, `addDeal`, `moveStage`)
- `src/app/App.tsx` — mounts, loads, and (this plan only) tracer-renders the deal list

<assumption_delta_decision>
The assumption-delta scanner flagged the phrase "move a deal from one pipeline stage to **another**" in ROADMAP.md's Phase 1 success criteria (a pluralization signal: "another"). Decision: **no-change**. Rationale: this is a routine single-deal, single-destination stage transition, not a structural singular->plural shift in the primary anchor. `moveStage(dealId: string, group: PipelineGroup)` already models exactly one deal moving to exactly one destination group at a time — the existing `pipelineStage`/`outcome` -> derived-`PipelineGroup` architecture (Pattern 2 above) already anchors this correctly. There is no second/alternate Deal identity or plural-move batch operation implied by the roadmap text; "another" is ordinary English for "a different one," not a request for multi-select or bulk stage moves.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository/store layer -> external network | No network call should ever originate from `src/data/` or `src/features/pipeline/store/` this phase — everything resolves against the in-memory array |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Tampering | `mock-deals-repository.ts` / `pipelineStore.ts` accidentally wired to a real network call | medium | mitigate | Structural: `MockDealsRepository` only ever touches an in-memory array; this plan's Task 2 acceptance criteria require the store to import `dealsRepository` only from `src/data/index.ts`, and 01-04-PLAN.md's final task adds a repo-wide grep gate confirming no `fetch`/`axios`/`XMLHttpRequest` call exists in `src/features/pipeline` |
| T-01-03 | Information Disclosure | `seed-data.ts` generated deal names/companies/owners | low | accept | Every field is generated by `@faker-js/faker`'s fictional generators (`faker.company.name()`, `faker.person.fullName()`) — never real prospect data, even for internal demos (research/PITFALLS.md guidance) |

</threat_model>

<verification>
- `npm run build` exits 0
- Dev server renders 40 real seeded deals via the store, not a hardcoded stub array in a component
- `Deal.pipelineStage` and `Deal.outcome` are distinct fields; no flat `stage` field exists anywhere in `src/shared/types/deal.ts`
</verification>

<success_criteria>
The data seam (types, repository interface, mock implementation, seed data) and the Zustand store exist, compile, and are proven end-to-end by a real render in the running dev server — the tracer slice for Phase 1.
</success_criteria>

<output>
Create `.planning/phases/01-pipeline-board-foundation/01-02-SUMMARY.md` when done
</output>
