---
phase: 03.1-lost-won-tracking
verified: 2026-09-11T00:00:00Z
status: passed
score: 11/15 must-haves verified
behavior_unverified: 4
overrides_applied: 0
behavior_unverified_items:

  - truth: "Dismissing the Lost popover via Escape/click-away calls zero store actions (moveToLost/moveStage never invoked on that path)."
    test: "Pick 'Lost' on a deal's StageSelect, then press Escape (or click away) without submitting."
    expected: "The popover closes; no network/store call fires; the deal's stage dropdown still shows its original group."
    why_human: "No test runner exists in this project (no vitest/jest, no test script in package.json) — code inspection shows no call path exists, but the runtime cancellation invariant is unexercised."

  - truth: "A rejected moveToLost call shows 'Couldn't save — try again.' inline, keeps the popover open, and preserves the selected category/note."
    test: "Force dealsRepository.update to reject (e.g. via devtools/mock), submit the Lost popover with a category chosen."
    expected: "Error text renders below the submit button; popover stays open; category/note remain exactly as entered."
    why_human: "Error-recovery state-preservation invariant; no automated test exists to exercise the rejection path."

  - truth: "Cancel or dismissing the Won contract-terms dialog resets the form and calls zero store actions."
    test: "Pick 'Won' on a deal, fill some fields, then click Cancel or press Escape."
    expected: "Dialog closes; form resets; no moveToWon/moveStage call fires; deal stays in its original group."
    why_human: "Cancellation/cleanup invariant; no test runner in this project to exercise it."

  - truth: "A rejected moveToWon call shows 'Update failed — your change wasn't saved. Try again.' inline, keeps the dialog open, and preserves all 4 entered field values."
    test: "Force dealsRepository.update to reject, submit the Won dialog with all 4 fields filled."
    expected: "Error text renders above the footer; dialog stays open; all 4 field values remain exactly as entered."
    why_human: "Error-recovery state-preservation invariant; no automated test exists to exercise the rejection path."
human_verification:

  - test: "Pick 'Lost' from any active-stage deal's StageSelect dropdown."
    expected: "A popover opens anchored to the dropdown with a Reason select, no category pre-selected, and 'Mark as Lost' disabled until a category is chosen. Picking a category and submitting moves the deal into the Lost group."
    why_human: "Visual/interactive confirmation of popover anchoring and live disabled-state requires a browser session (planner-deferred human-check, 03.1-01-PLAN.md Task 1)."

  - test: "Pick 'Lost', then press Escape before submitting."
    expected: "Popover closes; dropdown still shows the deal's original stage (not moved)."
    why_human: "Visual confirmation of dropdown revert requires a browser session (planner-deferred human-check, 03.1-01-PLAN.md Task 1)."

  - test: "Pick 'Lost', choose a category, type a note, submit; separately, pick 'Lost' on another deal, choose a category, leave note blank, submit."
    expected: "Both succeed; the note is preserved and concatenated into lostReason as '{category}: {note}' in the first case; the second succeeds with just the category."
    why_human: "Confirming persisted value end-to-end requires a browser/devtools session (planner-deferred human-check, 03.1-01-PLAN.md Task 2)."

  - test: "Pick 'Won' from any active-stage deal's StageSelect dropdown."
    expected: "The Mark Deal as Won dialog opens directly (no separate confirm screen) with 4 blank date/text fields plus a read-only Final Contract Value already showing the deal's current value. Filling Contract Start Date auto-fills Contract End Date to start + the deal's Contract Term (months), and remains editable. Filling the remaining fields and clicking 'Confirm Won' moves the deal into the Contracts group with a distinct yellow-accented header."
    why_human: "Visual/interactive confirmation of auto-prefill, dialog appearance, and group transition requires a browser session (planner-deferred human-check, 03.1-02-PLAN.md Task 1)."

  - test: "Reopen the Won dialog on another deal and click Cancel."
    expected: "Dialog closes; no group change occurs."
    why_human: "Visual confirmation requires a browser session (planner-deferred human-check, 03.1-02-PLAN.md Task 1)."

  - test: "Refresh the app on first load; separately, check a naturally-empty active-stage group."
    expected: "The Contracts group already shows at least one deal with its Final Contract Value visible; an empty active-stage group still reads 'No deals in this group yet.', not 'No contracts yet.'"
    why_human: "Visual confirmation of seed-data demoability and empty-state copy requires a browser session (planner-deferred human-check, 03.1-02-PLAN.md Task 2)."

  - test: "Confirm the 'Won' trigger mechanism (StageSelect dropdown opening a dedicated WonContractTermsDialog) satisfies the product intent behind ROADMAP.md's Success Criterion 3 ('clicking Next instead of Save replaces the modal's content in place with a contract terms form')."
    expected: "Stakeholder agreement that the as-built StageSelect-triggered dialog is an acceptable realization of 'gate Won behind a contract-terms form before the transition commits,' even though the originally-sketched mechanism (an in-place Add/Edit-modal 'Next' vs 'Save' swap) was not built."
    why_human: "03.1-UI-SPEC.md's own Assumptions Made #1 explicitly resolves this divergence and flags it 'for planner/UAT revisit' — this is a judgment call on UX intent, not a code defect."

  - test: "Review CR-01 (code review, 03.1-REVIEW.md): confirm whether moveToLost/moveToWon/moveStage must clear the opposing terminal-state fields (lostReason vs. the 4 contract-term fields) when a deal transitions away from Lost/Won via StageSelect."
    expected: "A decision on whether this is an acceptable gap for a frontend-only prototype (current state: reachable via the shipped dropdown with no bypass required) or must be fixed before Phase 4's forecast page consumes lostReason/contract-term data."
    why_human: "This is a genuinely reachable data-integrity bug distinct from the two prohibitions both plans already flagged and accepted (which cover bypassing the entry gate via direct moveStage calls, not stale-field leakage on exit from Lost/Won). Not a stated must-have in either plan, so not scored as a failed truth, but flagged here per this verifier's own judgment because it is reachable through the normal UI, not just a hypothetical non-UI code path."
---

# Phase 03.1: Lost & Won Tracking Verification Report

**Phase Goal:** Users can track deals that fell through with a reason, and capture contract terms when marking a deal as won, with won deals rolling up into a running contract list
**Verified:** 2026-09-11
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Marking a deal lost is blocked until a reason category is selected (LOST-01 / ROADMAP SC1) | ✓ VERIFIED | `LostReasonPopover.tsx:95` — `<Button type="submit" disabled={!form.watch("category")}>`; `lost-reason-schema.ts` requires `category` via `z.enum([...])` |
| 2 | Selecting "Lost" does not call `moveStage` — opens a Popover anchored to the trigger; Select stays bound to `currentGroup` until a write commits (D-01) | ✓ VERIFIED | `StageSelect.tsx:45-55` returns early on `group === "lost"`, never reaches the `moveStage` call on line 54; `Select value={currentGroup}` (line 73) is never reassigned locally |
| 3 | A lost deal moves to a distinct Lost group, separate from active pipeline stages; a deal belongs to exactly one `PipelineGroup` (LOST-02 / ROADMAP SC2) | ✓ VERIFIED | `pipeline-group.ts:9-12` — `toPipelineGroup` is a single-branch return (won → lost → pipelineStage), cannot return two groups |
| 4 | Dismissing the Lost popover via Escape/click-away calls zero store actions; the dropdown reverts automatically | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Code shows no call path (`StageSelect.tsx:64-69` only calls `setPendingGroup(null)`), but no test exercises the runtime cancellation invariant — see `behavior_unverified_items` |
| 5 | Submitting the Lost popover calls exactly one atomic `moveToLost` (single `dealsRepository.update()` call, no two-write race) | ✓ VERIFIED | `pipelineStore.ts:55-72` — exactly one `await dealsRepository.update(...)` inside `moveToLost` |
| 6 | A rejected `moveToLost` call shows "Couldn't save — try again." inline, keeps the popover open, preserves category/note | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Code shows the catch block sets `error` without calling `onClose` (`LostReasonPopover.tsx:46-55`), but no test exercises the rejection path |
| 7 | Lost popover opens with no category pre-selected, empty note; category+empty-note is valid/submittable; note+no-category is still blocked by the same gate | ✓ VERIFIED | `useForm` has no `category` default (`LostReasonPopover.tsx:40-42`); submit gate is solely `!form.watch("category")` |
| 8 | Selecting "Won" opens `WonContractTermsDialog` directly, gating the transition behind a contract-terms form before it commits (WON-01 / ROADMAP SC3) | ✓ VERIFIED (mechanism divergence flagged) | `StageSelect.tsx:50-53` intercepts "won" before any `moveStage` call; however the *trigger mechanism* (StageSelect dropdown + dedicated dialog) differs from ROADMAP SC3's literal wording ("clicking Next instead of Save... replaces the modal's content") — resolved via `03.1-UI-SPEC.md` Assumption #1, explicitly flagged "for planner/UAT revisit" — see human verification |
| 9 | The 4 contract-terms fields render in fixed order + read-only Final Contract Value; "Confirm Won" is blocked by zod-required validation until all 4 are filled (WON-01 / ROADMAP SC4) | ✓ VERIFIED | `WonContractTermsDialog.tsx:95-150` — Controllers in the exact order Start/End/Signed/Payment Terms, followed by a read-only `Field`; `wonContractTermsSchema` requires all 4 via `z.string().min(1, ...)`, so `form.handleSubmit` never calls `onSubmit` on an incomplete form |
| 10 | Submitting completes the Won transition — the deal appears in the distinct Contracts group with captured terms attached; the group is demoable from first load (WON-01/PIPE-03 / ROADMAP SC5) | ✓ VERIFIED | `pipelineStore.ts:74-92` (`moveToWon`); `GroupSection.tsx:47-52` (`won` → "Contracts" meta); `seed-data.ts:41,73-85` pre-seeds `isWon` deals with all 4 fields populated |
| 11 | Submitting the Won dialog calls exactly one atomic `moveToWon` (single `dealsRepository.update()` call) | ✓ VERIFIED | `pipelineStore.ts:74-92` — exactly one `await dealsRepository.update(...)` inside `moveToWon` |
| 12 | Cancel or dismissing the Won dialog resets the form and calls zero store actions | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Code shows `handleOpenChange(false)` only calls `form.reset`/`setError(null)` (`WonContractTermsDialog.tsx:79-85`), never `usePipelineStore`, but no test exercises the runtime cancellation invariant |
| 13 | A rejected `moveToWon` call shows `UPDATE_FAILED_MESSAGE` inline, keeps the dialog open, preserves all 4 entered values | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Code shows the catch block sets `error` without calling `form.reset`/`onOpenChange(false)` (`WonContractTermsDialog.tsx:66-77`), but no test exercises the rejection path |
| 14 | The Contracts group renders "No contracts yet." when empty; every other group (including Lost) keeps the generic "No deals in this group yet." copy | ✓ VERIFIED | `DealTable.tsx:160` — `{group === "won" ? "No contracts yet." : "No deals in this group yet."}` |
| 15 | The Contracts group renders identically to every other populated group (shell/header/overflow/count badge); no new sort/reorder logic introduced for Lost or Won | ✓ VERIFIED | `GroupSection.tsx` / `DealTable.tsx` share the same rendering path for all 6 groups; no sort/filter row model added (`getCoreRowModel()` only, confirmed in `DealTable.tsx` comment block) |

**Score:** 11/15 truths verified (4 present + wired, behavior-unverified — no test runner exists in this project to exercise the cancellation/error-recovery invariants)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/components/ui/popover.tsx` | `Popover`/`PopoverAnchor`/`PopoverContent` primitives | ✓ VERIFIED | Present, matches dialog.tsx's cn/radix-ui convention, exported and used by `StageSelect.tsx` |
| `src/components/ui/textarea.tsx` | `Textarea` primitive | ✓ VERIFIED | Present, mirrors input.tsx's class convention, used by `LostReasonPopover.tsx` |
| `src/features/pipeline/components/lost-reason-schema.ts` | `lostReasonSchema`/`LostReasonFormValues` | ✓ VERIFIED | Present, 5-option enum + optional note |
| `src/features/pipeline/components/LostReasonPopover.tsx` | Gated Lost-reason form | ✓ VERIFIED | Present, wired into `StageSelect.tsx`'s Popover content |
| `src/features/pipeline/store/pipelineStore.ts` | `moveToLost` atomic action | ✓ VERIFIED | Present, single `update()` call, try/catch/rethrow |
| `src/shared/types/deal.ts` | `PipelineGroup` gains `"won"`; `Deal` gains 4 optional contract fields | ✓ VERIFIED | Present (line 21, 79-82) |
| `src/shared/utils/pipeline-group.ts` | Parallel `"won"` branch | ✓ VERIFIED | Present (lines 10, 31-33) |
| `src/features/pipeline/components/won-contract-terms-schema.ts` | `wonContractTermsSchema`/`WonContractTermsFormValues` | ✓ VERIFIED | Present, all 4 fields required |
| `src/features/pipeline/components/WonContractTermsDialog.tsx` | WON-01 gated contract-terms modal | ✓ VERIFIED | Present, wired as sibling of StageSelect's Popover block |
| `src/features/pipeline/store/pipelineStore.ts` | `moveToWon` atomic action | ✓ VERIFIED | Present, single `update()` call, try/catch/rethrow |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `StageSelect.tsx` | `LostReasonPopover.tsx` | `pendingGroup` gates "lost" selection; rendered as shared Popover's content | ✓ WIRED | Confirmed in `StageSelect.tsx:43-53,87-91` |
| `LostReasonPopover.tsx` | `pipelineStore.ts` | `usePipelineStore.getState().moveToLost(...)` | ✓ WIRED | Confirmed in `LostReasonPopover.tsx:48` |
| `pipelineStore.ts` | `pipeline-group.ts` | `fromPipelineGroup("lost", ...)` inside `moveToLost` | ✓ WIRED | Confirmed in `pipelineStore.ts:59` |
| `StageSelect.tsx` | `WonContractTermsDialog.tsx` | `pendingGroup === "won"` renders the dialog as a sibling of the Popover | ✓ WIRED | Confirmed in `StageSelect.tsx:93-95` |
| `WonContractTermsDialog.tsx` | `pipelineStore.ts` | `usePipelineStore.getState().moveToWon(...)` | ✓ WIRED | Confirmed in `WonContractTermsDialog.tsx:69` |
| `usePipelineGroups.ts` | `pipeline-group.ts` | `GROUPS` array (includes "won") partitioned via `toPipelineGroup` | ✓ WIRED | Confirmed in `usePipelineGroups.ts:7,26` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| Contracts group (`GroupSection`/`DealTable`) | `deals` (won subset) | `usePipelineGroups()` → `usePipelineStore((s) => s.deals)` → `dealsRepository.list()` → `MockDealsRepository` seeded from `seedDeals` | ✓ FLOWING | Real in-memory data, not a static/mock fallback |
| `WonContractTermsDialog`'s Final Contract Value | `deal.value` | `usePipelineStore((s) => s.deals.find(...))` | ✓ FLOWING | Live store lookup, not hardcoded |
| Lost/Won `lostReason`/contract fields on the `Deal` record | `moveToLost`/`moveToWon` patch | `dealsRepository.update(dealId, patch)` → `MockDealsRepository.update` merge | ✓ FLOWING (with caveat) | Real write path; see CR-01 below — the record is written correctly for the *entering* transition, but stale opposing fields are never cleared on exit |

### Behavioral Spot-Checks

Step 7b: **SKIPPED** — no test runner is configured in this project (`package.json` has no `test` script; no Vitest/Jest dependency; no `*.test.*`/`*.spec.*` files exist outside `node_modules`). No dev server was started per this verifier's "do not start servers" constraint. All behavior-dependent truths above that would normally be upgraded by a passing named test are instead left ⚠️ PRESENT_BEHAVIOR_UNVERIFIED and routed to human verification.

### Probe Execution

No probes declared in either PLAN/SUMMARY and no `scripts/*/tests/probe-*.sh` files exist in this repository. Step 7c: **SKIPPED** (not applicable — not a migration/tooling phase).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| LOST-01 | 03.1-01-PLAN.md | Mark a deal lost, requiring a reason before the move completes | ✓ SATISFIED | Truths 1, 2, 6, 7 |
| LOST-02 | 03.1-01-PLAN.md | Lost deal moves to a separate, distinct Lost group | ✓ SATISFIED | Truths 3, 4, 5, 14 (lost keeps generic copy), 15 |
| PIPE-03 | 03.1-02-PLAN.md | Won deals appear as a distinct "contracts made so far" group | ✓ SATISFIED | Truths 3, 10, 14, 15 |
| WON-01 | 03.1-02-PLAN.md | Marking Won requires completing a contract-terms form before joining the contracts list | ✓ SATISFIED | Truths 8, 9, 10, 11, 12, 13 |

No orphaned requirements: REQUIREMENTS.md's traceability table maps exactly LOST-01, LOST-02, PIPE-03, WON-01 to Phase 3.1, matching both plans' declared `requirements:` frontmatter exactly.

### Anti-Patterns Found

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in any file modified by this phase. No stub `return null`/empty-array/empty-object patterns found in rendering paths. The following issues were surfaced by the separate code-review pass (`03.1-REVIEW.md`) and independently confirmed against the current code during this verification:

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/features/pipeline/store/pipelineStore.ts` | 55-92 | CR-01: `moveToLost`/`moveToWon` never clear the *opposing* terminal-state fields (`lostReason` vs. the 4 contract-term fields); generic `moveStage` clears neither | 🛑 Data-integrity (not a stated must-have; see human verification) | Reachable via the shipped `StageSelect` dropdown with no bypass required — e.g. mark a deal Lost, then pick "Prospect" from the same dropdown, and `lostReason` silently persists on what is now an open deal. `toPipelineGroup` ignores these stale fields today, so nothing currently renders incorrectly, but the stored `Deal` record is wrong and would corrupt Phase 4's FCST-02 loss-reason breakdown if consumed naively |
| `src/data/mock/seed-data.ts` | 73-77 | CR-02: seeded `contractEndDate` is anchored on `new Date()` (today) instead of the seeded `contractStartDate` | ⚠️ Warning (demo-data quality) | Contradicts the app's own business rule implemented correctly in `WonContractTermsDialog.tsx:58-62` (`contractEndDate = contractStartDate + contractTermMonths`); a seeded Won deal's dates can visibly disagree with the rule a user just saw demonstrated live in the dialog — confirmed still present in current code |
| `src/data/mock/mock-deals-repository.ts` | 69-76 | WR-01: `MockDealsRepository.update`'s declared `Pick<Deal,...>` patch type was not widened to include the 4 new contract fields (only the `DealsRepository` interface was) | ⚠️ Warning (type-safety drift) | Works today only because TS checks method params bivariantly and the implementation blindly spreads `patch` — confirmed still present; any future change relying on the implementation's own declared type would silently drop the 4 fields |
| `src/features/pipeline/components/LostReasonPopover.tsx` | 40-42 | WR-02: `useForm` omits `defaultValues`, unlike every other form in the codebase | ℹ️ Info | Triggers React's "uncontrolled→controlled" console warning on every popover open (note field starts `undefined`); confirmed still present |
| `src/features/pipeline/store/pipelineStore.ts` | 47-92 | WR-03: no guard against a stale `current` snapshot racing a concurrent update across `moveToLost`/`moveToWon`/`moveStage` | ℹ️ Info | Documented as a known, accepted gap (no double-submit guard per both plans' backstop items); compounds CR-01 |
| `src/features/pipeline/components/LostReasonPopover.tsx` | 80, 95 | IN-02: `category`'s validation-error branch is unreachable dead code given the disabled-submit gate | ℹ️ Info | Cosmetic; confirmed still present |

These were raised by the separate code-review gate (advisory, never blocking) and are **not** counted as failed must-haves here, since neither CR-01 nor CR-02 is stated as a truth in either plan's `must_haves`. CR-01 is escalated to Human Verification below because it is reachable through the shipped UI itself (not a hypothetical non-UI bypass, unlike the two prohibitions both plans already flagged and explicitly accepted as unresolved).

### Human Verification Required

See YAML frontmatter `human_verification` and `behavior_unverified_items` for the full structured list. Summary:

1. **Lost popover open/disabled-state/submit-moves-deal** — visual confirmation (planner-deferred, 03.1-01-PLAN.md Task 1).
2. **Lost popover Escape-dismiss reverts dropdown** — visual confirmation (planner-deferred, 03.1-01-PLAN.md Task 1); also a ⚠️ PRESENT_BEHAVIOR_UNVERIFIED cancellation invariant.
3. **Lost note persistence (with/without note)** — visual/devtools confirmation (planner-deferred, 03.1-01-PLAN.md Task 2).
4. **Won dialog open/auto-prefill/submit-moves-deal** — visual confirmation (planner-deferred, 03.1-02-PLAN.md Task 1).
5. **Won dialog Cancel is a no-op** — visual confirmation (planner-deferred, 03.1-02-PLAN.md Task 1); also a ⚠️ PRESENT_BEHAVIOR_UNVERIFIED cancellation invariant.
6. **Contracts group demoable on first load + empty-state copy correctness** — visual confirmation (planner-deferred, 03.1-02-PLAN.md Task 2).
7. **Won-trigger mechanism vs. ROADMAP SC3's literal wording** — a judgment call on whether the as-built StageSelect-triggered dialog satisfies the product intent behind the roadmap's originally-sketched "Next vs Save in the Add Deal modal" mechanism; already flagged by 03.1-UI-SPEC.md itself as "for planner/UAT revisit."
8. **CR-01 data-integrity decision** — whether stale `lostReason`/contract-term fields left behind after a deal transitions away from Lost/Won (reachable via the shipped dropdown, no bypass needed) must be fixed before Phase 4's forecast page (FCST-02) consumes this data, or is acceptable to defer.
9. Two rejection-path error-recovery invariants (Lost and Won) are ⚠️ PRESENT_BEHAVIOR_UNVERIFIED — code shows the correct catch-block structure, but no automated test exercises a forced repository rejection.

### Gaps Summary

No must-have truth, artifact, or key link **failed**. `npm run build` (`tsc -b && vite build`) exits 0. All 10 required artifacts exist, are substantive, and are wired into their declared key links; data flows from the mock repository through to rendered output with no static/hardcoded fallbacks. Both requirement sets (LOST-01/LOST-02 and PIPE-03/WON-01) are satisfied by the code as written.

The phase is not cleanly `passed`, however, for two reasons:

- Four truths describing cancellation and error-recovery invariants are **present and wired but behaviorally unverified** — this project has no test runner at all, so these can only be confirmed by a human exercising the UI (or by a future phase adding Vitest).
- Both plans deliberately deferred a set of interactive human-checks to end-of-phase (per `workflow.human_verify_mode = end-of-phase`), none of which were exercised by the autonomous executor — each SUMMARY.md's own `coverage` entries say so explicitly (`human_judgment: true`, "not exercised by this autonomous execution").

Additionally, this verifier's own review surfaced one data-integrity bug (CR-01) reachable through the shipped UI that neither plan's `must_haves.prohibitions` anticipated (those cover bypassing the gate via direct `moveStage` calls from non-UI code, not stale-field leakage when a deal legitimately transitions away from Lost/Won through the same dropdown). It does not fail any stated must-have truth, so it is not a blocking gap — but it is flagged prominently for human decision given it is reachable through normal use, not a hypothetical edge case, and could corrupt the data Phase 4's forecast page is expected to consume.

---

_Verified: 2026-09-11_
_Verifier: Claude (gsd-verifier)_
