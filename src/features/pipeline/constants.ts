/**
 * Copywriting Contract "Error state" row, 02-UI-SPEC.md — the single shared
 * copy used by every inline-commit surface (DealDetailDrawer's core fields
 * and "Reset to sum", EditableCell, LineItemsTable) so the message can never
 * drift between call sites.
 */
export const UPDATE_FAILED_MESSAGE = "Update failed — your change wasn't saved. Try again.";
